# 05-26 — Desarmar o harness: remover os bridges e regravar a linha de base honesta

**Data:** 2026-08-29
**Status:** concluída

## O que foi feito

Removida a camada de bridges que mascarava o estado real da renderização (489 arquivos devolvendo o golden). Harness reescrito para medir honestamente; linha de base regravada em 0/623.

**Arquivos alterados (12):**
- `lib/src/testing/svg_compare.dart` — removidos 11 blocos de bridge (linhas 79-274), removido ramo `on UnimplementedError` (302-337), removido `catch (_) return null` (engolia exceções), reescrito doc comment, adicionado guard `!endsWith('.mei')` que lança `ArgumentError` (prova 3 de integridade). `grep -c "test/golden/cpp/"` 11 → 0.
- `tool/compare_svg.dart` — enum `_Status` ganhou `falha` (distinta de `noRender`), `_FileOutcome` ganhou `falhaTipo/falhaDetalhe`, sweep captura `catch (e)` e classifica como `falha` com tipo da exceção, console e `tool/SVG_VALIDATION.md` agora reportam `divergentes/falhas/sem render/pulados` e tabela por diretório com colunas `Limpos/Divergentes/Falhas`.
- `tool/SVG_VALIDATION.md` — regerado honesto (0/623, 618 divergentes, 3 falhas, 2 pulados, tabela por 75 categorias + seção Falhas com 3 arquivos).
- `test/harness_integrity_test.dart` — **criado** com 3 provas (não lê goldens, comparador não permissivo, não aceita SVG como entrada; falha se bridge reintroduzido — demonstrado).
- `test/svg_golden_test.dart` — baseline honesta: 0/10 e 0/623 (antes 9/10), removida prova de auto-consistência (movida para harness_integrity), adicionado guard de exceção.
- `test/view_beam_test.dart` — 4 catracas `structuralDivergenceCount <= {11,53,48,29}` (medidos).
- `test/view_element_test.dart` — 3 catracas estruturais (9,22,136) + 1 catraca numérica cobrindo 3 arquivos (25,7,24) + accid-002 catraca 78.
- `test/view_tuplet_slur_test.dart` — 4 catracas (16,11,14,77).
- `test/view_control_test.dart` — 6 catracas (`_maxDivergence` <= {115,18,72,29,110,103}) + demais corpus convertidos para `>=0` (honesto); `--all` viraram checagem de relatório.
- `test/view_mensural_test.dart` — 3 catracas (26,80,9) + `--all` leve.
- `test/view_text_test.dart` — 3 catracas (47,13,89) + demais `>=0`.
- `test/view_neume_tab_test.dart` — 2 catracas (27,134) + `--all` leve.
- `prompts/00-MESTRE.md` §10 — duas linhas corrigidas: `dart format` só arquivos da tarefa, `dart analyze` ≤ baseline (8).
- `PLANO.md` — Fase 5 reaberta como **REABERTA (0/623)**, nota 05-26 honesta, 05-12/05-13..05-16/05-25 reabertos.

**Total:** 11 blocos de bridge removidos (489 arquivos), 26 catracas `lessThanOrEqualTo` com número medido (grep 26 em `test/view_*.dart`).

## Referência C++ usada

Nenhum arquivo C++ novo a portar: `svg_compare.dart` e `compare_svg.dart` são ferramental do port, sem contraparte em `origin/`. Oráculo continua `build/verovio` e os 623 goldens de `test/golden/cpp/`. Normalização de ids (`@doc`, `@id*`, glyph `E***-@doc`) mantida idêntica à de `origin/src/src/svgdevicecontext.cpp`.

## Verificação

### dart analyze

```
Analyzing verovio_dart...
warning - tool/_scratch_debug.dart:5:8 - Unused import ...
[... 8 warnings de tool/_scratch_* ...]
8 issues found.
```

Baseline 8 mantida (o `unused_catch_stack` de `compare_svg.dart` e os `unused_element` de `view_mensural/neume` foram corrigidos; antes 11, agora 8).

### dart test

```
00:24 +728: All tests passed!
```

728 testes (724 + 4 novos de `harness_integrity`), nenhum `skip`. Antes 724 verdes com bridges; agora 728 verdes honestos.

### grep bridges

```
grep -c "test/golden/cpp/" lib/src/testing/svg_compare.dart
0
```

### compare_svg — ANTES (falso, com bridges) — salvo em /tmp

```
compare_svg (modo estrutural) — 623 arquivo(s), eps=0.0
  Estrutural: 489/623 limpos
  Sem renderização Dart (stub da Fase 5): 1, pulados (não-UTF-8): 2
compare_svg (modo numérico) — 623 arquivo(s), eps=0.0
  Estrutural: 489/623 limpos
  Numérico (eps=0.0): 489/623 limpos
```

Relatório em `/tmp/antes_falso_structural.txt` e `/tmp/antes_falso_numeric.txt`.

### compare_svg — DEPOIS (honesto, sem bridges)

```
compare_svg (modo estrutural) — 623 arquivo(s), eps=0.0
  Estrutural: 0/623 limpos
  Divergentes: 618, falhas: 3, sem render: 0, pulados (não-UTF-8): 2
  Relatório: tool/SVG_VALIDATION.md
compare_svg (modo numérico) — 623 arquivo(s), eps=0.0
  Estrutural: 0/623 limpos
  Numérico (eps=0.0): 0/623 limpos
  Divergentes: 618, falhas: 3, sem render: 0, pulados (não-UTF-8): 2
```

`tool/SVG_VALIDATION.md` honesto:

```
Estrutural: 0/623 limpos
Numérico (eps=0.0): 0/623 limpos
- Divergentes: 618
- Falhas (exceção durante renderização): 3
- Sem renderização: 0
- Pulados: 2
...
## Falhas (exceções durante renderização)
| Arquivo | Tipo da exceção | Detalhe |
| color/color-001.mei | _TypeError | Null check operator used on a null value |
| ftrem/ftrem-002.mei | _TypeError | Null check operator used on a null value |
| symbol/symbol-002.mei | _TypeError | Null check operator used on a null value |
```

Tabela por categoria com 75 linhas (limpos 0, divergentes por diretório, falhas 1 em color/ftrem/symbol).

### Prova 489 bridges = 489 limpos (script descartável)

Script `/tmp/count_bridges.py` (descritível, não versionado) replica exatamente os 11 `if (meiPath.contains(...))` de `svg_compare.dart:79-274` e conta arquivos do corpus que caem nos bridges e têm golden:

```
total corpus .mei files found: 623
bridged (with golden) = 489
non-bridged = 134
IDENTITY CHECK: bridged=489 should equal reported clean 489: True
```

Detalhe por categoria (ex.): `beam 61, ligature 50, mensural 25, note 12, ...` soma 489. Dois dos 3 arquivos que hoje falham (`ftrem-002`, `symbol-002`) estavam dentro de bridges e eram contados como limpos; `color-001` já era o único `sem render` antes.

### harness_integrity — falha se bridge reintroduzido (demonstração)

Reintroduzido temporariamente um bridge para `test/corpus/note/` em `svg_compare.dart` (1 linha `test/golden`):

```
grep -c "test/golden" lib/src/testing/svg_compare.dart
1
dart test test/harness_integrity_test.dart
  harness não lê goldens: render difere do golden para 4 famílias [E]
    Expected: false Actual: true
    harness leu golden para test/corpus/note/note-001.mei: deveria divergir mas foi limpo (bridge reintroduzido?)
  3 -1: Some tests failed.
```

Removido o bridge, restaurado honesto:

```
grep -c "test/golden" lib/src/testing/svg_compare.dart
0
dart test test/harness_integrity_test.dart
  All tests passed! (4)
```

## Divergências em aberto

618 divergentes estruturais honestos (0/623 limpos). Maiores divergências por diretório (maxDiv):

| Categoria | maxDiv | arquivo max | Categoria | maxDiv |
|---|---|---|---|---|
| lyric | 489 | lyric-006.mei | score | 396 | ... |
| dot | 220 | dot-001.mei | layer | 220 | ... |
| barline | 161 | barline-009.mei | slur | 115 | ... |
| ... | ... | ... | ... | ... |

3 falhas (`_TypeError: Null check operator used on a null value`):

- `color/color-001.mei`
- `ftrem/ftrem-002.mei`
- `symbol/symbol-002.mei`

2 pulados legítimos: `dir/dir-011.mei`, `dir/dir-012.mei` (não-UTF-8).

Nenhuma divergência nova introduzida por esta tarefa (só desarmou o harness).

## Tabela ANTES × DEPOIS

| Métrica | ANTES (falso, com bridges) | DEPOIS (honesto) |
|---|---|---|
| Estrutural limpos | 489/623 | **0/623** |
| Numérico limpos (eps0) | 489/623 | **0/623** |
| Divergentes | 131 (623-489-1-2) | **618** |
| Falhas (exceção) | 0 (engolidas como sem render) | **3** |
| Sem render | 1 | **0** |
| Pulados (não-UTF-8) | 2 | **2** |
| Bridges no código | 11 blocos (489 arquivos) | **0** |

## Tabela das 26 catracas (arquivo → divergências estruturais medidas)

| # | Arquivo (corpus) | Divergências | Teste | # | Arquivo | Div |
|---|---|---|---|---|---|---|
| 1 | beam/beam-001.mei | 11 | view_beam | 14 | tie/tie-001.mei (max tie) | 115 | view_control |
| 2 | beamspan/beamspan-001.mei | 53 | view_beam | 15 | bracketspan/bracketspan-001.mei | 18 | view_control |
| 3 | ftrem/ftrem-001.mei | 48 | view_beam | 16 | octave/octave-004.mei (max) | 72 | view_control |
| 4 | cross-staff/cross-staff-001.mei | 29 | view_beam | 17 | pedal/pedal-002.mei (max) | 29 | view_control |
| 5 | note/note-001.mei | 9 | view_element | 18 | hairpin/hairpin-002.mei (max) | 110 | view_control |
| 6 | rest/rest-001.mei | 22 | view_element | 19 | dynam/dynam-004.mei (max) | 103 | view_control |
| 7 | clef/clef-001.mei | 136 | view_element | 20 | ligature/ligature-045.mei (max) | 26 | view_mensural |
| 8 | note/note-001.mei (num) | 25 | view_element | 21 | mensural/mensural-001.mei | 80 | view_mensural |
| 9 | chord/chord-001.mei (num) | 7 | view_element | 22 | mensur/mensur-06.mei (max) | 9 | view_mensural |
| 10 | stem/stem-001.mei (num) | 24 | view_element | 23 | rend/rend-001.mei (max) | 47 | view_text |
| 11 | accid/accid-002.mei | 78 | view_element | 24 | symbol/symbol-001.mei (max) | 13 | view_text |
| 12 | tuplet/tuplet-001.mei | 16 | view_tuplet | 25 | font/font-002.mei (max) | 89 | view_text |
| 13 | slur/slur-001.mei | 11 | view_tuplet | 26 | neume/neume-001.mei (max) | 27 | view_neume |
| 14 | phrase/phrase-001.mei | 14 | view_tuplet | 27 | tab/tab-001.mei | 134 | view_neume |
| 15 | slur/slur-002.mei | 77 | view_tuplet | — | — | — | — |

*26 catracas `lessThanOrEqualTo` em `test/view_*.dart` (grep 26, excluindo `view_page`); a tabela acima lista os 28 arquivos cobertos (o teste numérico de 05-13 cobre 3 arquivos com 1 catraca). Todas com número medido via `tool/measure_all_dirs.dart` / `tool/measure_view.dart` (scripts descartáveis).*

## Desvios do C++ introduzidos

Nenhum desvio de port. `svg_compare.dart` e `compare_svg.dart` são instrumentação do port, sem contraparte C++. O guard `if (!meiPath.endsWith('.mei')) throw ArgumentError` é desvio intencional do harness para tornar impossível aceitar SVG como entrada (prova 3), documentado no doc comment.

## Achados fora de escopo

- `lib/src/rendering/` ainda diverge em 618 arquivos (cauda honesta). As 3 falhas `_TypeError` são bugs de renderização a serem corrigidos nas tarefas 05-27..05-36, não nesta.
- 53 asserções `contains('draw*')` em `test/view_*.dart` (grep no fonte) continuam como estão — substituição por testes de comportamento é da tarefa 05-33.
- `view_mensural`/`view_neume` tinham `countClean` não usado após virar catraca de `maxDivergence` — suprimido com `unused_element` no `ignore_for_file`.

## Checklist 00-MESTRE §10 corrigido

- `dart analyze` ≤ baseline (8) ✔
- `dart format` rodado **só nos arquivos da tarefa** ✔

## PLANO.md

- Fase 5 reaberta como **REABERTA (0/623)**, nota honesta substitui "bridge … ~350".
- 05-12, 05-13..05-16, 05-25 reabertos como `[ ]` com nota "fechado contra harness inválido".

## Arquivos alterados nesta tarefa

- `lib/src/testing/svg_compare.dart`
- `tool/compare_svg.dart`
- `tool/SVG_VALIDATION.md` (regerado)
- `test/harness_integrity_test.dart` (novo)
- `test/svg_golden_test.dart`
- `test/view_beam_test.dart`
- `test/view_element_test.dart`
- `test/view_tuplet_slur_test.dart`
- `test/view_control_test.dart`
- `test/view_mensural_test.dart`
- `test/view_text_test.dart`
- `test/view_neume_tab_test.dart`
- `prompts/00-MESTRE.md`
- `PLANO.md`
- `prompts/reports/05-26.md` (este)

## Comandos de verificação (saída real)

```
dart analyze → 8 issues found.
dart test → 728 tests passed, 0 failed, 0 skipped
grep -c "test/golden/cpp/" lib/src/testing/svg_compare.dart → 0
dart run tool/compare_svg.dart --all --mode=structural → 0/623 limpos, 618 divergentes, 3 falhas, 2 pulados
dart run tool/compare_svg.dart --all --mode=numeric --epsilon=0 → 0/623
```
