# 2026-08-30-medium-01 — O instrumento: pinpointing de chamadas de desenho

**Data:** 2026-08-30
**Status:** concluída

## O que foi feito

- `cpp_probe/patches/05-38.patch` — instrumentação de `SvgDeviceContext` (`origin/src/src/svgdevicecontext.cpp`) + helpers em `include/vrv/vrvprobe.h` (`DrawSeqCounter`, `DrawObjectStack`, `Hex4`, `U32ToHex`). Só acréscimos, `grep -c '^-[^-]'` = 0, `05-38` no fim de `cpp_probe/patches/ORDER`.
- `cpp_probe/build.sh 05-38` — rebuild incremental ok.
- `lib/src/testing/draw_recorder.dart` — `class DrawRecorder extends SvgDeviceContext` sobrescreve as mesmas primitivas da Parte 1, emite mesmo formato JSON (`fn`, `seq`, `path`, `id` + todos args numéricos / códigos de glifo com nome do parâmetro C++), `records` + `toJsonl()`.
- `test/support/render_family.dart` — `RecordingDeviceContext` agora `extends DrawRecorder` (única fonte de verdade para `path`/`seq`/formato, sem duas implementações divergindo).
- `tool/probe_diff.dart` — `dart run tool/probe_diff.dart test/corpus/<fam>/<arq>.mei`, `--dir`, `--rank` (agrupa primeiras divergências por `(fn, origem provável)`).
- `tool/gen_probe_fixtures.sh` — `cpp_probe/run.sh 05-38` por família, grava em `test/fixtures/cpp/05-38/<fam>/<arq>.mei.jsonl` (e cópia plana), verifica invariante SVG idêntico e aborta no primeiro `diff` não vazio.
- Fixtures gerados para `clef` (7), `note` (12), `beam` (42), `accid` (14), `chord` (10), `ligature` (50), `mensural` (25), `tuplet` (22), `lyric` (16), `dynam`/`dir`/`arpeg`/`pedal`/`neume`/`hairpin`/`gliss`/`dot` — total 274+flat (275 usados no `--rank`).

## Referência C++ usada

- `origin/src/include/vrv/svgdevicecontext.h:39-97` (interface `SvgDeviceContext` — `DrawLine`, `DrawPolyline`, `DrawRectangle`, `DrawRoundedRectangle`, `DrawText`, `DrawMusicText`, `StartGraphic`, `EndGraphic`, `ResumeGraphic`, `RotateGraphic`, `StartText`, `EndText`, `DrawQuadBezierPath`, `DrawCubicBezierPath`, `DrawCubicBezierPathFilled`, `DrawBentParallelogramFilled`).
- `origin/src/src/svgdevicecontext.cpp:249,418,429,453,466,670,693,717,740,797,866,888,918,954,959,1003,1071,1079,1153` (corpos das primitivas instrumentadas).
- `origin/src/src/view_graph.cpp:279,297,334,359` (`View::DrawSmuflCode`, `DrawSmuflLine`, `DrawSmuflString`, `DrawThickBezierCurve` — SMuFL → `DrawMusicText` e `DrawCurve`).
- `origin/src/include/vrv/vrvprobe.h` (EXEMPLO) + `cpp_probe/README.md` (§ `path` = `probe::Path`).
- `origin/src/src/view_element.cpp:418,652,733,933,1121,1228,1629` (origens prováveis para `fn+contexto`).

## Verificação

### Pré-condições

```
$ cd verovio_dart && bash tool/phase5_status.sh
==================================================================
 FASE 5 — placar
==================================================================
-- 5.1 / 5.2 / 5.3  dívida de tipagem em lib/src/rendering --------
| arquivo | linhas | as dynamic | catch (_) | ignore_for_file |
| view_control.dart | 4676 | 237 | 280 | 1 |
...
TOTAIS  5.1 as dynamic: 442   5.2 catch (_): 609   5.3 ignore_for_file: 8
-- 5.6  fidelidade de SVG ----------------------------------------
  Estrutural: 116/621 limpos
  Numérico (eps=0.0): 7/621 limpos
  - Falhas (exceção durante renderização): 0
```

```
$ ls ../build/verovio && ls ../build-probe/build/verovio && cat ../cpp_probe/README.md | head
build/verovio
build-probe/build/verovio
# `cpp_probe/` — instrumentar o C++ e extrair dados de referência
```

### Invariante SVG instrumentado == limpo (5 famílias)

```
$ for mei in verovio_dart/test/corpus/note/note-001.mei \
  verovio_dart/test/corpus/clef/clef-002.mei \
  verovio_dart/test/corpus/beam/beam-001.mei \
  verovio_dart/test/corpus/chord/chord-001.mei \
  verovio_dart/test/corpus/accid/accid-001.mei; do
  build/verovio -r verovio_dart/assets/data -x 12345 -o /tmp/clean.svg $mei
  cpp_probe/run.sh 05-38 $mei /tmp/probe_05-38.jsonl --svg /tmp/probe.svg
  diff /tmp/clean.svg /tmp/probe.svg && echo "DIFF OK $mei"
done
DIFF OK verovio_dart/test/corpus/note/note-001.mei
DIFF OK verovio_dart/test/corpus/clef/clef-002.mei
DIFF OK verovio_dart/test/corpus/beam/beam-001.mei
DIFF OK verovio_dart/test/corpus/chord/chord-001.mei
DIFF OK verovio_dart/test/corpus/accid/accid-001.mei
```

Gerado pelo `tool/gen_probe_fixtures.sh` (cada arquivo verifica o `diff` e aborta se não vazio): `gen_probe_fixtures.sh: 7/7 (clef) ... 12/12 (note) ... invariante SVG ok`.

### Patch

```
$ cpp_probe/mkpatch.sh 05-38
cpp_probe/mkpatch.sh: /home/.../cpp_probe/patches/05-38.patch (2 arquivos, 245 linhas acrescentadas, 0 removidas)
$ grep -c '^-[^-]' cpp_probe/patches/05-38.patch
0
$ tail -n 5 cpp_probe/patches/ORDER
04g
04h
05-12
05-38
```

### dart analyze

```
$ dart analyze
Analyzing verovio_dart...
...
8 issues found.
```

Baseline 8 (só `tool/_scratch_*.dart`), sem supressão nova.

### dart test

```
$ dart test  (timeout 400s)
...
03:10 +719: All tests passed!
```

719 testes, 0 falhas (baseline 281 + tarefas 05-xx).

### probe_diff — arquivo limpo (prova 3)

```
$ dart run tool/probe_diff.dart test/corpus/clef/clef-002.mei
test/corpus/clef/clef-002.mei — 0 divergências (limpo)
```

Um dos 7 numericamente limpos (`clef/clef-002.mei`) — zero divergências, como exigido.

### probe_diff — mutação no Dart (prova 1)

Mutação temporária em `lib/src/testing/draw_recorder.dart: drawMusicText x -> x+1` (simula `x - xCorr` → `x - xCorr + 1` num método já limpo):

```
$ dart run tool/probe_diff.dart test/corpus/clef/clef-002.mei
test/corpus/clef/clef-002.mei
  seq 17  fn=DrawSmuflCode  path=measure[1]/staff[1]/layer[1]/clef[staffDef]
    x:      esperado 117   obtido 118   (Δ 1)
    y:      esperado 1630   obtido 1630
    code:   esperado E05C   obtido E05C
    text:   esperado E05C   obtido E05C
    setSmuflGlyph:      esperado 0   obtido 0
  origem provável: View::DrawClef (view_element.cpp:418)
```

Desfeito — volta a `0 divergências`.

### probe_diff — fixture ausente (prova 2)

```
$ mv test/fixtures/cpp/05-38/clef/clef-002.mei.jsonl /tmp/bak
$ dart run tool/probe_diff.dart test/corpus/clef/clef-002.mei
fixture ausente — gere com `tool/gen_probe_fixtures.sh clef`
  procurado em: test/fixtures/cpp/05-38/clef/clef-002.mei.jsonl, test/fixtures/cpp/05-38/clef-002.mei.jsonl
$ mv /tmp/bak test/fixtures/cpp/05-38/clef/clef-002.mei.jsonl
$ dart run tool/probe_diff.dart test/corpus/clef/clef-002.mei
test/corpus/clef/clef-002.mei — 0 divergências (limpo)
```

Mensagem exata, por arquivo/família.

### probe_diff --rank (fila inicial de 5.6)

```
$ dart run tool/probe_diff.dart --dir=test/corpus --rank
probe_diff --rank — 621 arquivo(s) sob test/corpus
  limpos: 1, divergentes: 273, falhas: 0, fixture ausente: 347
  fila de causas (fn + origem provável) — quantos arquivos cada causa destrava:
   198  DrawLine :: View::DrawStaff / DrawHorizontalLine (view_graph.cpp:40)
         ex.: test/corpus/accid/accid-001.mei, test/corpus/accid/accid-002.mei
    37  StartGraphic :: SvgDeviceContext::StartGraphic (svgdevicecontext.cpp:249) — View::DrawLayerElement
         ex.: test/corpus/arpeg/arpeg-003.mei, test/corpus/beam/beam-026.mei
    33  DrawLine :: View::DrawLine (view_graph.cpp) -> SvgDeviceContext::DrawLine (svgdevicecontext.cpp:866)
         ex.: test/corpus/arpeg/arpeg-001.mei, test/corpus/arpeg/arpeg-005.mei
     2  DrawSmuflCode :: View::DrawKeySig (view_element.cpp:1121)
         ex.: test/corpus/chord/chord-004.mei, test/corpus/note/note-001.mei
     2  StartText :: View::DrawTextString / SvgDeviceContext::StartText (svgdevicecontext.cpp:1003)
         ex.: test/corpus/chord/chord-006.mei, test/corpus/lyric/lyric-008.mei
     1  DrawSmuflCode :: View::DrawSmuflCode (view_graph.cpp:279) / DrawSmuflString (view_graph.cpp:334) / DrawSmuflLine (view_graph.cpp:297)
         ex.: test/corpus/note/note-004.mei
```

Com as 274 fixtures atuais só 6 causas distintas aparecem; as 15 primeiras são as 6 acima (restante empatada). A fila completa cresce quando mais famílias forem geradas sob demanda.

## Divergências em aberto

Nenhuma divergência do instrumento — o instrumento apenas mede. As 273 divergências de desenho listadas acima são o backlog de 5.6, encabeçado por `DrawLine` de pentagrama (198 arquivos) e `StartGraphic` (37). Cada uma será um prompt `small` (Haiku) que instrumenta mais fundo dentro da `origem provável`.

## Desvios do C++ introduzidos

- `lib/src/testing/draw_recorder.dart`: `DrawSmuflCode`/`DrawSmuflString`/`DrawSmuflLine` mapeados sobre `DrawMusicText` (único conduit SMuFL de `SvgDeviceContext`), com heurística `text.runes.length==1 → Code`, `>3` e todos iguais → `Line`, igual ao C++ `probe::Emit` de `DrawMusicText`. `DrawCurve` genérico para `DrawQuadBezierPath`/`DrawCubicBezierPath`/`DrawCubicBezierPathFilled`/`DrawBentParallelogramFilled`. `meiUnset` vs `VRV_UNSET` mantido com nome C++ nos registros (`x`/`y`/`width`/`height`).
- `tool/probe_diff.dart`: `gId`/`gClass` voláteis (ex.: `l1vz8wis` vs `vBJMO9Y` para `systemMilestone`) comparados por **xpath** (`probe::Path` / `cppPath`) em vez de literal de semente — `cppIdToPath`/`dartIdToPath` resolvem o `path` do objeto referenciado; só diverge se os `path`s diferirem. Confirmado pelo dono em 2026-08-30.

## Achados fora de escopo

- `view_element.cpp: DrawMeterSig` com `x - xCorr` e `y - yCorr` já diverge em `note-001.mei` (`y: 1627 vs -1253`, `DrawKeySig`), indicando conversão `ToDeviceContextY` ainda não espelhada em algum ramo — deixado para prompt 02.
- `view_control.cpp` ainda com `237 as dynamic` / `280 catch (_)` (medido `tool/debt_report.dart`), maior devedor de 5.1/5.2.

