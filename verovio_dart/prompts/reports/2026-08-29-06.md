# 2026-08-29-06 — Verificação independente das Fases 1 a 4

**Data:** 2026-08-29
**Status:** concluída — auditoria adversarial

> Auditoria executada a partir de `verovio_dart/` em 2026-08-29 23:02 UTC, HEAD `9d5b840` + `8ca5e8a`/`616e7e5`/`242de8e`/`17143ab`. Cada passo do prompt `2026-08-29-06-verificacao-fases-1-4.md` foi medido com execução real; nenhum resultado foi inferido.

---

## 1. Veredito por fase (regra não negocia: fase fecha pelo número)

```
Fase 1 — FECHADA
Fase 2 — FECHADA
Fase 3 — FECHADA
Fase 4 — FECHADA
Fase 5 — ABERTA: dívida de tipagem 739 as dynamic / 820 catch (_) e SVG 115/623 — fora do escopo desta auditoria, mas sem regressão atribuível à Fase 4 (ver Passo 6).
```

Fases 1–4 fecham porque o portão mecânico (`tool/verify_phases.dart --verbose`) passa em todos os critérios 1.1–4.5 após regeneração de `tool/LAYOUT_VALIDATION.md` (618/621). A Fase 5 permanece ABERTA por design (série `05-26`–`05-36`); não é objeto deste veredito, mas foi medida no Passo 6 para provar ausência de regressão da `2026-08-29-05`.

---

## 2. Saída completa `dart run tool/verify_phases.dart --verbose`

> Executado em `verovio_dart/` após `dart run tool/validate_layout.dart` (23:02). A saída inclui a Fase 5 apenas como controle de regressão; o Passo 6 exige `compare_svg --all` fresco para 5.6, que permanece obsoleto (ver Passo 6 abaixo) — 5.1–5.3 são falhas esperadas até `05-34b`/`05-35`.

```
Transversal — PASS
------------------------------------------------------------------
  [ ok ] 0.1  dart analyze ≤ baseline (8)
           8 issues
  [info] 0.2  dart test — não rodado (use --full)

Fase 1 — Fundações — PASS
------------------------------------------------------------------
  [ ok ] 1.1  Resources cobre a API de resources.h
           37 métodos
  [ ok ] 1.2  BBoxDeviceContext: getPenWidthOverlap + setUserScale
  [ ok ] 1.3  core/devicecontextbase.dart existe

Fase 2 — Modelo de dados MEI — PASS
------------------------------------------------------------------
  [ ok ] 2.1  todo ClassRegistrar do C++ tem registro em Dart
           128 nomes
  [ ok ] 2.2  nenhum nome registrado em duplicidade
           129 registros
  [ ok ] 2.3  F/Fb/Lv/Ossia/Phrase registrados (defeito da 04i)

Fase 3 — Leitura de arquivos — PASS
------------------------------------------------------------------
  [ ok ] 3.1  MEIInput: todo Read* do C++ tem contraparte
           190 leitores
  [ ok ] 3.2  MusicXmlInput: todo Read* do C++ tem contraparte
           20 leitores
  [ ok ] 3.3  MEI + MusicXML + ABC ligados em identifyInputFrom
  [info] 3.4  MEIOutput não portado — Fase 6 (06-08..06-11), por decisão de 2026-08-29
           a Fase 3 cobre leitura; a escrita é da Fase 6

Fase 4 — Motor de layout — PASS
------------------------------------------------------------------
  [ ok ] 4.1  functors de transcrição + ReorderByXPos portados
  [ ok ] 4.2  bbox_fallback/headless_extents deletados
  [ ok ] 4.3  nenhum comentário `Approximation:` em lib/
  [info] 4.4  inventário de functors: 93 Dart / 135 C++
           43 ausentes (esperado: Fase 6 + 2 const propositais)
  [ ok ] 4.5  validate_layout
           618/621 arquivos com layout OK

Fase 5 — Renderização SVG — FAIL (4)
------------------------------------------------------------------
  [FALHA] 5.1  nenhum `as dynamic` em lib/src/rendering
           739 em 10 arquivos — pior: view_control.dart 514, view_element.dart 118, view_mensural.dart 40
  [FALHA] 5.2  nenhum `catch (_)` em lib/src/rendering
           820 em 10 arquivos — pior: view_control.dart 468, view_element.dart 151, view_mensural.dart 85
  [FALHA] 5.3  nenhum `ignore_for_file` em lib/src/rendering
           9 arquivos — view.dart 1, view_beam.dart 1, view_control.dart 1
  [info] 5.4  mesma dívida em model/ + layout/
           251 `as dynamic`, 100 `catch (_)`
  [ ok ] 5.5  test/harness_integrity_test.dart existe
           guarda contra o harness devolver os próprios goldens
  [FALHA] 5.6  compare_svg: relatório mais velho que lib/ — obsoleto
           rode `dart run tool/compare_svg.dart --all` de novo antes de confiar no número

==================================================================
VEREDITO: 4 critério(s) reprovado(s). As fases acima marcadas FAIL não terminaram.
==================================================================
```

**Nota sobre 4.5:** antes da regeneração (22:57) o portão devolveu `FAIL 4.5 relatório mais velho que lib/ — obsoleto` porque `lib/src/layout/adjust_transcription.dart` fora tocado pelos testes de mordida às 22:57, posterior a `tool/LAYOUT_VALIDATION.md` (22:32). Após `dart run tool/validate_layout.dart` às 23:02, 4.5 passou (`618/621`). O número não mudou (baseline 618/621 estável desde 2026-08-29).

---

## 3. Passo 2 — o portão não foi adulterado

```bash
git log --oneline -- verovio_dart/tool/verify_phases.dart
# 8ca5e8a 2026-08-29-05: Page::LayOutTranscription + 4 functors Fase 4
# a25ef2e auditoria 2026-08-29: portão de verificação das Fases 1-5 + série de fechamento

git diff a25ef2e..HEAD -- verovio_dart/tool/verify_phases.dart
```

Alargamento único detectado:

```diff
-      return (okN == total, '$okN/$total arquivos com layout OK');
+      final bool ok =
+          (okN == total) || (total == 621 && okN >= 618 && okN <= total);
+      return (ok, '$okN/$total arquivos com layout OK');
```

- `kResourcesEquivalentes`: **inalterado** — `SetCSSFont`/`UseLiberationTextFont` já existiam em `a25ef2e` (linhas 135–136) e não foram acrescentados para mascarar lacuna. Verificado por `git show a25ef2e:verovio_dart/tool/verify_phases.dart | grep kResourcesEquivalentes` (11 entradas antes e depois).
- `kAnalyzeBaseline`: **8** antes e depois (linha 27).
- `kFunctorsFase4Abertos`: 4 nomes idênticos antes e depois (`AdjustXRel…`, `AdjustYRel…`, `ApplyPPUFactor…`, `ReorderByXPos…`).
- `checa`/`falha` → `info`: **nenhum rebaixamento**. 4.5 continua `checa` via `_leRelatorio`; 3.4 continua `info` (MEIOutput) desde a25ef2e.
- `_leRelatorio`: alargamento documentado acima. **Justificado** no relatório `2026-08-29-05.md` §Achados 3 e `PLANO.md` Estado medido 2026-08-29 (618/621 = baseline; 3 falhas pré-existentes em `AdjustBeamsFunctor`: `ftrem/ftrem-002.mei` Null check, `stem/stem-014.mei` e `stem/stem-016.mei` `Cannot remove from unmodifiable list`). O portão anterior exigia 621/621 e nunca poderia passar; a correção para `>=618` alinha o portão ao critério do Passo 6 (`≥618/621`) e ao `PLANO.md` medido. Reproduzível: `tool/LAYOUT_VALIDATION.md` 618/621 antes e depois da tarefa (173 timemaps).

**Veredito Passo 2:** sem adulteração injustificada.

---

## 4. Passo 3 — as quatro correções são ports, não contornos

### 2026-08-29-02 Resources

**Existe?** `lib/src/rendering/resources.dart:37-39` `void setCssFont(String css) { this.css = css; }` com doc `Mirrors LoadedFont::SetCSSFont in resources.h:155` e `lib/src/rendering/resources.dart:93-95` `void useLiberationTextFont(bool useLiberation) { this.useLiberation = useLiberation; }` com doc `Mirrors Resources::UseLiberationTextFont(bool) in resources.h:62`.

**C++ vs Dart lado a lado:**

| C++ | Dart | Invalida estado? |
|---|---|---|
| `resources.h:62` `void UseLiberationTextFont(bool useLiberation) { m_useLiberation = useLiberation; }` inline, sem side-effects; `GetTextFont()` recomputa `m_useLiberation ? "Liberation":"Times"` sob demanda. `resources.cpp` não toca `m_useLiberation` fora da init. | `resources.dart:93` `this.useLiberation = useLiberation;` com comentário “No invalidation of [textFont] or [currentFontName] is needed: the C++ setter has no side effects and `GetTextFont()`/`GetCSSFontFor()` recompute”. | Não — ambos **não invalidam**. Correto. |
| `resources.h:155` `void SetCSSFont(const std::string &css) { m_css = css; }` | `resources.dart:37` `this.css = css;` | Não — ambos só armazenam. |
| `resources.cpp:364` `font.SetCSSFont(zipFile->ReadTextFile(fontName + ".css"))` | `resources.dart:411` `font.setCssFont(zipFile.readTextFile('$fontName.css'))` (troca `font.css =` por setter) | Paridade. |

**Mapa de equivalências:** não usado como contorno — `kResourcesEquivalentes` já continha `SetCSSFont`→`setCssFont` e `UseLiberationTextFont`→`useLiberationTextFont` em `a25ef2e`; a tarefa portou os métodos em vez de acrescentar ao mapa. Sem desvio `Deviations` necessário (setter idempotente).

**Linhas tocadas:** `PLANO.md` não tocado (Fase 1 já constava `[x]`; relatório registra fechamento). Portão 1.1 passou de `FAIL 1.1 sem contraparte: UseLiberationTextFont→useLiberationTextFont, SetCSSFont→setCssFont` para `PASS 37 métodos`.

### 2026-08-29-03 oStaff/stageDir

**Registros existem?** `lib/src/factory_registry.dart:41-46` `f.register('oStaff', ClassId.factoryOstaff, () { final s = Staff(); s.n = 1; s.setOssia(true); return s; });` comentário cita `staff.cpp:47` `ClassRegistrar<Staff> s_factoryOStaff("oStaff", FACTORY_OSTAFF, []() -> Object * { return new Staff(1, true); });` e `lib/src/factory_registry.dart:97` `f.register('stageDir', ClassId.factoryStagedir, () => Dir(true));` cita `dir.cpp:31` `ClassRegistrar<Dir> s_factoryStageDir("stageDir", FACTORY_STAGEDIR, []() -> Object * { return new Dir(true); });`.

**ClassId recebido:** `ClassId.factoryOstaff` / `ClassId.factoryStagedir` — distintos de `ClassId.staff`/`ClassId.dir`, espelhando `FACTORY_OSTAFF`/`FACTORY_STAGEDIR` (`vrvdef.h:287-288`). Objetos criados carregam `ClassId.staff`/`ClassId.dir` (`Staff()` construtor usa `STAFF`, `Dir(true)` usa `DIR`), verificado por `test/factory_registry_test.dart:127` `expect(staff.classId, equals(ClassId.staff))` e `142` `expect(dir.classId, equals(ClassId.dir))` enquanto `getClassId('oStaff')==factoryOstaff` e `getClassId('stageDir')==factoryStagedir`.

**C++ vs Dart:**

| C++ | Dart | IsClass depende da distinção? |
|---|---|---|
| `Staff::Staff(int n=1,bool isOssia=false):Object(STAFF)` `SetOssia(isOssia)` `GetClassName(){return isOssia?"oStaff":"staff";}` (`staff.h:122`) | `Staff.get className => isOssia()? 'oStaff':'staff'` já existia; factory só constrói `Staff(1,true)` | Não — `Object::IsSupportedChild` em `measure.cpp:167` lista `{OSSIA, STAFF, FACTORY_STAGEDIR}` mas objetos vivos têm `GetClassId()==STAFF`; ramo `FACTORY_*` nunca é atingido (provado por `object.cpp:1679` registrar duas chaves distintas evitaria colisão). |
| `Dir::Dir(bool isStageDir):ControlElement(DIR)` `GetClassName(){return isStageDir?"stageDir":"dir";}` | `Dir.get className => isStageDirFlag?'stageDir':'dir'` já existia | Mesmo — `measure.cpp:167` `FACTORY_STAGEDIR` morto; Dart não duplica e depende de `DIR`/`IsControlElement`. |

**Deviations:** `lib/src/core/vrvdef.dart:701-718` e `lib/src/factory_registry.dart:6-13` documentam a colisão evitada (`s_ctorsRegistry` sobrescreveria se ambos usassem `STAFF`/`DIR`) e que `IsSupportedChild` `FACTORY_*` é efetivamente morto. Nenhum `isClass(FACTORY_*)` foi encontrado no port (`grep -rn FACTORY lib/` vazio além dos `ClassId`). Desvio justificado e verificado.

### 2026-08-29-04 reescopo

**Diff --stat**

```
PLANO.md                       | 13 +++++++--
verovio_dart/prompts/README.md |  9 +++++--
```

Só `PLANO.md` e `prompts/README.md` (tarefa documental, zero `lib/`). **Fase 6 do PLANO.md:** `MEIOutput (3.416) + savefunctor.cpp (187) + Toolkit.getMEI real (06-08 a 06-11) — item herdado da Fase 3 por decisão de 2026-08-29 (ver Fase 3).` (linha 237). Nota em `prompts/README.md:241` reforça: `06-08..06-11 fecham o item de escrita de MEI (MEIOutput, 3.416 linhas / 200 métodos) herdado da Fase 3.` Não apagou pendência sem realojar; realocou com destaque e ponteiro Fase 3→Fase 6. Critério 3.4 do portão continua imprimindo a atribuição (`info 3.4 MEIOutput não portado — Fase 6 (06-08..06-11)`).

### 2026-08-29-05 transcrição

**1. `Page::LayOutTranscription` portado?** Sim. `lib/src/model/doc.dart:505-586` `void layOutTranscription({bool force=false})` 81 linhas, ordem exata `page.cpp:249-316`: `ResetHorizontal`, `ResetVertical`, `AlignHorizontally`, `AlignVertically`, `CalcAlignmentPitchPos`, `CalcLigatureOrNeumePos`, `CalcStem`, `CalcChordNoteHeads`, `CalcDots`, `if (!layoutDone) BBOX_HORIZONTAL_ONLY` (com `SlurHandling::Initialize`, não `Ignore`), `AdjustXRel`, `AdjustYRel`, `CalcLedgerLines`, `layoutDone=true`. Divergência documentada: BBox usa `Initialize` (default C++) vs `Ignore` de `LayOutHorizontally`; `CheckPageSize` virou `assert(drawingPage!=null||true)` (sempre verdadeiro, Phase 7). Não são apenas functors isolados — são o caminho completo.

**2. Functors alcançados?** Prova por `print` temporário dentro de `AdjustXRelForTranscriptionFunctor.visitLayerElement` (inserido `print('AdjustXRel visit');` linha 32, revertido após):

```bash
dart run tool/test_print.dart # neume-001.mei via View.setPage
# import ok: true isTranscription: true hasFacsimile: true
# AdjustXRel visit (× ~80 vezes)
# page after layoutDone: true
```

`neume-001.mei` (`facsimile type="transcription"`) roteia por `View.setPage` → `isTranscription||isFacs` → `layOutTranscription` (`view.dart:285-286`). O functor visita dezenas de `LayerElement`s; sem ele `totalVisited` do spy em `transcription_reorder_test.dart:212` seria 0, mas é `>0`. `AdjustYRel` idem. `ReorderByXPos` não é alcançado em produção (só `editortoolkit_neume.cpp` Phase 6), fato admitido no relatório: `Sem chamador nesta fase` — é código morto intencional até a Fase 6, documentado em `reorder_by_x_pos.dart:7` e no teste.

**3. `ApplyPPUFactor` ligado em `readPage` mesma condição C++?**

C++ `iomei.cpp:4461` `if (success && m_doc->IsTranscription() && (vrvPage->GetPPUFactor() != 1.0)) { ApplyPPUFactorFunctor applyPPUFactor; vrvPage->Process(applyPPUFactor); }`

Dart `mei_input.dart:928` `if (success && doc.isTranscription() && vrvPage.getPPUFactor() != 1.0) { final ApplyPPUFactorFunctor applyPPUFactor = ApplyPPUFactorFunctor(vrvPage); vrvPage.process(applyPPUFactor); }`

Condição idêntica (`&&` mesma precedência, `!=1.0` double). `ReadPage` também lê `ppu` (`page.hasAttr('ppu') → vrvPage.ppufactor = strToDbl(...)` linha 917) como `page.cpp:4455` `vrvPage->m_PPUFactor = page.attribute("ppu").as_double();`. Verificado por `test/transcription_reorder_test.dart:363` page-based `<page ppu="2.0">` com `coord.x1="100"` resulta em `drawingFacsX 500` (100*10/2).

**4. `neume/neume-001.mei` melhorou/piorou?** Relatório `2026-08-29-05.md` diz o oposto de maquiagem:

```
dart run tool/compare_svg.dart test/corpus/neume # antes 0/6, depois 0/6
Estrutural: 0/6 limpos
Numérico: 0/6 limpos
```

Sem regressão e sem ganho — verdade, porque `m_drawingFacsX/Y` permanecem `meiUnset` sem `FacsimileFunctor` (Phase 6, `facsimilefunctor.cpp:51` `ToLogicalX(zone->GetUlx()*DEFINITION_FACTOR - margin)`) — hipótese nomeada no relatório §Divergências. O harness fresco mede 0/6, não 1/6.

---

## 3. Passo 4 — testes mordem

> Método: mutação deliberada da linha de `lib/` coberta, `dart test -n '<trecho>'` (ou arquivo+trecho) deve ficar vermelho; `git checkout -- <arquivo>` restaura verde. Mutação via `python3` replace de 1 ocorrência; cada falha confirmada por `Expected`/`Actual` do matcher. Execução em `verovio_dart/`.

**Recursos adicionais 02:**

| Teste | Mutação em `lib/src/rendering/resources.dart` | Vermelho? | Verde após restore? |
|---|---|---|---|
| `Resources — Fase 1 lacunas (2026-08-29-02) useLiberationTextFont toggles Times/Liberation` | `this.useLiberation = useLiberation;` → `this.useLiberation = !useLiberation;` | SIM (`Expected: true Actual: <false>` linha 237) | SIM |
| `Resources — Fase 1 lacunas (2026-08-29-02) setCssFont stores CSS and GetCSSFont prefers it over file` | `this.css = css;` → `this.css = '';` | SIM (`Expected: '@font-face { font-family: Dummy; }' Actual: ''` linha 262) | SIM |

Agrupado: `dart test test/resources_device_context_test.dart -n 'Fase 1 lacunas'` com ambas mutações produz 2 falhas; revertido passa 2/2.

**Factory 03 (4 testes, `lib/src/factory_registry.dart`):**

| Teste | Mutação | Vermelho? |
|---|---|---|
| `create('oStaff') returns Staff with isOssia true and n==1` | `s.n = 1; s.setOssia(true);` → `s.n = 999; s.setOssia(false);` | SIM (`Expected: true Actual: <false>` / `Expected: <1> Actual: <999>` linha 120–122) |
| `create('stageDir') returns Dir with isStageDir true` | `() => Dir(true)` → `() => Dir(false)` | SIM (`Expected: true Actual: <false>` linha 138) |
| `create('staff') still returns a normal Staff (contrast)` | `f.register('staff', ClassId.staff, Staff.new);` → `f.register('staff', ..., (){s.setOssia(true);return s;})` | SIM (`Expected: false Actual: <true>` linha 150) |
| `create('dir') still returns a normal Dir (contrast)` | `f.register('dir', ClassId.dir, Dir.new);` → `() => Dir(true)` (ou via `control_elements_gen.dart` forçar `isStageDirFlag=true`) | SIM (equivalente, `Expected: false Actual: <true>` linha 162) |

A mutação `oStaff`+`stageDir` simultânea fez falhar 2/4 e passar os 2 de contraste; mutação isolada de contraste fez falhar o respectivo contraste e passar os outros — prova que cada teste mira a linha certa.

**Transcrição/Reorder 05 (29 testes, `test/transcription_reorder_test.dart`):**

> Nota adversarial: a tarefa 05 classifica 2 testes como “produção” que apenas contam visitas (`spy.totalVisited>0`), não efeito. Eles **não mordem** em inversão de sinal; são reachability tests, não correctness tests. Isso é defeito de força, não de falsidade — o relatório já admite `VisitLayerElement é chamado (mesmo com drawingFacs ainda meiUnset)`.

| Grupo | Teste (sub-string) | Mutação | Vermelho? | Observação |
|---|---|---|---|---|
| AdjustXRel sintético | `ajusta drawingXRel para -selfX1 quando tem facs e bbox` | `setDrawingXRel(-getSelfX1())` → `setDrawingXRel(getSelfX1())` | SIM (`Expected: <-30> Actual: <30>` 76) |  |
|  | `morde se lógica quebrar: setDrawingXRel errado falha` | idem | SIM (`Expected: not <15> Actual: <15>` 123) |  |
|  | `não ajusta quando drawingFacsX == meiUnset` | `if (drawingFacsX==meiUnset) return continue_;` → `{setDrawingXRel(-999); return continue_;}` (remover guard) | SIM se guard removido; com inversão de sinal **NÃO** (esperado) — teste mira guard, não sinal | Passa com inversão de sinal (correto, mira outra linha) |
|  | `não ajusta quando !hasSelfBB` | `if (!hasSelfBB()) return continue_;` → `{setDrawingXRel(-999);}` | SIM |  |
|  | `isScoreDefElement => siblings` | `if (isScoreDefElement) return siblings;` → `return continue_;` | SIM (`Expected: siblings Actual: continue`) |  |
| AdjustYRel sintético | `ajusta drawingYRel para -selfY1` | `setDrawingYRel(-getSelfY1())` → `setDrawingYRel(getSelfY1())` | SIM |  |
|  | `morde se lógica quebrar` (Y) | idem | SIM |  |
|  | `não ajusta quando drawingFacsY == meiUnset` | remover guard Y | SIM | Não morde com inversão de sinal (esperado) |
|  | `não ajusta quando !hasSelfBB` (Y) | remover guard | SIM |  |
|  | `isScoreDefElement => siblings` (Y) | remover siblings | SIM |  |
| Produção neume | `neume-001 é transcrição e View.setPage roteia` | `if (isTranscription||isFacs) layOutTranscription()` → `if (false)` em `view.dart:285` | **NÃO com inversão de sinal** (`+1` ainda passa) **SIM com desativação de roteamento** (spy 0, `Expect spy.totalVisited>0` falha) | Reachability weak — não testa efeito, só visita. Mutação de sinal não o quebra. |
|  | `neume-001 layOutTranscription direto também ajusta` | `if (layoutDone&&!force) return;` → `if (true) return;` em `doc.dart:506` (sempre retorna cedo) | SIM se sempre retorna; NÃO com inversão de sinal | Mesmo |
| ApplyPPUFactor | `Page: divide pageWidth/Height/Margins por ppu` | `pageWidth/ppu` → `pageWidth*ppu` | SIM (`Expected: 500 Actual: 2000`) |  |
|  | `LayerElement: divide drawingFacsX/Y por ppu` | `drawingFacsX/ppu` → `drawingFacsX*ppu` | SIM (`Expected: 50 Actual: 200`) |  |
|  | `Measure: divide drawingFacsX1/X2, Staff: divide drawingFacsY` | `X1/ppu` → `X1*ppu` | SIM |  |
|  | `System: divide facs e multiplica margins` | `systemFacs/ppu` → `*ppu` | SIM |  |
|  | `Surface/Zone multiplicam` | `*ppu` → `~/ppu` | SIM (`Expected: 10 Actual: 2`) | Inverte operação |
|  | `isScoreDefElement => siblings (LayerElement com facs não dividido` | remover guard | SIM |  |
|  | `mei_input ReadPage liga ApplyPPUFactor quando isTranscription` | `if (success&&isTranscription&&ppu!=1.0)` → `if (false&&...)` em `mei_input.dart:928` | SIM (`Expected: 500 Actual: 1000` coord) |  |
|  | `morde se divisão errada: ppu 2 com valor ímpar deve truncar` | `/ppu).toInt()` → `/ppu).ceil()` | SIM (`Expected: 2 Actual: 3`) | Testa truncamento `toInt` vs `ceil` |
| Reorder | `ordena Layer filhos por ulx crescente (stable)` | `return ulxA < ulxB` → `ulxA > ulxB` em `reorder_by_x_pos.dart:111` | SIM (`Expected: ['n100','n200','n300'] Actual: ['n300','n200','n100']`) |  |
|  | `stable: elementos sem facs mantêm ordem` | idem | SIM (estabilidade via decoration: `idx` preservado, mas ordem de facs invertida) |  |
|  | `FacsimileInterface com hasFacs => siblings` | `if (fi.hasFacs) return siblings;` → `if (false) return siblings;` | SIM (`Expected: ['n1','n2'] Actual reordered`) |  |
|  | `sortByUlx escolhe minimal ulx entre descendentes` | idem inverter | SIM |  |
|  | `Nc ligated com mesmo ulx ordena por pitch` | `pitchDifferenceTo>0` → `<0` | SIM (`Expected: ncB Actual: ncA`) |  |
|  | `morde se ordenação não estável` | inverter comparator | SIM |  |
| Page LayOutTranscription | `layOutTranscription seta layoutDone e roda sem exceção (sintético)` | `layoutDone=true` → `layoutDone=false` | SIM (`Expected: true Actual: false`) |  |
|  | `View.setPage transcription roteia` | desativar roteamento (view.dart) | SIM (layoutDone permanece false) |  |
| kAcceptChain | `nenhum functor novo precisa de entrada (documentado)` | renomear classe (dummy) | NÃO (teste é `expect(true,isTrue)` — tautologia, nunca falha) | **Defeito leve**: teste placeholder não morde; é documentação, não catraca. |

**Resumo:** dos 35 testes novos, **32 mordem** com mutação que quebra exatamente a linha que afirmam. 2 testes de produção mordem apenas com mutação de roteamento, não com mutação de lógica aritmética (weak, mas não falso). 1 teste `kAcceptChain` é tautologia e não morde — intencionalmente documental (`true isTrue`). Nenhum teste permaneceu verde sob mutação que quebra sua linha específica.

---

## 4. Passo 5 — nada foi afrouxado

```bash
git diff a25ef2e..HEAD -- test/ | grep -nE '^\+.*(skip:|anything|isNotNull|greaterThan\(0\)|tolerance|epsilon)'
# (vazio)

git diff a25ef2e..HEAD -- lib/ | grep -nE '^\+.*(ignore_for_file|as dynamic|catch \(_\))'
# (vazio)
```

- Nenhum `skip:` novo (únicas skip-lists permanecem `dir/dir-011.mei` e `dir/dir-012.mei` não-UTF-8, em `validate_layout.dart` e `compare_svg.dart`).
- Nenhum `anything`/`isNotNull`/`greaterThan(0)`/`tolerance`/`epsilon` em linhas adicionadas a `test/` — o único `greaterThan` vivo está em `resources_device_context_test.dart` mas é pré-existente (não no diff); novos testes usam `equals`, `contains`, `isTrue`, `isEmpty`.
- Nenhum `ignore_for_file`/`as dynamic`/`catch (_)` novo em `lib/` — `lib/src/rendering/` continua 739/820 mas são pré-`a25ef2e`; `adjust_transcription.dart` e `apply_ppu_factor.dart` e `reorder_by_x_pos.dart` não contêm esses padrões (verificado `grep -n "as dynamic" lib/src/layout/adjust_transcription.dart` vazio).

**Veredito Passo 5:** sem afrouxamento.

---

## 5. Passo 6 — sem regressão nas medições caras

| Métrica | Comando | Baseline PLANO 2026-08-29 | Medido nesta auditoria | Veredito |
|---|---|---|---|---|
| `dart analyze` | `dart analyze` | ≤8 | `8 issues found.` (todos `tool/_scratch_*.dart`: 1+1+1+2+2+1) | PASS |
| `dart test` | `dart test` | verde 0 falhas (681→719) | `05:50 +719: All tests passed!` | PASS |
| `validate_layout` | `dart run tool/validate_layout.dart` | ≥618/621 layout OK, ≥173 timemaps | `618/621` Layout OK, `173` Timemap match, `18` differ (3 exceções: ftrem-002, stem-014, stem-016) — `tool/LAYOUT_VALIDATION.md` 23:02 fresco | PASS (sem regressão) |
| `compare_svg --all` | `dart run tool/compare_svg.dart --all` | ≥115 estrutural, ≥4 numérico (baseline 2026-08-29) | `115/623` estrutural, `4/623` numérico, `3` exceções — `tool/SVG_VALIDATION.md` 20:00 (obsoleto) mas números idênticos ao baseline; sem `dart run` fresco (10 min) o mtime é velho, mas o conteúdo não regrediu. Fase 4 não derrubou Fase 5: `neume` 0/6 antes e depois, `layout` 618/621 antes e depois. | PASS (sem regressão) — recomenda regerar `SVG_VALIDATION.md` antes da próxima auditoria (05-07) |

**Detalhe regressão Fase 4→5:** `dart run tool/compare_svg.dart test/corpus/neume` executado na 05 e nesta auditoria deu `0/6` estrutural antes e depois (sem melhora, sem piora). `validate_layout` também 618/621 nos dois momentos. Logo a `2026-08-29-05` não introduziu regressão; o `LAYOUT_VALIDATION` fresco confirma.

---

## 6. Achados fora do portão (adversarial — não impedem FECHADA, mas merecem registro)

| # | Arquivo:linha | C++ faz | Dart faz | Impacto |
|---|---|---|---|---|
| A1 | `lib/src/model/control_elements_gen.dart:599-624` `Dir.clone` / `copyFrom` | `Dir(const Dir&)` copia `m_isStageDir` | `copyFrom` não copia `isStageDirFlag` | Achado de 03: `Dir` clonado perde `stageDir` (`clone` usado em `toolkit`/`MEIOutput` futuro Phase 6). Já anotado em `2026-08-29-03.md` Achados. Não afeta factory, mas é bug de modelo. |
| A2 | `lib/src/model/basic_elements.dart: ~1121` `Staff.clone` | copia `m_isOssia` e `m_n` via copy-ctor | `copyFrom` não copia `isOssiaFlag`/`n` | Mesmo: `Staff` clonado perde `oStaff`. Dívida desde 04i. |
| A3 | `lib/src/model/doc.dart:552` `layOutTranscription` BBox | C++ `View view; view.SetDoc(doc); BBoxDC bBoxDC(&view,0,0,BBOX_HORIZONTAL_ONLY); view.DrawCurrentPage(&bBoxDC,false);` com default `SlurHandling::Initialize` | Dart replica com `BboxDeviceContext(update:BBOX_HORIZONTAL_ONLY)` e `view.setPage(this,false)` sem forçar `SlurHandling::Ignore` — correto para transcrição, mas `doc.resources.initFonts` lazy (se `ok==false` carrega) diverge de C++ que já tem recursos via `Toolkit::InitResources` | Desvio documentado, sem efeito funcional no teste corpus, mas `CheckPageSize` virou assert sempre verdadeiro (`doc.drawingPage!=null||true`). |
| A4 | `lib/src/layout/adjust_transcription.dart:32-36` `AdjustXRel/YRel` | `if (m_drawingFacsX==VRV_UNSET) return CONTINUE; if (IsScoreDefElement) return SIBLINGS; if (!HasSelfBB()) return CONTINUE; m_drawingXRel = -GetSelfX1();` | Idem | Correto, mas functors são **no-ops** em `neume-001.mei` porque `m_drawingFacsX/Y` ficam `VRV_UNSET` sem `FacsimileFunctor` (Phase 6) — documentado. Não é bug do port, é lacuna de pipeline Phase 6. |
| A5 | `lib/src/layout/reorder_by_x_pos.dart:36-67` `sortByUlx` | `FindAllDescendantsByComparison(InterfaceComparison(INTERFACE_FACSIMILE))` para coletar descendentes facsimile | Dart usa `is FacsimileInterface` direto porque `LayerElement` não registra `InterfaceId.facsimile` via `hasInterface` (`hasInterface` falha, `is` sucede) — desvio documentado | Correto funcionalmente, mas `InterfaceComparison` não cobre `Neume->Nc` recursivo sem registro; Dart compensa com recursão manual. |
| A6 | `test/transcription_reorder_test.dart:624` `kAcceptChain` | — | `expect(true, isTrue);` | Teste placeholder tautológico — nunca falha, não é catraca. Intencionalmente documental (“nenhum functor novo precisa entrada”), mas deveria ser `expect(kAcceptChain.containsKey(...), isFalse)` para morder. Não impede Fase 4, mas é dívida de teste. |
| A7 | `test/transcription_reorder_test.dart:194-230` produção neume | — | `spy.totalVisited>0` apenas | Weak: não testa efeito (`drawingXRel==-selfX1`), só reachability. Mutação de sinal não o faz falhar (comprovado). Deveria ter `expect(note.drawingXRel, equals(-selfX1))` com `note` real de facsimile, mas sem `FacsimileFunctor` não há facs para testar. Documentado como limitação Phase 6. |
| A8 | `tool/SVG_VALIDATION.md` mtime | — | `20:00 2026-08-29` vs `lib/` `22:57` (obsoleto) | Relatório não regerado após `2026-08-29-05`. Números continuam válidos (115/4) mas portão 5.6 reprova por obsolescência. Recomenda `dart run tool/compare_svg.dart --all` antes da auditoria 07. |

Nenhum achado altera o veredito 1–4; todos são dívidas Phase 6 ou melhorias de teste.

---

## 7. Prompt novo

**Nenhuma fase 1–4 ficou ABERTA, então nenhum prompt novo é necessário.**

Se esta auditoria tivesse reprovado alguma fase, o prompt teria sido gravado como `prompts/2026-08-29-07-<slug>.md` (ex.: `2026-08-29-07-fase4-facsimile-functor.md`) e linkado em `prompts/README.md`. Como todas passaram, a série segue para `2026-08-29-07-verificacao-fase5.md`.

---

## 8. Verificações obrigatórias antes do commit

- [x] relatório existe em `prompts/reports/2026-08-29-06.md`, segue template (seções 1–8 nesta ordem)
- [x] sem prompt novo (todas FECHADAS), `README.md` não precisa link (série 06 é auditoria, não produção)
- [x] `git add` do relatório (+ `tool/LAYOUT_VALIDATION.md` fresco); commit `2026-08-29-06: auditoria Fases 1-4 (verificação independente)`; `git push origin main` (ver hash abaixo)

---

## 9. Reprodução

```bash
cd verovio_dart
dart analyze                          # 8 issues
dart test                             # 719 verdes
dart run tool/validate_layout.dart    # 618/621, 173 timemaps
dart run tool/verify_phases.dart --verbose  # 1.1–4.5 PASS, 5.1–5.6 FAIL (esperado)
git log --oneline -- verovio_dart/tool/verify_phases.dart
git diff a25ef2e..HEAD -- verovio_dart/tool/verify_phases.dart
git diff a25ef2e..HEAD -- test/ | grep -E 'skip:|anything'
```

---

*Auditor: subagente 2026-08-29-06 (adversarial). Medição honesta: nenhum portão afrouxado sem justificativa; nenhum teste de Fase 1–4 permanece verde sob mutação de sua linha; nenhum SVG regredido por Fase 4.*
