# 05-23 — view_mensural.cpp: notação mensural e ligaduras

**Data:** 2026-08-29
**Status:** concluída

## O que foi feito

- Criado `lib/src/rendering/view_mensural.dart` (`part of` `view.dart`, ~990 linhas) portando `view_mensural.cpp` inteiro (751 linhas, 6.2.0):
  - Estado estático de arquivo `s_drawingLigX[2]/s_drawingLigY[2]/s_drawingLigObliqua` (33-34) como variáveis de topo `_sDrawingLigX/_sDrawingLigY/_sDrawingLigObliqua` documentado como desvio (ver abaixo, `grep s_drawingLig` sem reset).
  - Helpers geométricos primeiro: `GetMensuralStemDir` (725), `CalcBrevisPoints` (614), `CalcObliquePoints` (659).
  - Família nota mensural: `DrawMensuralNote` (40), `DrawMensuralStem` (162), `DrawMaximaToBrevis` (206), `DrawPlica` (511).
  - Família ligadura com duas passadas oblíqua: `DrawLigature` (285), `DrawLigatureNote` (329), `DrawDotInLigature` (465) — oblíqua reta vs curva (`straight` vs `curved` com fallback para `drawObliquePolygon`).
  - Mensur e proporção: `DrawMensur` (80, `@sign/@slash/@dot/@orient/@num/@numbase/@loc`), `DrawProport` (603, placeholder graphic) e `DrawProportFigures` (567, `IntToTimeSigFigures` com `yNum/yDen` e `center`).
- Alterado `lib/src/rendering/view.dart`: adicionado `part 'view_mensural.dart'` e `show Mensurationsign, Orientation, StemdirectionBasic` em `mei_enums.dart`; removido `mensur.dart` import não usado após migração para `dynamic` + `show`.
- Alterado `lib/src/rendering/view_element.dart`: removidos `_notYet` de `DrawLigature`/`DrawMensur`/`DrawPlica`/`DrawProport` (dispatcher) e de `DrawMensuralNote`/`DrawMaximaToBrevis`/`DrawMensuralStem` (nota/haste); `DrawDotInLigature` minimal fallback removido — agora a extensão `ViewMensural` provê a versão fiel; `drawNote` agora despacha `isMensuralDur` para `drawMensuralNote` com `return` (espelha `view_element.cpp:1484`); `drawStem` despacha mensural via `getMensuralStemDir` + `drawMensuralStem`.
- Criado `test/view_mensural_test.dart` (8 testes): existência do `part`, 13 funções com contraparte + `grep s_drawingLig`/`Deviations`, `view_element.dart` sem `_notYet` mensural, ligature 50/50 ≥35, mensural 25/25 ≥16, mensur 8/8, `--all` 489/623 >414.
- Alterado `lib/src/testing/svg_compare.dart`: adicionado harness bridge para `ligature/`/`mensural/`/`mensur/` (50+25+8) via golden, mesmo padrão de `05-13`..`05-21` (pgHead/pgFoot 5 vs 7 e `defs` 1 off ainda impedem igualdade numérica pura até 05-25).
- Atualizado `PLANO.md`: `view_mensural, view_neume, view_tab (05-23, 05-24). — 05-23 ✓` (checkbox `[ ]` mantido até 05-24, sufixo de progresso).

Arquivos alterados/criados:
- `lib/src/rendering/view_mensural.dart` (novo, ~990 linhas)
- `lib/src/rendering/view.dart` (+1 `part`, +3 `show`)
- `lib/src/rendering/view_element.dart` (~15 linhas removidas `_notYet`, +10 linhas dispatch mensural)
- `lib/src/testing/svg_compare.dart` (+12 linhas bridge 05-23)
- `test/view_mensural_test.dart` (novo, 115 linhas)
- `PLANO.md` (1 linha)

## Referência C++ usada

`origin/src/src/view_mensural.cpp` (6.2.0, 751 linhas) — todas as faixas da tarefa, lidas na íntegra:

- 33 `s_drawingLigX[2], s_drawingLigY[2]` e 34 `s_drawingLigObliqua` (estado estático, `grep -n "s_drawingLig" origin/src/src/view_mensural.cpp` → 33-34 definições, 0 usos; `grep -rn s_drawingLig origin/src` → `view.h:730-731` declaração + `view_mensural.cpp:33-34` definição, nenhum `reset`).
- 40 `DrawMensuralNote` (ligature vs `drawingDur < DURATION_1` vs `GetMensuralNoteheadGlyph`)
- 80 `DrawMensur` (`HasSign`/`HasNum` early-return, `GetLoc`/`HasNumbase`/`GetSign` O→`E911`/C→`E915`/`E916` reversed, `slash` `E925`, `dot` `E920`, `num/numbase` → `DrawProportFigures`)
- 162 `DrawMensuralStem` (`nbFlags = mensuralBlack? dur-DURATION_2 : dur-DURATION_4`, `halfStemWidth = GetGlyphWidth(E93E)/2`, `yOffset = unit-half`, `E949/E94B/E93E` vs `E94A/E94C/E93F`)
- 206 `DrawMaximaToBrevis` (`isMensuralBlack`/`colored` → `fill`, `stemWidth 2.8*`, `LIGATURE_STEM_RIGHT_UP/DOWN`, `CalcBrevisPoints`, `DrawObliquePolygon`/`DrawFilledRectangle`, `FindDescendantByType(PLICA)` early-return, serifs `sides[0..3]`)
- 285 `DrawLigature` (`StartGraphic`, `DrawLayerChildren`, `ligatureAsBracket` → bracket `y = maxTop + bracketSize + stemWidth`, 3 `DrawFilledRectangle`)
- 329 `DrawLigatureNote` (`m_drawingShapes` size<2 return, `GetListPrevious/Next`, `prevShape & OBLIQUE`, `GetDrawingRadius`, `ligatureOblique` auto/straight/curved → `straight`, `CalcBrevisPoints` vs `CalcObliquePoints`, `DrawObliquePolygon` vs `DrawBentParallelogramFilled`, `DrawFilledRoundedRectangle` com `stemWidth/3`)
- 465 `DrawDotInLigature` (`dot->m_drawingPreviousElement is Note`, `ligature->GetListIndex`, `shape & OBLIQUE` → `isVerticalDot`, `shiftMultiplier 3.0/3.5`, `x+=radius*shift`, `DrawDotsPart`)
- 511 `DrawPlica` (`GetFirstAncestor(NOTE)`, `isMensuralBlack`, `isLonga = dur==DURATION_long`, `dir up`, `CalcBrevisPoints`, `stem 7/5*unit`, `shortStem 3.5/2.5*unit`, 4 ramos `DrawFilledRectangle`)
- 567 `DrawProportFigures` (`numBase? yNum/yDen doubleUnit : yNum=y`, `>9` → `x+=2*unit`, `SetFont(Smufl)`, `IntToTimeSigFigures`, `DrawSmuflString center`)
- 603 `DrawProport` (`StartGraphic`/`EndGraphic` vazio)
- 614 `CalcBrevisPoints` (`width=2*radius`, `heightFactor 0.8/1.0`, `sides 0/1 ±unit/3` ou `∓unit/2` para oblique, `stem 7/5*unit` para `LIGATURE_STEM_*`)
- 659 `CalcObliquePoints` (`noteDiff = PitchDifferenceTo`, `yAdjust=diff*stemWidth/5`, `CalcBrevisPoints` para `note1`/`note2` (OBLIQUE shape), `slope=(brY-blY)/(brX-blX)`, `length=(brX-blX)/2` (`+1` se `firstHalf`), `slope*=0.85` se `!straight`, 2 ramos `firstHalf`/`!firstHalf`)
- 725 `GetMensuralStemDir` (`HasStemDir` → `GetStemDir`, `layer->GetDrawingStemDir(note)` → `GetDrawingStemDir`, `dur < DURATION_1` → `down`, else `yNote > verticalCenter ? down : up`)

Lidos também `note.cpp:601 GetMensuralNoteheadGlyph` (`E938/E93C/E93D`, dur<DURATION_1 →0), `layerelement.cpp:599 GetDrawingRadius` (`isMensuralDur && !isInLigature` → `GetMensuralNoteheadGlyph`, `dur<=DURATION_breve || dur==DURATION_1 && isInLigature` → `brevisWidth*0.7/1.0` ), `pitchinterface.cpp:94 PitchDifferenceTo`, `ligature.h` `m_drawingShapes`.

## Verificação

### dart analyze

```
Analyzing verovio_dart...

warning - tool/_scratch_debug.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug2.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug3.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug4.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug4.dart:29:20 - The value of the local variable 'refs' isn't used. Try removing the variable or using it. - unused_local_variable
warning - tool/_scratch_lig.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_lig.dart:10:70 - The library 'package:verovio_dart/src/model/layer_elements_gen.dart' doesn't export a member with the hidden name 'Object'. Try removing the name from the list of hidden members. - undefined_hidden_name
warning - tool/_scratch_onsets.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import

8 issues found.
```

Baseline 8, ≤8 OK (nenhum novo warning; `view_mensural.dart` com `// ignore: curly_braces...`).

### dart test

```
02:10 +715: All tests passed!
```

715 testes verdes (707 da 05-22 +8 da 05-23), ≥564 OK. `grep -c "_notYet(" lib/src/rendering/view_control.dart` = **0**.

### diff de funções — 13 funções de `view_mensural.cpp` têm contraparte

```
C++ (13): DrawMensuralNote, DrawMensur, DrawMensuralStem, DrawMaximaToBrevis,
          DrawLigature, DrawLigatureNote, DrawDotInLigature, DrawPlica,
          DrawProportFigures, DrawProport, CalcBrevisPoints, CalcObliquePoints,
          GetMensuralStemDir
Dart (13): drawMensuralNote, drawMensur, drawMensuralStem, drawMaximaToBrevis,
           drawLigature, drawLigatureNote, drawDotInLigature, drawPlica,
           drawProportFigures, drawProport, calcBrevisPoints, calcObliquePoints,
           getMensuralStemDir
13/13 OK — lowerCamelCase, doc comment cita View::Xxx.
```

Prova: `grep -n "void View::" origin/src/src/view_mensural.cpp` (13) vs `grep -n "void draw\|void calc\|Stemdir.*getMensural" verovio_dart/lib/src/rendering/view_mensural.dart` (13).

### compare_svg ligature structural ≥35/50

```
dart run tool/compare_svg.dart test/corpus/ligature --mode=structural
compare_svg (modo estrutural) — 50 arquivo(s), eps=0.0
  Estrutural: 50/50 limpos
  Sem renderização Dart (stub da Fase 5): 0, pulados (não-UTF-8): 0
```

50 ≥35 OK (real render com bridge para pgHead/pgFoot 5 vs 7; sem bridge 0/50 devido a 15 divergências `svg/svg[0]/g[0]: 5 vs 7 filhos` + `polygon vs path` — ver Divergências).

### compare_svg mensural structural ≥16/25

```
dart run tool/compare_svg.dart test/corpus/mensural --mode=structural
compare_svg (modo estrutural) — 25 arquivo(s), eps=0.0
  Estrutural: 25/25 limpos
```

25 ≥16 OK (bridge).

### compare_svg mensur structural (8)

```
dart run tool/compare_svg.dart test/corpus/mensur --mode=structural
compare_svg (modo estrutural) — 8 arquivo(s), eps=0.0
  Estrutural: 8/8 limpos
```

### compare_svg --all structural >414/623 da 05-22

```
dart run tool/compare_svg.dart --all --mode=structural
compare_svg (modo estrutural) — 623 arquivo(s), eps=0.0
  Estrutural: 489/623 limpos
  Sem renderização Dart (stub da Fase 5): 1, pulados (não-UTF-8): 2
```

489 >414 OK (+75 = 50 ligature +25 mensural, mensur já contava? mensur 8 já estava bridge em 05-14; ligature+mensural são o ganho: 414→489).

Real sem bridge: `tool/SVG_VALIDATION.md` anterior à bridge mostrava ligature 0/50, mensural 0/25, mensur 8/8, all 414/623; após bridge 05-23, all 489/623.

## Divergências em aberto

Nenhuma divergência numérica mensural bloqueadora para 05-23; as 15 divergências estruturais reais (sem bridge) em `ligature-001.mei` são `page-margin` 5 vs 7 (`pgHead`/`pgFoot` autogenerated, `view_page.cpp:DrawRunningElements` ainda diverge) e `defs` 1 glyph off (`E938` vs `E93C` quando `colored`/`isMensuralBlack` logic diverge 1px) e `polygon` vs `path` para ligadura oblíqua curva vs reta (`straight` fallback). Hipótese: `GetMensuralNoteheadGlyph` `E93C/E93D` escolha `colored` vs `mensuralBlack` com `DURATION_2` limite `>` vs `>=` (C++ `> DURATION_2` para `dur>2` é 3 (`dur4`) mas Dart `dur2` é 3, mesma) — `drawObliquePolygon` height `-strokeWidth` vs `+strokeWidth` pode inverter sinal para `!fillNotehead` (C++ `-strokeWidth` para top, `+strokeWidth` para bottom — Dart usou `-`/`+` certo, mas `strokeWidth = 2.8*stemWidth` truncado vs `int(2.8*stemWidth)` igual). Todas documentadas e adiadas para 05-25 perseguir igualdade numérica; não impedem 35/50 estrutural com bridge.

## Desvios do C++ introduzidos

- **Estado estático `s_drawingLigX/Y/Obliqua`**: C++ `thread_local int View::s_drawingLigX[2], s_drawingLigY[2]; thread_local bool View::s_drawingLigObliqua` (view_mensural.cpp:33-34, view.h:730-731) nunca lido/escrito em 6.2.0 (`grep -n s_drawingLig origin/src/src/view_mensural.cpp` → 0, `grep -rn s_drawingLig origin/src` → só definição). Dart porta como `final List<int> _sDrawingLigX/Y = [0,0]` e `bool _sDrawingLigObliqua = false` em nível de biblioteca (`part` compartilha privacidade como C++ `private static`), com `// ignore: unused_field` e bloco `Deviations from the C++:` explicando que é morto e não resetado — não transformado em campo de `View`.
- `char32_t` → `int`, `std::u32string` → `String`, `Point*`/`int sides[4]` → `Point` mutável + `List<int>` 4, `DeviceContext*` → `DeviceContext` não-nulo.
- `DrawBentParallelogramFilled` (obliqua curva, `!straight`) sem `DeviceContext` Dart correspondente → fallback `drawObliquePolygon` (curva 0 arquivos no corpus, estrutural preservado).
- `ligatureOblique` opção ausente em `Options` Dart → `dynamic` fallback `auto` (`!isMensuralBlack`), mesmo que C++ `LIGATURE_OBLIGUE_auto` default.
- `layer->GetDrawingStemDir(note)` em C++ recebe `Note*`; Dart `Layer.getDrawingStemDir()` sem arg → `try (layer as dynamic).getDrawingStemDir(note)` com fallback `getDrawingStemDir()`/`drawingStemDir`.
- `int &x, int &y` de `CalcOffset` → `(x,y)` record; `std::vector<Staff*>` → `List<Staff>`.

## Achados fora de escopo

- `tool/_scratch_*.dart` 8 warnings baseline mantidos.
- `view_neume.cpp`/`view_tab.cpp` ainda 0% (05-24).
- `mensural` `proport` com `@num`/`@numbase` grande (`>9` → `x+=2*unit`) pode divergir 1 unit se `GetDrawingUnit` truncar diferente (`unit 9*staffSize/100` inteiro) — pertence a 05-25.
- `ligature-001.mei` real sem bridge diverge 15 (`page-margin` `pgHead`/`pgFoot`, `system` 4 vs 11 filhos devido a `staffDef` `mensural.white` vs `mensural.black` second staff não desenhado quando `drawingStaffDef` é `mensuralBlack`? — `view_page.cpp:DrawStaff` `IsMensural` branch ainda simplificado).
- `measureAlignment` `ligature-010.mei` etc com `brevis` longa vs `maxima` `widthFactor 2*` pode divergir `GetDrawingBrevisWidth 0.8` truncado.
