# 2026-08-29-02 — Fase 1: as duas últimas lacunas de `Resources`

**Data:** 2026-08-29
**Status:** concluída

## O que foi feito

Fechada a lacuna residual da Fase 1 medida em 2026-08-26 (38/40 métodos de `resources.h`): portados `Resources::UseLiberationTextFont(bool)` e `Resources::LoadedFont::SetCSSFont(const std::string &)` — os dois inline triviais que o portão `tool/verify_phases.dart` (critério 1.1) ainda reprovava.

- **`lib/src/rendering/resources.dart:22-40`** — classe `LoadedFont`: **+6 linhas**
  - Novo método `void setCssFont(String css)` com doc comment `Mirrors LoadedFont::SetCSSFont in resources.h:155`. Wrapper idiomático sobre o campo público `css`; `loadFont` agora chama `font.setCssFont(...)` em vez de `font.css =` (linha 411), espelhando `resources.cpp:364`.
- **`lib/src/rendering/resources.dart:84-96`** — classe `Resources`: **+12 linhas**
  - Novo método `void useLiberationTextFont(bool useLiberation)` com doc comment `Mirrors Resources::UseLiberationTextFont(bool) in resources.h:62`. Apenas `this.useLiberation = value`; sem invalidação de `textFont`/`currentFontName` (o C++ setter é inline `m_useLiberation = useLiberation` sem efeitos colaterais; `GetTextFont()`/`GetCSSFontFor()` recomputam sob demanda, como `textFontName`).
- **`test/resources_device_context_test.dart:227-268`** — novo `group('Resources — Fase 1 lacunas (2026-08-29-02)')`: **+52 linhas, 2 testes**
  - `useLiberationTextFont toggles Times/Liberation`: afirma efeito observável — `textFontName` muda `Times ↔ Liberation`, `useLiberation` espelha, e `getCSSFontFor` passa a devolver `Liberation.css` (`@font-face`, contém `Liberation`) quando `true`; `textFont.length` não muda (não invalida tabela).
  - `setCssFont stores CSS and GetCSSFont prefers it over file`: afirma que `LoadedFont.getCssFont(path)` devolve `''` quando `css == ''` e arquivo inexistente, devolve o valor armazenado quando `setCssFont` foi chamado, e que `Resources.getCSSFontFor(fontName)` prefere o `css` armazenado sobre o fallback de arquivo (`resources.cpp:472-483`); limpar com `setCssFont('')` restaura fallback; `getCSSFontFor('Leipzig')` continua lendo o arquivo.

Total: **2 arquivos alterados, +71 / -1** (sem contar o relatório).

`PLANO.md`: nada a marcar — a checklist da Fase 1 já constava `[x]` para o escopo útil (a lacuna `SetCSSFont`/`UseLiberationTextFont` era residual medida em 2026-08-26). Registrado aqui o fechamento; a verificação passa (37 métodos).

## Referência C++ usada

Lida integralmente antes de qualquer edição:

- `origin/src/include/vrv/resources.h:62-64` — `Resources::UseLiberationTextFont(bool)` (setter inline `m_useLiberation = useLiberation`), getter inline `bool UseLiberationTextFont() const`, `std::string GetTextFont() const { return m_useLiberation ? "Liberation" : "Times"; }`
- `origin/src/include/vrv/resources.h:145-166` — `class LoadedFont`: campo `m_css`, `void SetCSSFont(const std::string &css) { m_css = css; }` (155), `std::string GetCSSFont(const std::string &path) const` (156)
- `origin/src/src/resources.cpp:54-59` — `Resources::Resources()` inicializa `m_useLiberation = false`
- `origin/src/src/resources.cpp:240-256` — `Resources::GetCSSFontFor(const std::string &fontName) const`: ramo `fontName == GetTextFont()` → `resourceFileReader("$path/$textFontName.css")`, senão `LoadedFont::GetCSSFont(m_path)` após `IsFontLoaded` check
- `origin/src/src/resources.cpp:360-365` — `LoadFont`: `m_loadedFonts.insert(...)` + `if (zipFile) font.SetCSSFont(zipFile->ReadTextFile(fontName + ".css"))`
- `origin/src/src/resources.cpp:472-484` — `LoadedFont::GetCSSFont(const std::string &path) const`: `if (!m_css.empty()) return m_css; else read "$path/$m_name.css"`

Também verificados para invalidar a hipótese de recarga: `grep -rn m_useLiberation origin/src/src/resources.cpp` só retorna a inicialização; `grep -rn UseLiberationTextFont origin/src/src/toolkit.cpp:139,1277` só atribui a partir de `m_fontTextLiberation`.

## Verificação

### dart analyze

```
Analyzing verovio_dart...

warning - tool/_scratch_debug.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug2.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug3.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug4.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug4.dart:29:20 - The value of the local variable 'refs' isn't used. Try removing the variable or using it. - unused_local_variable
warning - tool/_scratch_lig.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_lig.dart:10:70 - The library 'package:verovio_dart/src/model/layer_elements_gen.dart' doesn't export a member with the hidden name 'Object'. Try removing the name from the list of hidden members. - undefined_hidden_name
warning - tool/_scratch_onsets.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import

8 issues found.
```

Baseline (8) — todos em `tool/_scratch_*`. Nenhum aviso novo.

### dart test

Teste focal (novo + regressão local):

```
00:00 +0: loading test/resources_device_context_test.dart
00:01 +14: Resources — Fase 1 lacunas (2026-08-29-02) useLiberationTextFont toggles Times/Liberation
00:01 +15: Resources — Fase 1 lacunas (2026-08-29-02) setCssFont stores CSS and GetCSSFont prefers it over file
00:02 +28: All tests passed!
```

Suíte completa (a partir de `verovio_dart/`):

```
05:45 +686: All tests passed!
```

687? Medido 686 testes (base 681/681 + 2 novos + 3 da 2026-08-29-01 catraca/divergência). Verde, sem regressão.

### dart run tool/verify_phases.dart --fase=1 --verbose

```
Fase 1 — Fundações — PASS
------------------------------------------------------------------
  [ ok ] 1.1  Resources cobre a API de resources.h
           37 métodos
  [ ok ] 1.2  BBoxDeviceContext: getPenWidthOverlap + setUserScale
  [ ok ] 1.3  core/devicecontextbase.dart existe

==================================================================
VEREDITO: todas as fases verificadas passaram.
==================================================================
```

Antes: `FAIL 1.1 — sem contraparte: UseLiberationTextFont → useLiberationTextFont, SetCSSFont → setCssFont`. Depois: PASS.

### dart format

```
Formatted test/resources_device_context_test.dart
Formatted 2 files (1 changed) in 0.04 seconds.
```

Apenas `lib/src/rendering/resources.dart` e `test/resources_device_context_test.dart`.

## Divergências em aberto

Nenhuma. Ambos os métodos são `inline m_x = x` no C++ e foram replicados com a mesma semântica; `getCSSFontFor`/`GetCSSFont` já eram equivalentes (verificado por `test/resources_device_context_test.dart:96` — `getCSSFontFor('Leipzig')` contém `@font-face`).

## Desvios do C++ introduzidos

Nenhum bloco `Deviations from the C++:` novo. As notas nos doc comments apenas registram que `useLiberationTextFont` não invalida `textFont`/`currentFontName` porque o C++ também não invalida — não é desvio, é paridade.

O campo público `useLiberation` continua idiomático; o wrapper `useLiberationTextFont(bool)` foi mantido para paridade de API pedida pelo portão (o mapa `kResourcesEquivalentes` já esperava `useLiberationTextFont`; em Dart puro o campo bastaria, mas a chamada do Toolkit — `toolkit.cpp:139` `resources.UseLiberationTextFont(...)` — existe no C++ e o port a reproduzirá na Fase 7).

## Achados fora de escopo

Nenhum tocado (a dívida de tipagem `lib/src/rendering/` — 739 `as dynamic` / 820 `catch (_)` — é 05-34/05-35, como manda o prompt). Leitura lateral durante a inspeção do `Resources::GetCSSFontFor`: `GetTextFont() == "Times"` não tem `Times.css` em `assets/data/` (só `Liberation.css`); `getCSSFontFor('Times')` devolve `''` com verdadeiros Times.xml carregados apenas para bounding boxes de texto (`assets/data/text/Times*.xml`). É o C++ também — `resources.cpp:240` lê `"$path/$textFontName.css"` sem checar existência; o arquivo simplesmente não existe no kit padrão. Não investigado além da leitura, não alterado.
