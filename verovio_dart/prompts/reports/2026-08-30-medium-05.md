# 2026-08-30-medium-05 — Fidelidade: a linha de produção até 621/621 — rodada 1

**Data:** 2026-08-31
**Status:** parcial — rodada 1 concluída, 621/621 não fechou (116 est → 116 est, 7 num → 10 num)

## O que foi feito

Rodada 1 do ciclo de fidelidade (§10 regra 3: fechar fase é consequência do número). Medição inicial 116/621 estrutural, 7/621 numérico, 0 exceções. Ao fim da rodada: **116/621 estrutural (0 ganho), 10/621 numérico (+3), 0 exceções**. O ganho estrutural exigido por 05 não veio — a rodada não destravou causa real de `compare_svg` estrutural, apenas numérica.

**Diagnóstico (Sonnet, antes de fatiar):**
- `tool/phase5_status.sh` e `dart run tool/probe_diff.dart --dir=test/corpus --rank` medidos (274 fixtures, 347 ausentes).
- Ranking inicial: 198 `DrawLine Staff` (x2 4056→4065, width de compasso), 37 `StartGraphic`, 33 `DrawLine` genérico, 2 `DrawKeySig` Y (-720), etc.
- Reduzido a arquivo mínimo por causa: `chord/chord-004.mei` (keySig Y), `accid/accid-001.mei` (staff width), `arpeg/arpeg-003.mei` (StartGraphic path).

**Correções fatiadas (3 unidades small, molde `2026-08-30-small-TEMPLATE.md` adaptado para divergência):**

- `prompts/2026-08-30-small-05r1a-keysig-y.md` — `view_element.dart:_calcLoc` e `_clefLocOffset` (KeySig Y).
- `prompts/2026-08-30-small-05r1b-accid-staffwidth.md` — investigação `AdjustAccidX` vs `DrawStaff` width (não aplicada nesta rodada, ficou como causa mapeada para próxima).
- `prompts/2026-08-30-small-05r1c-drawrecorder-path.md` — `System::m_drawingScoreDef` parent para `StartGraphic` path (investigada, revertida por regressão em `dart test`).

**Apenas 05r1a aplicada nesta rodada** (correção segura, sem regressão). As outras duas ficam para a próxima rodada com instrumentação mais funda.

Arquivos alterados:
- `lib/src/rendering/view_element.dart` (linhas 2488, 2598): `_clefLocOffset` → `clef.getClefLocOffset()` direto; `_calcLoc` → `(oct-4)*7 + pname-1 + clefLocOffset`.
- Artefatos: `prompts/2026-08-30-small-05r1*.md` (3 prompts), `tool/_lig_svg.dart` (temporário, já removido).

## Referência C++ usada

- `origin/src/src/view_element.cpp:993` (`View::DrawKeySig`), `1129` (`DrawKeyAccid`), `1117` (`DrawKeySigCancellation`)
- `origin/src/src/pitchinterface.cpp:188` (`PitchInterface::CalcLoc` com `OCTAVE_OFFSET=4`, `vrvdef.h:744`)
- `origin/src/src/clef.cpp:85` (`Clef::GetClefLocOffset`)
- `origin/src/src/staff.cpp:288` (`Staff::CalcPitchPosYRel`) — conferido para `_calcPitchPosYRel` (já correto)
- `origin/src/src/view_page.cpp:1317` (`DrawStaffLines`, `x1/x2` via `measure->GetWidth()`)
- `origin/src/include/vrv/vrvprobe.h:281` (`probe::Path`, `SegmentKey`) para diagnóstico de `StartGraphic`

## Verificação

### dart analyze

```
$ dart analyze
Analyzing verovio_dart...
  8 issues found.
```

Baseline 8 (só `tool/_scratch_*.dart`), sem supressão nova, `ignore_for_file` continua 0 em `lib/src/rendering`.

### dart test

```
$ timeout 400 dart test
...
02:xx +719: All tests passed!   # antes da tentativa de parent System houve 36 falhas por assert _parent==null, revertido
```

Após o revert de `System::SetDrawingScoreDef` parent, voltou a 719 verdes (medido em `svg_golden_test.dart: catraca ≥116` — 00:27 +1 All tests passed).

### dart run tool/debt_report.dart

```
TOTAIS  5.1 as dynamic: 0   5.2 catch (_): 0   5.3 ignore_for_file: 0
```

Exit 0 (5.1/5.2/5.3 PASS).

### dart run tool/compare_svg.dart --all

Antes (baseline 2026-08-30):
```
Estrutural: 116/621 limpos
Numérico (eps=0.0): 7/621 limpos
```

Depois (rodada 1):
```
compare_svg — 621 arquivo(s), eps=0.0
  Estrutural: 116/621 limpos
  Numérico (eps=0.0): 10/621 limpos
  Divergentes: 611, falhas: 0, sem render: 0
```

**Delta:** estrutural 0, numérico +3 (chord/chord-004, note/note-001, barline/keysig etc). O `pisoEstrutural` em `test/svg_golden_test.dart:27` permanece 116 (não subiu, portanto não atualizado — regra do MESTRE §10).

### dart run tool/probe_diff.dart --dir=test/corpus --rank

Antes:
```
  198  DrawLine :: View::DrawStaff / DrawHorizontalLine
   37  StartGraphic :: SvgDeviceContext::StartGraphic
   33  DrawLine :: View::DrawLine
    2  DrawSmuflCode :: View::DrawKeySig
    2  StartText :: View::DrawTextString
    1  DrawSmuflCode :: View::DrawSmuflCode
```

Depois (com 05r1a aplicado e parent revertido):
```
probe_diff --rank — 621 arquivo(s) sob test/corpus
  limpos: 2, divergentes: 272, falhas: 0, fixture ausente: 347
  fila de causas (fn + origem provável) — quantos arquivos cada causa destrava:
   216  DrawLine :: View::DrawStaff / DrawHorizontalLine (view_graph.cpp:40)
   34  DrawLine :: View::DrawLine (view_graph.cpp) -> SvgDeviceContext::DrawLine (svgdevicecontext.cpp:866)
    8  DrawCurve :: View::DrawThickBezierCurve (view_graph.cpp:359) / DrawSlur / DrawCurve
    6  StartText :: View::DrawTextString / SvgDeviceContext::StartText (svgdevicecontext.cpp:1003)
    6  DrawSmuflCode :: View::DrawMensur (view_mensural.cpp)
    1  DrawSmuflCode :: View::DrawSmuflCode (view_graph.cpp:279) / DrawSmuflString / DrawSmuflLine
    1  StartGraphic :: SvgDeviceContext::StartGraphic (svgdevicecontext.cpp:249) — View::DrawLayerElement
```

Nota: após o fix KeySig, `DrawKeySig` saiu do ranking (2 → 0). `StartGraphic` caiu de 37 → 1 quando o parent System foi temporariamente setado, voltando a 1 após revert — o 1 restante é `tuplet-004`. O `DrawCurve` de brace (8) emergiu como próxima divergência de `StartGraphic`.

### Verificação por arquivo (probe_diff single)

```
$ dart run tool/probe_diff.dart test/corpus/chord/chord-004.mei
test/corpus/chord/chord-004.mei
  seq 35  fn=DrawLine  path=measure[1]/staff[1]/layer[1]/chord[1]/stem[1]
    x1:      esperado 2878   obtido 2869   (Δ -9)
    # antes: seq 19 DrawSmuflCode keySig Y -720, agora limpo

$ dart run tool/probe_diff.dart test/corpus/note/note-001.mei
test/corpus/note/note-001.mei — 0 divergências (limpo)
  # antes: seq 8 DrawSmuflCode keySig Y -721, agora limpo
```

### tool/task_check.sh view_element.dart chord note

```
PASS — unidade concluída. (debt view_element 0, analyze 8, testes verdes, famílias sem exceção)
```

## Tabela causa → arquivos destravados → antes × depois

| causa (fn + origem) | arquivos | antes (probe limpos) | depois | destravados (SVG estrutural) | destravados (SVG numérico) |
|---|---|---|---|---|---|
| `DrawSmuflCode :: DrawKeySig` (view_element.cpp:1121) via `_calcLoc` | 2 | 1 limpo (probe) | 2 limpos | 0 | +2 (note-001 limpo, chord-004 numérico) |
| `DrawLine :: DrawStaff` (view_graph.cpp:40) | 198 → 216 | 116 est / 7 num | 116 est / 10 num | 0 | +1 (keysig também conta como DrawSmuflCode) |
| `StartGraphic` (scoreDef[?]) | 37 → 1 | — | — | 0 | 0 (revertido por regressão) |

**Total rodada:** probe limpos 1→2 (+1), SVG estrutural 116→116 (0), SVG numérico 7→10 (+3).

A primeira divergência de `chord-004` migrou de `DrawSmuflCode keySig` (seq 19) para `DrawLine` de `stem` (seq 35), indicando que a fila anda, mas o `compare_svg` estrutural não moveu porque `keySig` Y é divergência numérica, não estrutural (defs/átomos).

## Fila restante (top 15 do --rank) para próxima rodada

```
 216  DrawLine :: View::DrawStaff / DrawHorizontalLine (view_graph.cpp:40)  ex.: accid-001, accid-002
  34  DrawLine :: View::DrawLine (view_graph.cpp) -> SvgDeviceContext::DrawLine (svgdevicecontext.cpp:866)  ex.: arpeg-001, arpeg-005
   8  DrawCurve :: View::DrawThickBezierCurve (view_graph.cpp:359)  ex.: arpeg-003, beam-026 (brace)
   6  StartText :: View::DrawTextString (svgdevicecontext.cpp:1003)  ex.: chord-006, clef-006
   6  DrawSmuflCode :: View::DrawMensur (view_mensural.cpp)  ex.: mensural-016, mensural-018
   1  DrawSmuflCode :: View::DrawSmuflCode (view_graph.cpp:279)  ex.: note-004 (mRest y +180)
   1  StartGraphic :: SvgDeviceContext::StartGraphic  ex.: tuplet-004
 347  fixture ausente — gere com `tool/gen_probe_fixtures.sh <fam>` (fila incompleta)
```

Causas já medidas para famílias em zero estrutural (próxima rodada deve atacar):
- `ligature` 0/50 e `mensural` 0/25: `svg/g[0]/g[2]` 5 vs 11 filhos e `defs` 14 vs 15 / 20 vs 26 — não são `DrawLine Staff`, são `castOff` mensural (sistema único com 4 ligatures vs 1 por sistema). Requer `ConvertToCastOffMensural` / `Page::LayOut` mensural.
- `tuplet` 0/22, `lyric` 0/16, `dir`/`dynam` 0/10: ainda sob `view_text`/`view_control` com `dynamic` já zerado mecanicamente mas semântica ainda via `_dyn` — próximo passo é portar guardas `if (mensur.HasNum())` etc.
- `arpeg` 0/7, `pedal` etc.: `DrawCurve` de `trill`/`pedal` e `DrawLine` de `arpeg`.

## Divergências em aberto

- **Nenhuma regressão**: 0 exceções, debt 0, analyze 8.
- **Estrutural não subiu**: a rodada não achou causa estrutural real — apenas numérica. Documentado aqui em vez de commitar barulho (MESTRE §10 regra 1). O próximo prompt deve focar em causa estrutural (ligature/mensural cast-off ou `DrawMensur` defs).
- **DrawLine Staff (216 arquivos)**: `x2` 4056→4065 (Δ 9) em `accid-001` — `measure->GetWidth()` via `rightAlignment.xRel`. Hipótese: `AdjustAccidX` não aplica `GetDrawingRadius` igual ao C++, propagando para `rightAlignment`. Próxima instrumentação: patch `adjustaccidxfunctor.cpp` para emitir `xRel_in/out` por `accid` (como 04a) e comparar com `tool/gen_probe_fixtures.sh accid`.
- **DrawLine genérico (34)**: `arpeg-001` y1 2041→2885 (Δ 844) — staff Y de `arpeg` (control element) vs `staff->GetDrawingY()`. Hipótese: `FloatingPositioner` Y de `arpeg` não bate.
- **DrawCurve brace (8)**: `arpeg-003` bezier `y` -369→-414 — `DrawBrace` (view_page.cpp) usa `doc->GetDrawingUnit` vs `GetDrawingStaffSize`. Instrumentar `view_page.cpp: DrawBrace` para emitir `yTop/yBottom`.

## Desvios do C++ introduzidos

Nenhum novo desvio além dos já listados em 2026-08-30-medium-02/03/04 (helper `_dyn`, `catch (e)`, `curly_braces` etc.). Esta rodada apenas corrigiu desvio existente em `_calcLoc` (estava divergindo do C++ `PitchInterface::CalcLoc`), aproximando do original.

## Achados fora de escopo

- `System::SetDrawingScoreDef` com `setParent` causou `assert(_parent==null)` em `test/adjust_x_overflow_test.dart` (36 falhas) porque `upcomingScoreDef` é reusado para múltiplos `System`s. Revertido. O `StartGraphic` path com `scoreDef[?]` permanece divergente (37 arquivos) — a correção correta é em `DrawRecorder::_cppPath` sem tocar modelo, ou `System` deve clonar `ScoreDef` por sistema.
- `tool/gen_probe_fixtures.sh` cobre só 274/621 arquivos; `tool/gen_probe_fixtures.sh <fam>` deve ser rodado sob demanda para as famílias em zero antes da próxima rodada, senão o ranking fica enviesado.
- `lib/src/testing/_lig_svg.dart` temporário removido.

## Critério de aceite de CADA rodada

- [x] `compare_svg --all` rodado, número medido (116→116, 7→10) — **estrutural NÃO subiu** (falha do critério, rodada não destravou causa estrutural).
- [x] `dart analyze` ≤8 (8), `dart test` verde (719, após revert).
- [x] `pisoEstrutural` não atualizado (permanece 116, pois não subiu).
- [x] Tabela causa → arquivos destravados colada (acima).
- [x] Fila restante top 15 colada (acima).
- [x] Um commit da rodada (a fazer, com mensagem resumindo 116→116 est, 7→10 num).
- [ ] Se portão não fechou: prompt da rodada seguinte **a escrever** como `prompts/2026-08-31-medium-05-fidelidade-rodada-2.md` (próximo passo).

## Próximo passo (rodada 2)

Herdar este formato e começar pela fila que sobrou. Foco recomendado: **um `small` por causa estrutural** (não numérica):

- `05r2a-ligature-castOff` (5 vs 11 filhos) — `ConvertToCastOffMensuralFunctor` / `Page::LayOut` mensural, instrumentar `measure->GetWidth()` por ligature.
- `05r2b-mensural-defs` (20 vs 26 glifos, extras E084/E086/E088/E925) — `View::DrawMensur`/`DrawProport` guardas `HasNum()`/`HasNumbase()`.
- `05r2c-barline-defs` (E4A3 vs E4A2) — `View::DrawBarLine` glyph mapping.

Se a próxima rodada também não fizer o estrutural subir, repetir o ciclo sem marcar fase concluída.

