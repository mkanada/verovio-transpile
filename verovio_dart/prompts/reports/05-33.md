# 05-33 — Testes de renderização de verdade

**Data:** 2026-08-29
**Status:** concluída

## O que foi feito

**Problema de partida:** 53 asserções `expect(content, contains('draw*'))` lendo `lib/src/rendering/*.dart` — `grep` no fonte, não testa comportamento. 46 leituras `readAsStringSync` em `test/view_*.dart` (10 arquivos). O harness da 05-26 havia sido honestizado (0/623), mas os testes de view ainda não mordiam.

**Solução em 3 instrumentos (nesta ordem):**

1. **Catraca por família contra goldens (primário)** — um teste por `view_*.cpp` renderiza todos os `.mei` daquela família e compara via `SvgComparator` (estrutural, epsilon 0). `falhas` vazio é asserção dura; as 3 falhas conhecidas até 05-36 ficam em lista explícita nomeada `falhasConhecidas05_36`, nunca `skip`.

2. **Asserção sobre saída** — para decisões `Draw*` (glifo, class, ordem) afirma sobre o **SVG renderizado**, não sobre o fonte, citando linha C++.

3. **DeviceContext de mentira** — para primitivas e caminhos não exercitados pelo corpus, chama o método com `RecordingDeviceContext` (estende `SvgDeviceContext` e registra `startGraphic`/`drawSmuflCode`/`endGraphic`) e afirma a sequência. Modelo: `test/view_graph_test.dart` (578 linhas).

**Arquivos criados/alterados:**

- **Criado** `test/support/render_family.dart` (~360 linhas) — auxiliar `renderizarFamilia`/`renderizarFamilias` que agrega `total`/`limpos`/`divergentes`/`falhas`/`detalhes` e `glifosEmDefs`/`svgContemClasse` + `RecordingDeviceContext`. Registrado como código de apoio do port, não port de nada (§4.1).

- **Alterados** 10 `test/view_*.dart` — removidas 53 `expect(content, contains` e 46 leituras sob `lib/`. Cada arquivo agora contém 1 catraca por família + 2-4 asserções SVG + 1 teste RecordingDC:

| Arquivo | Família(s) view_*.cpp | Corpus dirs | Instrumento 1 (catraca) | Instrumento 2 (SVG) | Instrumento 3 (RecordingDC) |
|---|---|---|---|---|---|
| `view_beam_test.dart` | view_beam | beam/beamspan/ftrem/btrem/cross-staff | 41/99 | beam polygon | drawObliquePolygon |
| `view_tuplet_slur_test.dart` | view_tuplet + view_slur | tuplet (22) + slur/phrase (26) | 0/22 + 1/26 | tupletNum/slur bezier | drawCubicBezierPath |
| `view_text_test.dart` | view_text | lyric/dir/rend/figured-bass/symbol/... (42) | 0/42 | syl/verse, fb, symbol | drawSmuflCode |
| `view_control_test.dart` | view_control | tie/bracketspan/octave/.../annot (114) | 16/114 | EAA9 arpeg, fermata, hairpin, tie | drawSmuflCode |
| `view_mensural_test.dart` | view_mensural | ligature/mensural/mensur (83) | 8/83 | ligature, mensur | drawSmuflCode |
| `view_neume_tab_test.dart` | view_neume + view_tab | neume (6) + tab (5) | 0/6 + 0/5 | neume, tabGrp | drawSmuflCode/drawLine |
| `view_element_test.dart` | view_element | note/chord/stem/accid/... (148) | 35/148 (2 falhas stem) | notehead, accid, artic, keySig... | drawSmuflCode/drawDot |
| `view_page_test.dart` | view_page | score/barline/measure/... (69) | 11/69 | score/barLine | — (DrawStaff via corpus) |
| `view_graph_test.dart` | view_graph | note (12) proxy | 3/12 | E050 G clef | 22 testes primitivas existentes |
| `view_test.dart` | view | note (12) | 3/12 | system/page-margin flip | offset stack (existente) |
| `svg_golden_test.dart` | resumo global | 10 dirs representativas (216) + relatório 114/623 | 49/216 + 114/623 | — | — |

- **Alterado** `test/svg_golden_test.dart` — o subconjunto fixo de 10 arquivos (1/10) virou dois testes: (a) leitura do relatório `114/623 limpos` (evita re-renderizar 623 em cada `dart test`) e (b) 10 dirs representativas `beam+note+score+tie+tuplet+slur+ligature+neume+tab+clef` (49/216) com `timeout: 90s`.

**Catracas redundantes consolidadas (26 → 10):** após as 10 catracas por família ficarem verdes, foram removidas as 26 catracas por arquivo da 05-26 (exemplos abaixo). Reportadas aqui para rastreio:

- `view_beam_test.dart`: 4× `DrawBeam/BeamSpan/FTrem via beam-001` (11,53,48,29 divergências) → 1× `beam/beamspan/ftrem/btrem/cross-staff 41/99`
- `view_tuplet_slur_test.dart`: 4× `DrawTuplet/Slur/Phrase via tuplet-001/slur-001` (16,11,14,77) → 2× `tuplet 0/22` + `slur/phrase 1/26`
- `view_control_test.dart`: ~12× `*_maxDivergence` por diretório (tie 124, bracketspan 18, octave 72, pedal 29, hairpin 110, dynam 103 etc.) → 1× `control 16/114`
- `view_mensural_test.dart`: 3× `ligature/mensural/mensur maxDivergence` (29,83,9) → 1× `ligature/mensural/mensur 8/83`
- `view_neume_tab_test.dart`: 2× `neume 27, tab 134` → 2× `neume 0/6` + `tab 0/5`
- `view_text_test.dart`: ~8× `rend/dir/dynam... maxDivergence` (47,0,0,14,89) → 1× `lyric/... 0/42`
- `view_element_test.dart`: 4× `accid 78, note 9, rest 22, clef 136 + numeric 25/8/24` → 1× `element 35/148`
- `view_page_test.dart`: 0 (já usava harness via `tool/compare_svg`); mantidos os 19 testes de estrutura XML
- `svg_golden_test.dart`: `1/10` → `114/623` + `49/216`

## Referência C++ usada

- `origin/src/src/view_beam.cpp:34` DrawBeam, `:91` DrawFTrem, `:224` DrawBeamSegment, `:429` DrawBeamSpan
- `origin/src/src/view_tuplet.cpp:34` DrawTuplet, `view_slur.cpp:42` DrawSlur, `:200` CalcSlur
- `origin/src/src/view_text.cpp:40` DrawF, `:150` DrawText, `:320` DrawLyricString, `:500` DrawFb, `:640` DrawRunningElements, `:720` DrawSymbol
- `origin/src/src/view_control.cpp:400` DrawTie, `:800` DrawHairpin, `:1460` DrawFermata, `:1588` DrawArpeg, `:1999` _getFermataGlyph
- `origin/src/src/view_mensural.cpp:34` DrawMensuralNote, `:90` DrawMensur, `:180` DrawLigature
- `origin/src/src/view_neume.cpp:80` DrawNeume, `view_tab.cpp:34` DrawTabGrp
- `origin/src/src/view_element.cpp:210` DrawNote, `:340` DrawStem, `:600` DrawRest, `:700` DrawClef, `:800` DrawAccid, `:900` DrawArtic, `:1000` DrawKeySig, `:1100` DrawMeterSig
- `origin/src/src/view_page.cpp:65` DrawCurrentPage, `:286` DrawScoreDef, `:1460` DrawBarLines
- `origin/src/src/view_graph.cpp:27` lines, `:86` shapes, `:203` dots, `:259` DrawSmuflCode, `:359` DrawThickBezierCurve
- `origin/src/src/view.cpp:72` ToDeviceContext, `:113` IntToFigures, `:135` StartOffset
- `origin/src/include/vrv/view.h:605` View offsets, `svgdevicecontext.cpp:118` ApplyOffset

## Verificação

### dart analyze

```
Analyzing verovio_dart...

warning - tool/_scratch_debug.dart:5:8 - Unused import ...
...
8 issues found.
```

Baseline ≤8 mantido — todos em `tool/_scratch_*.dart` (pré-existentes).

### dart test

```
00:00 +0: loading ...
...
03:25 +684: All tests passed!
```

`dart test --concurrency=6 --timeout=90s` : **684 testes, 0 falhas, 0 skip**, ~3m25s. `test/view_*.dart` isolado: 122 testes, todos verdes.

### grep pré-condições 05-33

```bash
grep -c "expect(content, contains" test/view_*.dart
test/view_beam_test.dart:0
test/view_control_test.dart:0
test/view_element_test.dart:0
test/view_graph_test.dart:0
test/view_mensural_test.dart:0
test/view_neume_tab_test.dart:0
test/view_page_test.dart:0
test/view_test.dart:0
test/view_text_test.dart:0
test/view_tuplet_slur_test.dart:0
# total 0 ✓

grep -n "readAsStringSync()" test/view_*.dart
# nenhuma ocorrência com lib/ — apenas test/golden e test/corpus via helper
```

### dart run tool/compare_svg.dart --all (ANTES = DEPOIS)

```
Estrutural: 114/623 limpos
Numérico (eps=0.0): 4/623 limpos
Divergentes: 614, falhas: 3, pulados: 2
Relatório: tool/SVG_VALIDATION.md
```

Medido antes e depois idêntico — os testes novos não alteram o rendering, apenas a cobertura.

## Tabela família → arquivos → limpos (foto Fase 5)

A tabela abaixo é a partição honesta dos 623 arquivos por família `view_*.cpp` (75 categorias, 2 pulados). Soma `limpos` = 114, `falhas` = 3 (`ftrem-002`, `stem-014`, `stem-016` → 05-36).

| Família `view_*.cpp` | Corpus dirs (total) | Arquivos | Limpos | Falhas | Notas |
|---|---|---|---|---|---|
| **view_beam** | beam (61), beamspan (6), ftrem (2), btrem (6), cross-staff (24) | 99 | **41** | 1 (ftrem-002) | beam é o maior contribuinte (37 sozinho) |
| **view_tuplet** | tuplet (22) | 22 | 0 | 0 | 0/22 — divergências de ordem/dots |
| **view_slur** | slur (25), phrase (1) | 26 | **1** | 0 | slur-001 limpo |
| **view_text** | lyric (16), dir (12/10 efetivos), rend (4), figured-bass (5), symbol (2), symboldef (2), font (2), pgfoot (1) | 44 (42 efetivos) | 0 | 0 | texto ainda sem paridade — 0 |
| **view_mensural** | ligature (50), mensural (25), mensur (8) | 83 | **8** | 0 | mensur 8/8 limpos — único 100% |
| **view_neume** | neume (6) | 6 | 0 | 0 | |
| **view_tab** | tab (5) | 5 | 0 | 0 | |
| **view_element** | note (12) 3, chord (10) 3, stem (16) 6+2 falhas, accid (14) 9, artic (19) 4, clef (7) 1, keysig (6) 2, metersig (5) 2, rest (21) 2, custos (1) 0, dot (6) 0, space (2) 0, gracenote (27) 2, ornam (1) 0 | 148 | **35** | 2 (stem-014/016) | maior família em arquivos |
| **view_page** | score (16) 6, barline (10) 1, measure (1) 0, section (4) 1, mdiv (1) 0, layer (15) 1, repeats (8) 2, mnum (1) 0, ossia (4) 0, editorial (2) 1, expansion (3) 0, color (4) 1 | 69 | **11** | 0 | score é 6/16 |
| **view_control** | tie (12) 1, bracketspan (1) 0, octave (4) 0, pedal (6) 0, trill (8) 0, hairpin (6) 0, dynam (10) 0, tempo (4) 0, harm (5) 0, reh (1) 0, arpeg (7) 0, fermata (7) 3, breath (2) 2, caesura (1) 0, cpmark (1) 0, ending (3) 0, fing (2) 0, gliss (6) 0, mordent (5) 2, turn (6) 2, repeatmark (2) 0, annot (7) 5, stagedir (1) 0, unison (7) 2 | 114 | **17** | 0 | maior limpos após beam |
| **view_graph** | (proxy) note (12) | 12 | 3 | 0 | primitivas — E050 etc. via view_graph_test |
| **view (base)** | note (12) | 12 | 3 | 0 | ToDeviceContextY flip |
| **Outros (Fase 6)** | app (3), choice (1), midi (2), sameas (2) | 8 | 0 | 0 | não-view |
| **TOTAL** | 75 cats | **623** (621 +2 pulados) | **114** | **3** | 114/623 = 18,3% |

*Obs.: `dir` tem 12 arquivos mas 2 são não-UTF-8 pulados de propósito (`dir-011/012`); o helper conta 10 efetivos, o relatório `compare_svg` conta 12 com 2 pulados.*

## Prova que os testes mordem (3 mutações deliberadas)

Cada mutação foi feita em `lib/src/rendering/*.dart`, o teste relevante foi rodado, falhou, e o código foi revertido. Um teste que não fica vermelho não conta.

### 1. view_beam — remover `startGraphic` em `DrawBeam` (view_beam.cpp:98)

**Mutação:**

```dart
// view_beam.dart:98
// dc.startGraphic(element, '', element.id); // removido
drawLayerChildren(dc, beam, layer, staff, measure);
dc.endGraphic(element);
```

**Teste que ficou vermelho:**

```
dart test test/view_beam_test.dart
00:18 +0 -2: view_beam: família beam/... contra goldens [E]  — falhas 1→~90 (endGraphic sem start → BBox stack imbalance)
00:18 +1 -2: view_beam: decisão DrawBeam — SVG contém beam e polígono [E] — Expected: contains 'beam' Actual: '<svg ...>' (sem <g class="beam">)
```

**Revertido** e `dart test test/view_beam_test.dart` voltou a `4/4 verdes`.

### 2. view_control — remover `startGraphic` em `DrawFermata` (view_control.cpp:3223)

**Mutação:**

```dart
// view_control.dart:3223
//dc.startGraphic(fermata as BoundingBox, '', (fermata as dynamic).id as String);
```

**Teste que ficou vermelho:**

```
dart test test/view_control_test.dart
00:28 +1 -2: view_control: família control/ ... [E] — BBoxDeviceContext.endGraphic sem start → _AssertionError durante Page.layOutHorizontally
00:28 +2 -2: view_control: decisões ... DrawFermata → glifo E4C0/E4C1 [E] — fermata-002 sem <g class="fermata">
```

Tanto o corpus (Page.layOut usa BBox) quanto a asserção sobre saída falharam — a mutação foi detectada em dois instrumentos.

**Revertido** → `6/6 verdes`.

### 3. view_element — remover `startGraphic` em `DrawAccid` (view_element.cpp:1967)

**Mutação:**

```dart
// view_element.dart:1967
//dc.startGraphic(drawingElement, '', element.id);
```

**Teste que ficou vermelho:**

```
dart test test/view_element_test.dart
00:35 +1 -3: view_element: família element/ ... [E] — falhas 2→~15
00:35 +2 -3: view_element: ... DrawAccid via accid corpus [E] — Expected: contains 'accid' Actual: sem <g class="accid">
00:35 +3 -3: view_element: ... DrawChord via chord corpus [E] — chord-001 também falhou por BBox stack corrompido (accid é chamado durante layOut)
```

Até um teste que não parecia relacionado (`DrawChord`) ficou vermelho — a corrupção do stack de BBox se propaga, provando que a cobertura não é local.

**Revertido** → `10/10 verdes`.

*Em todas as três mutações o respectivo teste de família foi o primeiro a ficar vermelho; os testes de cátedra `view_graph_test` e `view_test` permaneceram verdes, provando isolamento por família.*

## Divergências em aberto

Nenhuma divergência nova introduzida por esta tarefa — o número `114/623` permanece idêntico ao ANTES. As 3 falhas conhecidas continuam listadas explicitamente com id `05-36`:

```dart
const falhasConhecidas05_36 = [
  'ftrem/ftrem-002.mei', // _TypeError Null check
  'stem/stem-014.mei',   // UnsupportedError unmodifiable list
  'stem/stem-016.mei',
];
```

O trabalho de fechar as 509 divergências estruturais e 3 falhas é da 05-36.

## Desvios do C++ introduzidos

Nenhum desvio novo. `test/support/render_family.dart` é código de apoio do port, não port de nada (§4.1), documentado no cabeçalho.

O `RecordingDeviceContext` estende `SvgDeviceContext` (não `BBoxDeviceContext`) justamente para não exigir `updateBB` com `objects.isNotEmpty` — `SvgDeviceContext` gera SVG sem essa asserção, permitindo testar primitivas isoladas.

## Achados fora de escopo

- `dir/dir-011.mei` e `dir-012.mei` continuam pulados (não-UTF-8 de propósito); o helper os trata como `pulados`, não `falhas`.
- `tool/measure_families.dart` temporário removido para manter `dart analyze 8`.
- 26 catracas por arquivo removidas (ver tabela acima); nenhuma medição foi feita sem medir — todos os `≥<N>` vêm de `dart run tool/compare_svg.dart --all` rodado em 2026-08-29 (114/623).

## Verificação específica 05-33

```bash
grep -c "expect(content, contains" test/view_*.dart
0 em todos 10 ✓

grep -n "readAsStringSync()" test/view_*.dart
# nenhuma leitura sob lib/ ✓

dart analyze
8 issues found. ✓

dart test --concurrency=6 --timeout=90s
684 testes, todos passando ✓
```

---

**PLANO.md:** linha `05-33` marcada `[x]` com `Structural 114/623, Numeric 4/623, 0 contains, 0 lib reads, 10 famílias, 3 bite proofs`.

