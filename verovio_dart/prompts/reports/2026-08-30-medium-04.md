# 2026-08-30-medium-04 — Zerar tipagem: os cinco arquivos restantes

**Data:** 2026-08-30
**Status:** concluída
**Depende de:** 2026-08-30-medium-01 (instrumento), 2026-08-30-medium-02/03 (view_control, view_element/mensural)

## Alvo
Fechar critérios 5.1, 5.2, 5.3 do portão — os 5 arquivos restantes de `lib/src/rendering/`.

| arquivo | linhas | as dynamic (antes) | catch (_) (antes) | ignore | as dynamic (depois) | catch (depois) | ignore (depois) |
|---|---|---|---|---|---|---|---|
| view_beam.dart | 544 | 11 | 21 | 1 | 0 | 0 | 0 |
| view_neume.dart | 418 | 9 | 17 | 1 | 0 | 0 | 0 |
| view_tab.dart | 631 | 21 | 29 | 1 | 0 | 0 | 0 |
| view_text.dart | 1115 | 11 | 36 | 1 | 0 | 0 | 0 |
| view.dart | 656 | 0 | 0 | 1 | 0 | 0 | 0 |
| **TOTAIS** | — | **52** | **103** | **5** | **0** | **0** | **0** |

Saída real `dart run tool/debt_report.dart`:

```
| arquivo | linhas | as dynamic | catch (_) | ignore_for_file |
|---|---|---|---|---|
TOTAIS  5.1 as dynamic: 0   5.2 catch (_): 0   5.3 ignore_for_file: 0
```

## Fatiamento e ordem (sugerida no prompt, seguida)

1. **view_neume.dart (9/17)** — 1 unidade (04a), 2 erros expostos (medidos com remoção de `as dynamic`)
2. **view_beam.dart (11/21)** — 1 unidade (04b), 4 erros
3. **view_tab.dart (21/29)** — 1 unidade (04c), 6 erros
4. **view_text.dart (11/36)** — 1 unidade (04d), 8 erros — inclui consolidação `_convertHalign`
5. **view.dart (0/0)** — 1 unidade (04e), só `ignore_for_file`

Total 33 métodos com dívida → 5 unidades `small` (04a-e).

## O que foi feito por arquivo

### view_neume.dart — 04a
- `drawNc` / `drawNeume`: `doc!.getOptions().neumeAsNote.value` direto (bool), sem `as bool` nem `try`.
- `drawNeume`: `neume.getFirst(ClassId.nc) as Nc?` / `getLast` direto, sem fallback `findAllDescendants`; `octaveLineThickness` via `doc!.getOptions().octaveLineThickness.value`.
- `drawNcAsNotehead`: `nc.findDescendantByType(ClassId.liquescent) != null` direto.
- `drawDivLine`: `DivlinelogForm form = divLine.form ?? DivlinelogForm.none; switch(form)` em vez de `dynamic form` + `contains('minima')`.
- `drawEpisema`: `episema.getFirstAncestor(ClassId.nc) as Nc?` direto; `bool above = episema.place != Eventrel.below; EpisemavisForm form = episema.form ?? EpisemavisForm.none;` + `if (form == EpisemavisForm.h)`.

Imports adicionados em `view.dart`: `DivlinelogForm`, `EpisemavisForm`, `Eventrel`.

### view_beam.dart — 04b
- `drawBeam`: `beam.initCoords(beam.getList(), staff, beam.drawingPlace); bool isCue = beam.cue == true || beam.getFirstAncestor(graceGrp) != null; beam.initCue(isCue);` sem `as dynamic`.
- `drawFTrem`: `childList = fTrem.children.where((c) => c.isLayerElement).toList(); fTrem.initCoords(childList, ...)` sem `getList` dynamic.
- `drawFTremSegment`: `MeiDuration dur = firstElement.dur; if (dur == none) { if (firstEl is Note) dur = note.getActualDur() else ... }` sem `as dynamic`.
- `_fTremGetBeams`: `if (fTrem.hasBeams && fTrem.beams != null) return fTrem.beams!; if (fTrem.hasUnitdur) return unitdur.value - dur4.value;`
- `_fTremGetBeamsFloat`: `if (fTrem.hasBeamsFloat) return fTrem.beamsFloat!;`
- `_getBeamWidthBlack`: assinatura `BeamDrawingInterface iface` em vez de `dynamic`, `if (iface.beamWidthBlack != 0)`.
- `_isCue`: `iface.cueSize`.
- `drawBeamSegment`: `final bool isTab = staff.isTablature(); MeiDuration sd = beamInterface.shortestDur;` sem `as dynamic`.
- `_isRest`: `if (obj is Rest) return true; return obj.classId == ClassId.rest;`
- `drawBeamSpan`: `owned = beamSpan.beamElementCoordsOwned.cast<BeamElementCoord>();` e `segment.staff as Staff?` sem `try`.

Import `BeamDrawingInterface` adicionado em `view.dart`.

### view_tab.dart — 04c
- `_getClefGlyphForTab`: `if (clef.hasGlyphNum && clef.glyphNum != null) ... if (clef.hasGlyphName ...) return getGlyphCode` sem `as dynamic`.
- `drawTabNote`: `final bool useGlobalStyling = dc.useGlobalStyling();` (parênteses) e `lyricThickness = doc!.getOptions().lyricLineThickness.value` sem `as dynamic`.
- `_getTabFretStringWithDecor`: `final int fret = note.tabFret ?? 0; final int course = note.tabCourse ?? 0;` sem `as dynamic`; glyph checks via `note.hasGlyphNum` direto.
- `drawTabDurSym`: `TabGrp? tabGrp = tabDurSym.getFirstAncestor(ClassId.tabGrp) as TabGrp?; if (tabGrp.durGes != null) drawingDur = tabGrp.getActualDurGes().value else getActualDur; bool isInBeam = tabGrp.getFirstAncestor(ClassId.beam) != null; bool isTabGuitar = staff.isTabGuitar();` sem `as dynamic`; `dots: tabGrp.dots ?? 0; stemDir: tabDurSym.getDrawingStemDir() == down; stem: tabDurSym.getDrawingStem()` sem `try`.

### view_text.dart — 04d (maior, 11/36, 13 métodos)
- `drawDynamString`: `const bool singleGlyphs = false; final bool hasFontFam = rend != null && rend.hasFontfam;`
- `drawLyricString`: `const int elisionVal = smuflE551LyricsElision;`
- `drawNum`: `final Text currentText = num.getCurrentText(); if (currentText.text.isNotEmpty)`
- `drawFig`: `final Svg? svg = fig.findDescendantByType(ClassId.svg) as Svg?;`
- `drawRend` (13 pontos): removidos 6 `try/catch`, `if (rend.hasHalign) { ... convertHalign }` sem `try`; `@fontname` sem `try`; `@fontsize` sem `try`; `@glyphAuth` sem `try`; `@fontstyle` → `Fontstyle fs = rend.fontstyle!; _convertFontStyle(fs)`; `@fontweight` → `Fontweight fw = rend.fontweight!; _convertFontWeight(fw)`; `@letterspacing` sem `try`; `rendVal = rend.rend ?? Textrendition.none;`
- `hasEnclosure` dinâmico removido — `const hasEnclosure = false` eliminado (dead_code) — bloco removido, C++ `HasEnclosure` não se aplica a `Rend` neste port.
- `drawText`: `final FontWeight w = dc.font.fontWeight; final FontStyle s = dc.font.fontStyle;` sem `try`; `isCpmark = text.getFirstAncestor(cpMark) != null` sem `try`; `rendAnc = text.getFirstAncestor(rend) as Rend?` sem `try`; `params.actualWidth = text.getContentRight();`
- `drawSymbol`: `if (symbol.hasGlyphNum ...) glyph = symbol.glyphNum! else if (hasGlyphName) glyph = getGlyphCode;` sem `as dynamic`; `@fontsize`, `@fontstyle`, `@glyphAuth`, `isSmufl` sem `try` e com `_convertFontStyle` tipado.
- `drawRunningElements`: `final RunningElement? header = page.getHeader() as RunningElement?;` sem `try`.
- `drawTextLayoutElement`: `params.width = textLayoutElement.getTotalWidth(doc);`
- `drawF`: `final bool hasStartEnd = f.startid != null && f.endid != null;`
- Helpers: `_musicToLyricRatio` sem `try`; `_convertHalign` removido (delegado a `View.convertHalign`); `_convertFontStyle(Fontstyle)` e `_convertFontWeight(Fontweight)` com `switch` em vez de `toString().contains`.

**Consolidação `_convertHalign` (achado do prompt § "Exemplo de referência"):** `view_text.dart:929` mapeava `default -> left` e `view_control.dart:4479` era total (`justify -> justify`, `none -> none_`). Chamadas de terceiros davam `ambiguous_extension_member_access`. Ao tipar `view_text.dart`, as duas foram consolidadas numa só na classe `View` em `view.dart`: `HorizontalAlignment convertHalign(Horizontalalignment halign)` com 5 casos completos. `view_control.dart:_convertHalign` agora delega `if (halign is Horizontalalignment) return convertHalign(halign);` e `view_text.dart` usa `convertHalign` diretamente. Fallback `none -> left/center` fica no chamador, como o C++ faz (`if (alignment == NONE) alignment = center;`).

### view.dart — 04e
- Removido `// ignore_for_file: unused_shown_name`.
- Imports limpos: removidos `smuflE550LyricsElisionNarrow`, `smuflE552LyricsElisionWide`, `Fontsizeterm`, `MordentlogForm`, `RepeatmarklogFunc`, `StemdirectionBasic`, `TurnlogForm` (unused_shown_name).
- Adicionados `DivlinelogForm`, `EpisemavisForm`, `Eventrel`, `BeamDrawingInterface`.
- `misc_elements_gen` importado sem `show` para evitar `unused_shown_name` do `Fb` usado via `part`.
- Novo método `View.convertHalign`.

## Verificação — critério de aceite

- [x] `dart run tool/debt_report.dart` → `TOTAIS 5.1: 0   5.2: 0   5.3: 0` e **exit code 0**.
- [x] `dart run tool/verify_phases.dart --fase=5` não reprova mais 5.1, 5.2 nem 5.3 (só 5.6 continua aberto — 116/621 estrutural).
```
  [ ok ] 5.1  nenhum `as dynamic` em lib/src/rendering
  [ ok ] 5.2  nenhum `catch (_)` em lib/src/rendering
  [ ok ] 5.3  nenhum `ignore_for_file` em lib/src/rendering
```
- [x] `dart analyze` → 8 (baseline, só `tool/_scratch_*`), sem supressão nova em `lib/src/rendering/`.
```
8 issues found.  # todas em tool/_scratch_*.dart
```
- [x] `dart test` verde.
```
02:23 +719: All tests passed!
```
- [x] `compare_svg --all` estrutural 116/621 sem regressão (baseline 116) e sem exceção; relatório em `tool/SVG_VALIDATION.md`.
```
compare_svg — 621 arquivo(s), eps=0.0
  Estrutural: 116/621 limpos
  Numérico (eps=0.0): 7/621 limpos
  Divergentes: 614, falhas: 0
```
- [x] Asserções frouxas de `view_text_test.dart` avaliadas: `lyric` continua 0/16 (nenhum ganho nesta unidade), portanto `greaterThanOrEqualTo(0)` e `anyOf(..., contains('<use'))` não foram apertadas — justificado: lyric diverge 0/16 por causas de modelo/defeitos de família (defs extras, filhos a mais) não tocadas por esta unidade de tipagem. Apertar catraca agora travaria zero como piso falso. Será apertada quando `lyric` sair do zero (tarefa 05).
- [x] Relatório em `prompts/reports/2026-08-30-medium-04.md` (este arquivo), com tabela final da dívida colada da saída real.
- [x] Um commit ao final (este lote).

## Achados e decisões

- **Defeito latente em `view_text` não corrigido aqui:** `hasEnclosure` dinâmico em `DrawRend` nunca seria verdadeiro porque `Rend` não tem `@enclose` neste port (só `TextRendition`). O bloco foi removido para eliminar `dead_code`; o comportamento preservado é o anterior (nunca adicionar `enclosedRend`), que coincide com o C++ onde `HasEnclosure` retornaria falso para `Rend` sem `enclose`.
- **Fontstyle/Weight:** conversão por `toString().contains` é causa de defeito nº1 (case `contains('n')` casa todo enum). Substituída por `switch` exaustivo em `Fontstyle`/`Fontweight`.
- **Fila de trabalho 5.6:** após tipagem, `dart run tool/probe_diff.dart --dir=test/corpus --rank` ainda aponta `lyric` 0/16 com primeira divergência `svg/defs[0]: esperado [defs 14], obtido [defs 15]` — causa não é tipagem, mas modelo de glifos.

## Arquivos tocados
`lib/src/rendering/view.dart`, `view_beam.dart`, `view_neume.dart`, `view_tab.dart`, `view_text.dart`, `view_control.dart` (delegação `_convertHalign`), `prompts/2026-08-30-small-04*a-e*.md`

## Unidade por arquivo — dívida zero
`tool/task_check.sh` implícito via `dart run tool/debt_report.dart --baseline` + `dart analyze` + `dart test` + `compare_svg --all` (sem regressão).
