# 04c — AdjustTupletsXFunctor + AdjustTupletsYFunctor + AdjustTupletNumOverlapFunctor + AdjustTupletWithSlursFunctor

**Data:** 2026-08-27
**Status:** concluída com divergências documentadas (ramo beam-dependente degrada até a fase 05-17)

## O que foi feito

### Instrumentação C++ (`cpp_probe/patches/04c.patch`: 3 arquivos, **88 linhas acrescentadas, 0 removidas**)

- `adjusttupletsxfunctor.cpp`: include + captura do estado de entrada e emissão única ao fim de
  `VisitTuplet` (`br_xrelleft/right_in/out`, `num_aligned_bracket`,
  `bracket_aligned_beam`/`num_aligned_beam`, `xrel_left/right`).
- `adjusttupletsyfunctor.cpp`: include + contador de passada reutilizado; emissões em
  - `VisitTuplet` (profundidade de aninhamento, staffN, yRel de bracket/num antes/depois),
  - `AdjustTupletBracketBeamY` (articPadding, restAdjust, yRel antes/depois),
  - `CalcBracketShift` (**o retorno**, ref_x/y, slope, sign, nº de obstáculos — inserido entre o
    loop e o `return shift;`, sem tocar na linha original),
  - chamada do `AdjustTupletNumOverlap` dentro de `AdjustTupletNumY`
    (margens, drawing_y_in/out, y_rel),
  - `AdjustTupletNumOverlapFunctor::VisitLayerElement` (um registro por elemento limitante),
  - `AdjustTupletWithSlursFunctor::VisitTuplet` (yRel antes/depois, tuplet_shift).
- `page.cpp`: `probe::BeginPass("AdjustTupletsX")` (:498), `"AdjustTupletsY"` (:559) e
  `"AdjustTupletWithSlurs"` (:572), todos dentro de seus `Process`.

### Dart

| Arquivo | Mudança |
|---|---|
| `lib/src/layout/adjust_tuplets.dart` (novo, ~740 linhas) | Os quatro functors portados na ordem do C++ + helpers de modelo espelhados de `tuplet.cpp`/`elementpart.cpp`: `_getDrawingLeftRightXRel` (tuplet.cpp:272), `_getDrawingRadius` (layerelement.cpp, com leitura de métricas SMuFL igual a `Doc::GetGlyphWidth` via Resources, fallback tabulado), geometria de bracket/num (elementpart.cpp:153-250), `calculateTupletNumCrossStaff`/`hasValidTupletNumPosition` (tuplet.cpp:146/:192), `_calcDrawingBracketAndNumPos` (tuplet.cpp:208). |
| `lib/src/model/doc.dart` | `AdjustTupletsX` ligado em `layOutHorizontally` (posição do page.cpp:496, após o slot do AdjustTempo); `AdjustTupletsY` ligado em `layOutVertically` antes de AdjustSlurs (page.cpp:558); `AdjustTupletWithSlurs` depois de `fillCurvePositionerBoxes` (o substituto headless da segunda render pass, page.cpp:565-573); comentários "skipped" atualizados. |
| `lib/src/model/layer_elements_gen.dart` (+121) | **Exceção temporária registrada** (gen_elements.py quebrado): campos `bracketAlignedBeam`/`numAlignedBeam` + lista `innerSlurs` com `addInnerSlur`/`resetInnerSlurs` (mirrors `m_bracketAlignedBeam`/`m_numAlignedBeam`/`m_innerSlurs`) em `Tuplet`; campo `drawingStemAdjust` em `Stem`; cópia dos novos estados no `copyFrom`. |
| `lib/src/core/options_shell.dart` | Duas opções novas espelhando options.cpp:1565/:1573: `tupletAngledOnBeams` (false) e `tupletNumHead` (false). |
| `lib/src/layout/slur_positioning.dart` | `discardTupletElements` (espelho integral de `Slur::DiscardTupletElements`, slur.cpp:388-425) chamado ao fim de `calcSpannedElementsFor` (= slur.cpp:344): descarta colchetes de quiáltera sobrepostos e registra as slurs interiores (`addInnerSlur`). |
| `lib/src/layout/reset_functor.dart` | `ResetVerticalAlignmentFunctor.visitTuplet` chama `resetInnerSlurs()` (resetfunctor.cpp:891). |
| `test/adjust_tuplets_test.dart` (novo, 15 testes) | Paridade por arquivo (tuplet-001/010/015): X exato, flags/profundidade exatas, linhas de overlap exatas (margens = 2 unidades invariantes), invariantes estruturais de Y + tabela completa das divergências beam-dependentes impressa para este relatório. |

## Referência C++ usada

- `adjusttupletsxfunector.h/cpp` (107 l.) inteiro; `adjusttupletsyfunctor.h/cpp` (429 l.) inteiro
- `page.cpp:391-498` (LayOutHorizontally) e `:509-612` (LayOutVertically)
- `tuplet.cpp:52-310` (Reset/AddChild/FilterList/GetMelodicDirection/
  CalculateTupletNumCrossStaff/HasValidTupletNumPosition/CalcDrawingBracketAndNumPos/
  GetDrawingLeftRightXRel)
- `elementpart.cpp:145-250` (geometria TupletBracket/TupletNum)
- `layerelement.cpp:GetDrawingRadius`; `slur.cpp:388-425` (`DiscardTupletElements`)
- `options.cpp:1565/:1573`; `view_tuplet.cpp:64` e `view_beam.cpp:69` (onde o C++ executa os
  passos dependentes da renderização)

## Verificação

### dart analyze

```
10 issues found.
```

(baseline preservado)

### dart test

```
00:22 +334: All tests passed!
```

334 = baseline pós-04b (319) + 15 novos.

### Fixtures do zero + SVG idêntico

```bash
cpp_probe/build.sh 04c && for f in tuplet/tuplet-001 tuplet/tuplet-010 tuplet/tuplet-015; do ... done
```

Saída: `SVG idêntico: tuplet-001` / `-010` / `-015` (`diff` vazio × 3);
`grep -c '^-[^-]' patches/04c.patch` = **0**.

### Paridade numérica (epsilon 0)

Comparação contra a última passada presente por functor no fixture:

| Arquivo | X (flags+xRels) | Overlap (margins/drawing_y_in-out/y_rel) | Y flags+depth | yRel strict |
|---|---|---|---|---|
| tuplet-001 | 3/3 batem | 3/3 batem | 3/3 batem | — todas beam-dependentes (abaixo) |
| tuplet-010 | 1/1 bate | ausente (legítimo: números alinhados/skip do C++) | 1/1 bate | invariante: num↔colchete ✓ |
| tuplet-015 | 1/1 bate | ausente (legítimo) | 1/1 bate | invariante: num↔colchete ✓ |

Nota sobre as margens: cada linha `TupletNumOverlap` traz `horizontal_margin=180` — exatamente
`2 × getDrawingUnit(100)` no plano fatorado (assertado nos testes).

Divergências restantes, **todas da mesma família de causa** (dados de beam só existem após a
render pass; Dart headless não os produz — ver Desvios):

```
tuplet-001 beam[1]  C++ br=0    num=-720  Dart br=0    num=-720   ← coincide por coincidência? não: esta foi contada como conocida pois alinhado-a-beam
tuplet-001 beam[2]  C++ br=0    num=-1623 Dart br=0    num=-720
tuplet-001 staff[2] C++ br=0    num=-1208 Dart br=0    num=-720
tuplet-010          C++ br=711  num=711   Dart br=-1000 num=-1000
tuplet-015          C++ br=-180 num=-180  Dart br=-900 num=-900
```

Hipótese nomeada (uma causa, três sintomas):
- override do num pelo segmento do beam: `adjusttupletsyfunctor.cpp:184-191` lê
  `m_beamSegment.GetStartingY()`/`.m_beamSlope`;
- posição do colchete alinhado a beam: `AdjustTupletBracketBeamY` inteiro (`:223-309`);
- direção derivada das hastes de notas em beam: dirs atribuídas só em
  `beam.cpp:920-935` durante a render pass; `view_tuplet.cpp:64` executa
  `CalcDrawingBracketAndNumPos` com esse estado.
Substituindo os acessores `_segmentStartYOrNull/_beamStartingYFor/_beamSlopeFor` (arquivo
ajust_tuplets.dart, marcados como único ponto de troca) pela fase 05-17, estas linhas viram exatas.

`dart run tool/validate_layout.dart`: `Check notes: None. All structural assertions passed.` ·
timemap **26/30 clean** (≥ baseline 24/30) · `tuplet/tuplet-001.mei`: Layout OK, PASS×5,
timemap match(4).

## Divergências em aberto

Somente as 5 listadas acima, com causa e função/linha C++ nomeadas. Nenhuma nos valores
cobertos por dados headless-completos (X inteiro, Y flags/depth, Overlap completo).

## Desvios do C++ introduzidos

- `_calcDrawingBracketAndNumPos` roda inline nos functors (headless) em vez de na render pass
  (`view_tuplet.cpp:64`); idempotente e com a mesma entrada option default.
- Blocos que consomem `BeamSegment`/coords de FTrem são guardados e pulados
  (`_segmentStartYOrNull == null`), mantendo a estrutura do restante do functor; acessores
  concentrados num único ponto de substituição para a fase 05-17.
- Métrica de glifo da portação do raio usa Resources carregadas sob demanda (padrão
  `HeadlessExtents`), com fallback tabulado quando assets indisponíveis.

## Achados fora de escopo

- `BeamSegment::CalcBeam` (beam.cpp:89) é o pré-requisito real das divergências acima; também
  bloqueia AdjustBeams (04d) e o desenho (05-17). Recomenda-se priorizá-lo logo após 04d/05-11.
- O `doc.getGlyphWidth` do modelo segue tabulação aproximada; os functors desta tarefa usam
  Resources diretas. Migrar o modelo para resources na fase 05.
