# 2026-08-29-08 — Veredito: as Fases 1 a 5 terminaram? (portão final --full)

**Data:** 2026-08-30 (execução 2026-08-30 00:50 UTC, HEAD `0b81026`)
**Status:** veredito adversarial — sem código de produção
**Auditor:** subagente 2026-08-29-08 (portão final reutilizável, mesma metodologia 06/07)
**Modo:** `dart run tool/verify_phases.dart --full --verbose` a partir de `verovio_dart/` (medidas caras regeradas, não relatórios gravados)

> Este relatório não pressupõe que `2026-08-29-01`..`07` foram feitas — mede o que está na árvore agora, por execução real. Cada número foi reexecutado; nenhum foi inferido. Duas regras não negociam: fechar fase é consequência do número (se critério reprovar, fase ABERTA por menor que pareça) e silêncio não é aprovação (se verificação não rodou → NÃO MEDIDO → ABERTA).

---

## 1. Veredito por fase (regra não negocia: fase fecha pelo número)

| Fase | Veredito | Evidência (critério do portão + motivo) |
|---|---|---|
| **1 — Fundações** | **FECHADA** | `1.1 Resources cobre API resources.h` PASS (37 métodos), `1.2 BBoxDeviceContext: getPenWidthOverlap + setUserScale` PASS, `1.3 devicecontextbase.dart existe` PASS. Sem alargamento. |
| **2 — Modelo de dados MEI** | **FECHADA** | `2.1 todo ClassRegistrar do C++ tem registro em Dart` PASS (128 nomes), `2.2 nenhum nome registrado em duplicidade` PASS (129 registros), `2.3 F/Fb/Lv/Ossia/Phrase registrados` PASS. |
| **3 — Leitura de arquivos** | **FECHADA** | `3.1 MEIInput: todo Read* tem contraparte` PASS (190 leitores), `3.2 MusicXmlInput: todo Read* tem contraparte` PASS (20 leitores), `3.3 MEI+MusicXML+ABC ligados em identifyInputFrom` PASS, `3.4 MEIOutput não portado — Fase 6` INFO (por decisão 2026-08-29, não bloqueia). |
| **4 — Motor de layout** | **FECHADA** | `4.1 functors de transcrição + ReorderByXPos portados` PASS (4/4), `4.2 bbox_fallback/headless_extents deletados` PASS, `4.3 nenhum Approximation:` PASS, `4.5 validate_layout` PASS (618/621 layout OK, ver §2). `4.4 inventário 93/135` INFO (43 ausentes = Fase 6 + 2 const propositais). |
| **5 — Renderização SVG** | **ABERTA** | `5.1 as dynamic em lib/src/rendering` **FAIL** (739 em 10 arquivos — pior view_control 514, view_element 118, view_mensural 40), `5.2 catch (_) em lib/src/rendering` **FAIL** (820 em 10 arquivos — pior view_control 468, view_element 151, view_mensural 85), `5.3 ignore_for_file em lib/src/rendering` **FAIL** (9 arquivos), `5.6 compare_svg` **FAIL** (estrutural 115/623, numérico 4/623, 3 exceções vs alvo 621/623 nos dois, 0 exceções). 5.4 INFO (251 as dynamic + 100 catch em model/layout) e 5.5 PASS (harness_integrity_test existe). **Número não interpretável enquanto 5.1/5.2/5.3 falharem.** |

**Resumo:** 4 de 5 fases FECHADAS (1,2,3,4). Fase 5 ABERTA — mesma medição fresca de 2026-08-29 (115/623 estrutural, 4/623 numérico, 3 exceções) sem progresso nem regressão.

---

## 2. Saída completa `dart run tool/verify_phases.dart --full --verbose`

> Executado em `verovio_dart/` em 2026-08-30 00:50 UTC, após regeneração das medições caras (`compare_svg --all` ~10 min + `validate_layout` + `dart test` ~7 min). Timeout 1200000 ms.

```
Modo --full: regerando as medições caras primeiro.
  … rodando compare_svg --all (pode levar ~10 min)
  … rodando validate_layout
  … rodando dart test (pode levar ~7 min)

Transversal — PASS
------------------------------------------------------------------
  [ ok ] 0.1  dart analyze ≤ baseline (8)
           8 issues
  [ ok ] 0.2  dart test verde
           719 passaram, 0 falharam

Fase 1 — Fundações — PASS
------------------------------------------------------------------
  [ ok ] 1.1  Resources cobre a API de resources.h
           37 métodos
  [ ok ] 1.2  BBoxDeviceContext: getPenWidthOverlap + setUserScale
  [ ok ] 1.3  core/devicecontextbase.dart existe

Fase 2 — Modelo de dados MEI — PASS
------------------------------------------------------------------
  [ ok ] 2.1  todo ClassRegistrar do C++ tem registro em Dart
           128 nomes
  [ ok ] 2.2  nenhum nome registrado em duplicidade
           129 registros
  [ ok ] 2.3  F/Fb/Lv/Ossia/Phrase registrados (defeito da 04i)

Fase 3 — Leitura de arquivos — PASS
------------------------------------------------------------------
  [ ok ] 3.1  MEIInput: todo Read* do C++ tem contraparte
           190 leitores
  [ ok ] 3.2  MusicXmlInput: todo Read* do C++ tem contraparte
           20 leitores
  [ ok ] 3.3  MEI + MusicXML + ABC ligados em identifyInputFrom
  [info] 3.4  MEIOutput não portado — Fase 6 (06-08..06-11), por decisão de 2026-08-29
           a Fase 3 cobre leitura; a escrita é da Fase 6

Fase 4 — Motor de layout — PASS
------------------------------------------------------------------
  [ ok ] 4.1  functors de transcrição + ReorderByXPos portados
  [ ok ] 4.2  bbox_fallback/headless_extents deletados
  [ ok ] 4.3  nenhum comentário `Approximation:` em lib/
  [info] 4.4  inventário de functors: 93 Dart / 135 C++
           43 ausentes (esperado: Fase 6 + 2 const propositais)
  [ ok ] 4.5  validate_layout
           618/621 arquivos com layout OK

Fase 5 — Renderização SVG — FAIL (4)
------------------------------------------------------------------
  [FALHA] 5.1  nenhum `as dynamic` em lib/src/rendering
           739 em 10 arquivos — pior: view_control.dart 514, view_element.dart 118, view_mensural.dart 40
  [FALHA] 5.2  nenhum `catch (_)` em lib/src/rendering
           820 em 10 arquivos — pior: view_control.dart 468, view_element.dart 151, view_mensural.dart 85
  [FALHA] 5.3  nenhum `ignore_for_file` em lib/src/rendering
           9 arquivos — view.dart 1, view_beam.dart 1, view_control.dart 1
  [info] 5.4  mesma dívida em model/ + layout/
           251 `as dynamic`, 100 `catch (_)`
  [ ok ] 5.5  test/harness_integrity_test.dart existe
           guarda contra o harness devolver os próprios goldens
  [FALHA] 5.6  compare_svg
           estrutural 115/623, numérico 4/623, 3 exceções (alvo: 621/623 nos dois, 0 exceções)

==================================================================
VEREDITO: 4 critério(s) reprovado(s). As fases acima marcadas FAIL não terminaram.
==================================================================
```

**Métricas isoladas (mesma execução --full):**

- `dart analyze`: `8 issues found.` (todos em `tool/_scratch_*.dart`: 1+1+1+2+2+1) — baseline 8, sem supressão nova em `lib/src/rendering/` (9 arquivos com `ignore_for_file` idênticos ao baseline `a25ef2e`).
- `dart test`: `719 passaram, 0 falharam` (~7 min). Contagem subiu 681→719 após `2026-08-29-02`..`05` (38 testes novos: 2 Resources + 4 factory + 29 transcription + 3 outros). Zero `skip`.
- `tool/LAYOUT_VALIDATION.md` (regerado --full, fresco): `618/621 layout OK`, `173 timemaps`, 3 falhas pré-existentes `ftrem/ftrem-002.mei` (`Null check`), `stem/stem-014.mei` e `stem-016.mei` (`Cannot remove from unmodifiable list`).
- `tool/SVG_VALIDATION.md` (regerado --full, fresco): `115/623 estrutural`, `4/623 numérico`, `3 exceções`, `2 pulados` (dir-011, dir-012 não-UTF-8). Mesmos números de 2026-08-29 23:40 — sem progresso, sem regressão.

---

## 3. Passo 2 — o portão mede o que diz medir (não foi afrouxado desde 2026-08-29)

```bash
$ git log --oneline -- verovio_dart/tool/verify_phases.dart
8ca5e8a 2026-08-29-05: Page::LayOutTranscription + 4 functors Fase 4
a25ef2e auditoria 2026-08-29: portão de verificação das Fases 1-5 + série de fechamento

$ git diff a25ef2e..HEAD -- verovio_dart/tool/verify_phases.dart
diff --git a/verovio_dart/tool/verify_phases.dart b/verovio_dart/tool/verify_phases.dart
@@ -368,6 +368,9 @@ void verificaFase4() {
   // 4.5 — o relatório do layout, se existir e estiver fresco.
+  // Baseline medida em 2026-08-29: 618/621 (3 falhas pré-existentes em AdjustBeams:
+  // ftrem-002, stem-014, stem-016). O portão da Fase 4 exige sem regressão,
+  // não 621/621 — ver PLANO.md Estado medido e prompt 2026-08-29-05 §4.
   _leRelatorio(
@@ -380,7 +383,9 @@ void verificaFase4() {
-      return (okN == total, '$okN/$total arquivos com layout OK');
+      final bool ok =
+          (okN == total) || (total == 621 && okN >= 618 && okN <= total);
+      return (ok, '$okN/$total arquivos com layout OK');
```

**Verificação critério a critério:**

- `kAnalyzeBaseline`: **8** antes e depois (`verify_phases.dart:27` `const int kAnalyzeBaseline = 8;`). Sem alargamento (reprovaria se >8).
- `kResourcesEquivalentes`: **inalterado** — 11 entradas antes e depois, com `SetCSSFont`→`setCssFont` e `UseLiberationTextFont`→`useLiberationTextFont` já em `a25ef2e:125-136` (verificado por `git show a25ef2e:verovio_dart/tool/verify_phases.dart | grep kResourcesEquivalentes`). Nenhuma entrada nova sem justificativa C++; a tarefa 02 portou métodos em vez de acrescentar ao mapa.
- `kFunctorsFase4Abertos`: **4 nomes idênticos** antes e depois (`AdjustXRelForTranscriptionFunctor`, `AdjustYRelForTranscriptionFunctor`, `ApplyPPUFactorFunctor`, `ReorderByXPosFunctor` em `verify_phases.dart:314`). Sem encurtamento.
- `checa`/`falha`→`info`: **nenhum rebaixamento**. 4.5 continua `checa` via `_leRelatorio`; 3.4 continua `info` (MEIOutput Fase 6) desde `a25ef2e`; 5.1-5.3 continuam `checa`.
- `_leRelatorio` 4.5: único alargamento é a correção `>=618` documentada acima. **Justificado** no relatório `2026-08-29-05.md` §Achados 3 e `PLANO.md` Estado medido 2026-08-29 (baseline 618/621, 3 falhas pré-existentes em `AdjustBeamsFunctor`). O portão anterior exigia 621/621 e nunca poderia passar; a correção alinha o portão ao critério do Passo 6 e ao `PLANO.md` medido. Comentário no código cita baseline e referência. Não é adulteração para mascarar 5.6 (5.6 continua exigindo 621/623 sem alargamento).
- `5.6` alvo: continua `total - 2 = 621/623` nos dois modos e `0` exceções (`verify_phases.dart:443-460`), sem alargamento.

**Veredito Passo 2:** portão íntegro, sem critério removido, rebaixado ou com limiar alargado injustificado. Se tivesse sido adulterado, veredito seria ABERTA mesmo com PASS — não é o caso.

---

## 4. Passo 3 — as três provas que o portão não sabe fazer (não automatizáveis)

### (a) Harness ainda não devolve goldens (prova por mutação — a mais importante)

> Sem esta prova, o número da Fase 5 (115/623) não vale nada — é o defeito grave do episódio `05-26` (489 "limpos" eram goldens devolvidos).

**Histórico obrigatório:** Fase 5 fechada com `489/623` limpos, auditoria `05-26` mediu `0/623` honesto, instrumento consertado `lib/src/testing/svg_compare.dart:77` e `test/harness_integrity_test.dart` guarda — série `05-27`..`05-36` reconstruiu até `115/623`.

**(a1) `grep -rn "test/golden/cpp" lib/src/testing/svg_compare.dart` — esperado nada**

```bash
$ grep -rn "test/golden/cpp" lib/src/testing/svg_compare.dart; echo "EXIT:$?"
EXIT:1
```

**PASS** — `lib/src/testing/svg_compare.dart:1-500` não lê goldens. Doc `Does not read goldens.` em `svg_compare.dart:77` e assinatura `compare({String? dartSvg, required String goldenSvg})` recebe SVG como string.

**(a2) `grep -rn "golden" lib/src/testing/svg_compare.dart` — examine cada ocorrência**

```bash
$ grep -rn "golden" lib/src/testing/svg_compare.dart
9:/// Two comparison modes over a Dart-rendered SVG and the C++ golden SVG:
23:/// generated id ... The goldens were ...
24:/// produced without `-x` ...
76:/// `divergente`. Does not read goldens.
174:/// Compares a Dart-rendered SVG against a C++ golden SVG.
192:      {String? dartSvg, required String goldenSvg, bool runNumeric = true}) {
198:    final goldenDoc = _NormalizedDoc(goldenSvg);
...
```

**PASS** — todas as ocorrências são doc ou variável `goldenSvg`/`goldenDoc` passada pelo chamador (`tool/compare_svg.dart:331` lê golden de disco, `test/harness_integrity_test.dart:36` lê golden). Nenhum `File('test/golden`.

**(a3) `dart test test/harness_integrity_test.dart`**

```bash
$ dart test test/harness_integrity_test.dart
00:00 +0: harness integrity — 05-27 harness não lê goldens: render difere do golden para 4 famílias
Warning: 2 time pointing element(s) could not be matched in measure b1LGACOH
00:05 +1: harness integrity — 05-27 comparador não é permissivo: golden vs si mesmo é limpo
00:05 +2: harness integrity — 05-27 comparador não é permissivo: mutação única é divergente nos dois modos
00:05 +3: harness integrity — 05-27 harness não aceita SVG do disco como se fosse renderizado
00:05 +4: All tests passed!
```

**PASS** — guarda existe e passa (4 famílias divergentes `chord-001`, `ligature-001`, `beam-001`, `dir-001` realmente divergem).

**(a4) Prova viva divergente `dir-001`**

```bash
$ dart run tool/compare_svg.dart test/corpus/dir/dir-001.mei
Warning: 2 time pointing element(s) could not be matched in measure g10T8NFR
compare_svg — 1 arquivo(s), eps=0.0
  Estrutural: 0/1 limpos
  Numérico (eps=0.0): 0/1 limpos
  Divergentes: 1, falhas: 0
```

**PASS** — arquivo que C++ e Dart renderizam diferente acusa divergência (0/1). Se harness devolvesse golden, seria 1/1.

**(a5) Prova por mutação — arquivo limpo `clef/clef-002.mei` (único `clef` 1/1 nos dois modos, também um dos 4 numeric cleans: `clef-002`, `rest-007`, `rest-015`, `score-006`)**

*Antes (honesto):*

```bash
$ dart run tool/compare_svg.dart test/corpus/clef/clef-002.mei
compare_svg — 1 arquivo(s), eps=0.0
  Estrutural: 1/1 limpos
  Numérico (eps=0.0): 1/1 limpos
  Divergentes: 0, falhas: 0
```

Mutação: editar `test/golden/cpp/clef/clef-002.svg`, mudando `x="10000"` → `x="9999"` (primeira ocorrência `x="10000"` em `clef-002.svg:6572`):

```python
path = Path("test/golden/cpp/clef/clef-002.svg")
txt = path.read_text()
m = re.search(r'x="(\d+)"', txt) # x="10000"
new = txt[:m.start()] + 'x="9999"' + txt[m.end():]
path.write_text(new)
```

*Depois (golden mutado):*

```bash
$ dart run tool/compare_svg.dart test/corpus/clef/clef-002.mei
compare_svg — 1 arquivo(s), eps=0.0
  Estrutural: 1/1 limpos
  Numérico (eps=0.0): 0/1 limpos
  Divergentes: 1, falhas: 0
```

**PASS — harness honesto.** Transição `1/1 → 0/1` numérico prova que `renderSvgForComparison` produz SVG independente do golden e que `SvgComparator` compara números com epsilon 0 (`_extractNumbers` regex `r'[+-]?(?:\d+\.?\d*|\.\d+)'`). Se harness devolvesse golden, comparação permaneceria `1/1` limpo.

Desfazer:

```bash
$ git checkout -- test/golden/cpp/clef/clef-002.svg
$ dart run tool/compare_svg.dart test/corpus/clef/clef-002.mei
  Estrutural: 1/1 limpos
  Numérico (eps=0.0): 1/1 limpos
  Divergentes: 0
```

**Veredito (a): PASS — instrumento honesto, prova por mutação passou. Sem isto, número Fase 5 não valeria.**

### (b) Testes mordem (3 ao acaso entre os que cobrem trabalho recente)

> Método: mutação deliberada da linha de `lib/` coberta, `dart test -n '<trecho>'` deve ficar vermelho; `git checkout -- <arquivo>` restaura verde. Mutação via replacement de 1 ocorrência; cada falha confirmada por `Expected`/`Actual`.

| # | Teste (arquivo:trecho) | Mutação em `lib/` (arquivo:linha) | Ficou vermelho? | Verde após restore? |
|---|---|---|---|---|
| 1 | `Resources — Fase 1 lacunas (2026-08-29-02) useLiberationTextFont toggles Times/Liberation` (`test/resources_device_context_test.dart:228`) | `lib/src/rendering/resources.dart:93-94` `this.useLiberation = useLiberation;` → `this.useLiberation = !useLiberation;` | **SIM** `Expected: true Actual: <false>` (linha 237) | **SIM** `All tests passed!` após `git checkout` |
| 2 | `factory registry — oStaff / stageDir (2026-08-29-03) create('oStaff') returns Staff...` + `create('stageDir')` (`test/factory_registry_test.dart:113`) | `lib/src/factory_registry.dart:41-46` `s.n = 1; s.setOssia(true);` → `s.n = 999; s.setOssia(false);` e `lib/src/factory_registry.dart:97` `() => Dir(true)` → `() => Dir(false)` | **SIM** `Expected: true Actual: <false>` `oStaff factory must set isOssia` + `Expected: true Actual: <false>` `stageDir factory must set isStageDir` (2 falhas, 2 contrasts ainda verdes) | **SIM** `13 passed` após checkout |
| 3 | `ApplyPPUFactorFunctor — sintético LayerElement: divide drawingFacsX/Y por ppu` (`test/transcription_reorder_test.dart:256`) | `lib/src/layout/apply_ppu_factor.dart:40` `drawingFacsX / ppu` → `drawingFacsX * ppu` | **SIM** `Expected: <50> Actual: <200>` (linha 263) | **SIM** `1 passed` após checkout |

**Detalhe execução (b1):**

```
$ dart test test/resources_device_context_test.dart -n 'useLiberationTextFont toggles'
00:00 +0 -1: Expected: true Actual: <false> test/resources_device_context_test.dart 237:7
Failing tests: Resources — Fase 1 lacunas useLiberationTextFont toggles Times/Liberation
$ git checkout -- lib/src/rendering/resources.dart
00:00 +1: All tests passed!
```

**(b2):**

```
$ dart test test/factory_registry_test.dart -n 'oStaff'
00:00 +0 -2: Expected: true Actual: <false> (oStaff isOssia) + Expected: true Actual: <false> (stageDir isStageDir)
$ git checkout -- lib/src/factory_registry.dart
00:00 +13: All tests passed!
```

**(b3):**

```
$ dart test test/transcription_reorder_test.dart -n 'LayerElement: divide drawingFacsX'
00:00 +0 -1: Expected: <50> Actual: <200>
$ git checkout -- lib/src/layout/apply_ppu_factor.dart
00:00 +1: All tests passed!
```

**Veredito (b): PASS — 3/3 testes mordem exatamente na linha que cobrem; nenhum permaneceu verde sob mutação de sua linha. Prova que suite recente não é tautológica.**

### (c) SVG desenha mesma música (limpo estruturalmente != correto — olho no olho, 3 famílias diferentes)

> Comparador estrutural não considera limpos dois SVGs que desenham coisas diferentes — mas só mede estrutura, não se música soa igual. Escolher 3 arquivos estruturalmente limpos de famílias diferentes e verificar que Dart vs golden têm mesma estrutura musical (elementos, glifos, ordem) — quando há limpo, é música idêntica com erro numérico, não falso positivo.

Escolhidos **estruturalmente limpos** de famílias diferentes (ver `tool/SVG_VALIDATION.md` e `dart run tool/compare_svg.dart <arquivo>`):

| Arquivo | Família | Estrutural | Numérico | Elementos Dart vs Golden | Glifos `<defs>` Dart vs Golden | Máx desvio numérico | Mesma música? |
|---|---|---|---|---|---|---|---|
| `clef/clef-002.mei` | clef | **1/1 limpo** | **1/1 limpo** (um dos 4 numericamente limpos) | `93` vs `93` | `4` vs `4` (`E05C,E084,E4E3,E069`) | `0.0` | **Sim** — SVG idêntico (exceto `@doc` seed). |
| `mensur/mensur-01.mei` | mensur | **1/1 limpo** | **0/1** (201 divergências) | `566` vs `566` | `7` vs `7` (`E003,E004,E909,E911,E083`…) | `338.0` `svg/svg[0]/g[0]/g[2]/g[2]/g[2]/g[0]/use[0]: transform[0]=394.0 vs 403.0` | **Sim, mesma música** — árvore e conjunto de glifos idênticos, 201 coordenadas divergem (9 unidades médio). |
| `beam/beam-002.mei` | beam | **1/1 limpo** | **0/1** (36 divergências) | `115` vs `115` | `2` vs `2` (`E050,E0A4`) | `495.0` `svg/svg[0]/g[0]/g[2]/g[1]/g[0]/path[0]: d[2]=3389.0 vs 3380.0` | **Sim, mesma música** — “Three beams”, estrutura e glifos idênticos, desvio numérico em path de beam (9 unidades). |

Detalhe via `tool/proof_visual.dart` (usa `renderSvgForComparison` + `SvgComparator` + contagem `XmlDocument`):

```
=== test/corpus/clef/clef-002.mei ===
dartElements=93 goldenElements=93
dartDefs=4 goldenDefs=4 dartDefsSet=E05C,E084,E4E3,E069 ...
goldDefsSet=E05C,E084,E4E3,E069 ...
structuralClean=true numericClean=true structuralDivs=0 numericDivs=0 maxDev=0.0
glyph set identical (4 glyphs)

=== test/corpus/mensur/mensur-01.mei ===
dartElements=566 goldenElements=566
dartDefs=7 goldenDefs=7 dartDefsSet=E003,E004,E909,E911,E083 ...
structuralClean=true numericClean=false structuralDivs=0 numericDivs=201 maxDev=338.0
first numeric: svg/svg[0]/g[0]/g[2]/g[2]/g[2]/g[0]/use[0]: esperado [transform[0]=394.0], obtido [transform[0]=403.0]
glyph set identical (7 glyphs)

=== test/corpus/beam/beam-002.mei ===
dartElements=115 goldenElements=115
dartDefs=2 goldenDefs=2 dartDefsSet=E050,E0A4 ...
structuralClean=true numericClean=false structuralDivs=0 numericDivs=36 maxDev=495.0
first numeric: svg/svg[0]/g[0]/g[2]/g[1]/g[0]/path[0]: esperado [d[2]=3389.0], obtido [d[2]=3380.0]
glyph set identical (2 glyphs)
```

**Conclusão (c):** comparador estrutural não é falso positivo — nos 3 casos limpos a estrutura e o conjunto de glifos batem exatamente; quando há limpo estrutural mas divergência numérica, é mesma música deslocada (erro de coordenada, não de conteúdo). Porém **fidelidade não é real**: `621/623` não alcançado nem nas famílias fáceis, e seis difíceis permanecem zero (`ligature 0/50`, `mensural 0/25`, `tuplet 0/22`, `lyric 0/16`, `dir 0/12`, `dynam 0/10`), todas dependentes de `view_mensural.dart`/`view_control.dart` com maior densidade `catch (_)`.

**Veredito Passo 3: 3/3 provas PASS — harness honesto, testes mordem, SVGs limpos desenham mesma música (estruturalmente idênticos). Sem estas, números --full não valeriam.**

---

## 5. O que falta (só Fase 5 ABERTA)

> Para cada fase ABERTA — uma frase dizendo o que falta e qual prompt a fecha. Se prompt não existir, escrever e linkar README.md. Se já existe (ex: 05-37 criado pela 07), citar em vez de criar novo.

**Fase 5 — ABERTA:** falta quitar a dívida de tipagem (`739 as dynamic` / `820 catch (_)` / `9 ignore_for_file` em `lib/src/rendering/` — 514/468 em `view_control.dart`, 118/151 em `view_element.dart`) e elevar `115/623 estrutural`, `4/623 numérico`, `3 exceções` até `621/623` nos dois modos com `0` exceções — fecha com **prompt `05-37-fase5-tipagem-restante-e-fidelidade.md`** (criado pela auditoria `2026-08-29-07`, linkado em `prompts/README.md` linha `05-37` na seção Fase 5 reabertura, status ☐, relatório `reports/05-37.md`). Regra não negocia: fechar fase é consequência do número, não decisão; se `05-37` não atingir os dois números, escreverá `05-38`, não fechará.

*Fases 1–4 não têm frase porque estão FECHADAS.*

---

## 6. Reconciliação PLANO.md (Estado medido e checkboxes vs veredito)

**PLANO.md estava divergindo em 4 pontos — corrigido nesta entrega (diferença medida --full vs arquivo em disco antes do commit):**

| Local | Antes (PLANO.md em disco) | Agora (medido --full, corrigido) | Tipo |
|---|---|---|---|
| `## Estado medido` > métrica `dart test` | `681 testes, ~7 min, verdes` (baseline 2026-08-29 pré-02) | **`719 testes, ~7 min, verdes`** (681 + 38 novos de 02/03/05: 2 Resources, 4 factory, 29 transcription, 3 outros) — atualizado para `719` e data `2026-08-30 --full` | stale count |
| `| Fase | Estado |` linha `1 — Fundações` | `🔶 faltam SetCSSFont e UseLiberationTextFont` | **`✅ concluída`** (`2026-08-29-02` portou 37 métodos, gate 1.1 PASS) | checkbox desatualizado (tarefa 02 fechou mas PLANO não marcado) |
| `| Fase | Estado |` linha `5 — Renderização` coluna `Fecha com` | `05-34b`..`05-36` | **`05-37-fase5-tipagem-restante-e-fidelidade.md`** (prompt criado pela 07, único que fecha 5.1-5.6) | referência obsoleta (05-36 era caudas pré-tipagem) |
| `### Fase 1 — Fundações — 🔶 falta 1 item` + `- [ ] Resource/...` | checkbox aberto, texto "Falta portar..." | **`### Fase 1 — Fundações — ✅ concluída`** + `- [x] Resource/... portado em 2026-08-29-02: SetCSSFont/UseLiberationTextFont (37 métodos) — relatório reports/2026-08-29-02.md` | reconciliação com gate 1.1 PASS |

**Ação:** `PLANO.md` editado para bater com esta tabela (3 linhas em Estado medido + 1 seção Fase 1). Outras fases já batiam (`2 ✅`, `3 ✅`, `4 ✅`, `5 🔶 18,5%` mantido, `0 ✅`, `6 ⬜`, `7 🔶`). Nenhum checkbox foi marcado por antecipação; `5` permanece `🔶` até `05-37` atingir `621/623`.

---

## 7. Achados fora do portão (adversarial — com arquivo/linha, não impedem FECHADA 1–4)

| # | Arquivo:linha | C++ faz | Dart faz | Impacto | Fecha em |
|---|---|---|---|---|---|
| A1 | `lib/src/rendering/view_control.dart:1` `ignore_for_file: invalid_assignment` | `view_control.cpp` tipado | `view_control.dart` desliga verificação de tipos; `dart analyze` 8 esconde ~115 erros reais (`05-34.md:43`) | Cada `as dynamic` esconde membro de modelo inexistente; `catch (_)` vira ramo pulado silencioso | **05-37** (19/179 quitados em `1d31040`, faltam 160) |
| A2 | `lib/src/rendering/view_mensural.dart:40/85` | `view_mensural.cpp:34` `DrawMensuralNote` usa `Note::GetDrawingDur` | `drawMensuralNote` via `as dynamic` + `catch` | `ligature 0/50` e `mensural 0/25` nunca executam | 05-37 |
| A3 | `lib/src/layout/adjust_beams.dart:407` `ftrem/ftrem-002.mei` → `tool/SVG_VALIDATION.md:96` `_TypeError: Null check` | C++ testa `GetDrawingPlace` antes de `GetStemDir` | Dart `!` sem guard | Falha 1/3 | 05-37 Parte 1 (mesma lista `2026-08-29-01.md:43`) |
| A4 | `lib/src/layout/adjust_beams.dart:526` `stem/stem-014/016.mei` → `Cannot remove from unmodifiable list` | C++ `std::vector` mutável | Dart `List.unmodifiable` | Falha 2/3 | 05-37 Parte 1 |
| A5 | `test/view_text_test.dart:41` `greaterThanOrEqualTo(0)` | — | Catraca limiar 0 nunca falha | `lyric 0/16` travada em 0, teste verde mesmo se -1 | 05-37 — trocar por `greaterThan(0)` quando família sair do zero |
| A6 | `test/view_text_test.dart:81` `anyOf(contains('syl'), ..., contains('<use'))` | `View::DrawLyricString` emite `<g class="syl">` | `anyOf` com token ubíquo passa mesmo sem alvo (`drawSymbol` precoce ainda verde) | Decisão não morde | 05-37 |
| A7 | `lib/src/model/control_elements_gen.dart:599` `Dir.clone` não copia `isStageDirFlag` e `basic_elements.dart:1121` `Staff.clone` não copia `isOssiaFlag`/`n` | `Dir(const Dir&)` copia flag | `copyFrom` perde flag | `stageDir`/`oStaff` clonados perdem identidade (futuro MEIOutput Fase 6) | fora escopo 05-37, mas corrigir se tocar modelo |

Nenhum achado altera que Fases 1–4 permanecem PASS (ver §2, §7 transversal).

---

## 8. Referência estado 2026-08-29 (para saber se subiu/desceu)

> Do prompt `2026-08-29-08` e `PLANO.md` medido 2026-08-29 (auditado 06/07), antes deste veredito:

| Métrica | 2026-08-29 | Agora (2026-08-30 --full) | Δ |
|---|---|---|---|
| `dart analyze` | 8 | **8** | = |
| `dart test` | 681, ~7min, 1 falha working tree (`vertical_layout_test`) | **719, ~7min, 0 falhas** | +38 testes, falha sanada por `2026-08-29-01` (`DrawTempo` guard) |
| `compare_svg --all` | 115/623 est, 4/623 num, 3 exceções | **115/623 est, 4/623 num, 3 exceções** | = (estável) |
| `validate_layout` | 618/621 OK, 173/191 timemaps | **618/621 OK, 173/191 timemaps** | = |
| Fases fechadas | 3 de 5 (1,2 e 4 com lacuna cada; 5 em 18,5%) | **4 de 5 (1,2,3,4 fechadas; 5 em 18,5%)** | +1 (Fase 1 fechada por 02) + lacunas 2 e 4 fechadas por 03/05 |

Sem progresso em Fase 5 desde 2026-08-29; sem regressão. Fase 1–4 agora integralmente fechadas.

---

## 9. Reprodução

```bash
cd verovio_dart
# transversal + fases (rápido, mede contra relatórios frescos)
dart run tool/verify_phases.dart --verbose

# portão final (não use modo rápido para veredito: --full regera ~20 min)
dart run tool/verify_phases.dart --full --verbose

# harness honesto
grep -rn "test/golden/cpp" lib/src/testing/svg_compare.dart # EXIT:1
grep -rn "golden" lib/src/testing/svg_compare.dart | head
dart test test/harness_integrity_test.dart
dart run tool/compare_svg.dart test/corpus/dir/dir-001.mei # 0/1 divergente
# mutação golden limpo → divergente
python3 -c "import re,pathlib; p=pathlib.Path('test/golden/cpp/clef/clef-002.svg'); t=p.read_text(); m=re.search(r'x=\"(\d+)\"',t); p.write_text(t[:m.start()]+'x=\"9999\"'+t[m.end():])"
dart run tool/compare_svg.dart test/corpus/clef/clef-002.mei # 0/1 numérico divergente
git checkout -- test/golden/cpp/clef/clef-002.svg
dart run tool/compare_svg.dart test/corpus/clef/clef-002.mei # volta 1/1 limpo

# mordida 3 testes
dart test test/resources_device_context_test.dart -n 'useLiberationTextFont toggles' # verde
# (mutar lib/src/rendering/resources.dart: !useLiberation → vermelho → checkout → verde)
dart test test/factory_registry_test.dart -n 'oStaff' # verde (mutar → vermelho)
dart test test/transcription_reorder_test.dart -n 'LayerElement: divide drawingFacsX' # verde (mutar → vermelho)

# olho no olho 3 famílias
dart run tool/proof_visual.dart # 93/93, 566/566, 115/115; glyph sets idênticos; maxDev 0/338/495

# outros
dart analyze # 8 issues
dart test # 719 verdes
dart run tool/validate_layout.dart # 618/621, 173
dart run tool/verify_phases.dart --verbose # 1.1–4.5 PASS, 5.1-5.3+5.6 FAIL
git log --oneline -- verovio_dart/tool/verify_phases.dart
git diff a25ef2e..HEAD -- verovio_dart/tool/verify_phases.dart
```

---

## 10. Prompt que fecha (Fase 5 ABERTA)

Fase 5 ficou **ABERTA**, então o prompt que a fecha já existe (não criado nesta tarefa, citada):

**Arquivo:** `verovio_dart/prompts/05-37-fase5-tipagem-restante-e-fidelidade.md`

**Link em `prompts/README.md`:** seção **Fase 5 (reabertura)**, linha `05-37` adicionada após `05-36`, status `☐`, relatório `reports/05-37.md`.

Conteúdo resume §5 e §7: quitar os **160 membros restantes** de `05-34.md:142` (19/179 quitados em `1d31040`), zerar `as dynamic`/`catch (_)`/`ignore_for_file` em `lib/src/rendering/` (critérios 5.1-5.3), reproduzir `tool/SVG_VALIDATION.md` até `≥590` est / `≥400` num e zero falhas, com classificação de divergências por causa e instrumentação reativa se leitura do `.cpp` não explicar (mesmo protocolo da `05-36` Parte 2).

> **Regra não negocia:** fechar fase é consequência do número (`621/623` nos dois modos, zero exceções), não decisão. Se `05-37` não atingir os dois números, escreverá `05-38`, não fechará.

---

*Auditor: subagente 2026-08-29-08 (adversarial, portão final --full). Medição honesta: --full realmente rodado (~17 min), 3 provas Passo 3 executadas e documentadas com evidência, PLANO.md reconciliado (4 divergências corrigidas), nenhum portão afrouxado sem justificativa, nenhum teste permanece verde sob mutação de sua linha (exceto view_text reportado mas fora deste veredito), harness honesto provado por mutação golden, SVGs limpos desenham mesma música (estrutura e glifos idênticos).*
