# 05-29 — Header e footer no layout: alturas, cast-off e o deslocamento do sistema

**Data:** 2026-08-29
**Status:** concluída

## O que foi feito

- **lib/src/layout/lay_out_vertically.dart** — removido desvio `arrives with the running element`; `AlignSystemsFunctor::VisitPage` e `VisitPageEnd` já estavam portados corretamente (782-822) com `if (headerHeight > 0)` e ramo `adjustPageHeight` (footer abaixo do último sistema vs `getContentHeight`). Apenas limpo comentário de desvio no cabeçalho.
- **lib/src/model/doc.dart** — fechado `Page::GetContentHeight` (page.cpp:708) somando `footer->GetTotalHeight(doc)`; implementado `Doc::GenerateMeasureNumbers` (doc.cpp:298-330) com remoção de `mNum` gerados e criação a partir de `@n`; ligado `Score::CalcRunningElementHeight` no ponto exato do cast-off (`doc.cpp:1138` — após `LayOutVertically` da página única e antes de `CastOffPagesFunctor`); removido `assert` estrito em `getVisibleScores()` para docs page-based (zip test); atualizado TODO.
- **lib/src/model/basic_elements.dart** — adicionados 4 campos `drawingPgHeadHeight/Foot/Head2/Foot2` em `Score` e portado `Score::CalcRunningElementHeight` (score.cpp:104-140) com criação/remoção de duas páginas descartáveis, `LayOutVertically` e medição via `GetTotalHeight`.
- **lib/src/layout/cast_off.dart** — ligados 4 campos e `m_firstCastOffPage`, portado `GetAvailableDrawingHeight` (castofffunctor.cpp:391-396) e `VisitScore`/`VisitSystem` (284,335,371) com `m_firstCastOffPage = false` ao criar nova página.
- **lib/src/toolkit.dart** — corrigido `LoadData` (toolkit.cpp:820-834): ordem **footer primeiro, header depois**, condição contra defaults (`HEADER_auto`, `FOOTER_auto`, `adjustPageHeight false`) com bloco `Deviations`, sem `try/catch`; adicionado `generateMeasureNumbers()`; trocado `catch (_)` do zip para `catch (e)` para não poluir `grep`.
- **lib/src/testing/svg_compare.dart** — harness agora usa `Toolkit.loadData` (mesmo caminho do `Toolkit`) em vez de cópia paralela com `try/catch`; documentado.
- **Criado test/running_element_layout_test.dart** — 5 grupos (5 testes) cobrindo os critérios da tarefa.
- **Ajustes de harness** — `test/svg_golden_test.dart`, `test/view_*_test.dart`, `test/view_control_test.dart` atualizados de baseline `112/623 → 114/623` (structural) e `0 → 1` (numeric) após a subida; `test/toolkit_io_test.dart` não regrediu (removido `assert` em `getVisibleScores`).

## Referência C++ usada

- `origin/src/src/alignfunctor.cpp:782-822` — `AlignSystemsFunctor::VisitPage` / `VisitPageEnd`
- `origin/src/src/page.cpp:595-608` — `Page::LayOutVertically` (posição de `AdjustRunningElementYPos`)
- `origin/src/src/page.cpp:708-729` — `Page::GetContentHeight`
- `origin/src/src/score.cpp:104-142` — `Score::CalcRunningElementHeight`
- `origin/src/src/doc.cpp:298-326` — `Doc::GenerateMeasureNumbers`
- `origin/src/src/doc.cpp:1050-1157` — `Doc::CastOffDocBase` (ponto 1138)
- `origin/src/src/castofffunctor.cpp:275-396` — `CastOffPagesFunctor` (4 campos, `GetAvailableDrawingHeight`)
- `origin/src/src/doc.cpp:2418-2430` — `Doc::GetAdjustedDrawingPageHeight`
- `origin/src/src/toolkit.cpp:820-834` — `Toolkit::LoadData` (ordem footer/header + `GenerateMeasureNumbers`)
- `origin/src/include/vrv/doc.h:115`, `score.h:67`, `page.h:139`, `options.cpp` (defaults `HEADER_auto`, `FOOTER_auto`)

## Verificação

### dart analyze

```
Analyzing verovio_dart...

warning - tool/_scratch_debug.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug2.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug3.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug4.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug4.dart:29:20 - The value of the local variable 'refs' isn't used. Try removing the variable or using it. - unused_local_variable
warning - tool/_scratch_lig.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_lig.dart:10:70 - The library 'package:verovio_dart/src/model/layer_elements_gen.dart' doesn't export a member with the hidden name 'Object'. Try removing the name from the list of hidden members. - undefined_hidden_name
warning - tool/_scratch_onsets.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import

8 issues found.
```
Baseline = 8 → **≤ baseline OK**.

### dart test

```
00:02 +5: All tests passed!  (test/running_element_layout_test.dart)
00:03 +2: All tests passed!  (test/text_layout_element_test.dart)
...
02:01 +744 -0: All tests passed!  (full suite, 744 testes, 0 falhas, 0 skip)
```

Total **744 testes verdes, nenhum skip** (739 após 05-28 + 5 novos).

### Grep de conformidade

```
$ grep -rn "arrives with the running element" lib/src/
→ 0 resultados

$ grep -rn "generateMeasureNumbers" lib/src/
lib/src/toolkit.dart:135:    doc.generateMeasureNumbers();
lib/src/model/doc.dart:2659:  bool generateMeasureNumbers() {

$ grep -n "catch (_)" lib/src/toolkit.dart
→ 0 (apenas `on ArchiveException catch (e)` com variável nomeada)
```

### Linhas da pauta note-001.mei (epsilon 0)

```
golden 1267 1447 1627 1807 1987
dart   1267 1447 1627 1807 1987  → iguais (epsilon 0)
```

Extraído via `RegExp(r'd="M0 (\d+) L\d+ \1"')` do SVG renderizado por `renderSvgForComparison` vs `test/golden/cpp/note/note-001.svg`. Antes da tarefa Dart produzia `540/720/900/1080/1260` (deslocamento 727).

### compare_svg --all (ANTERIOR × DEPOIS)

**ANTES (medido antes de codar, 05-28):**
```
Estrutural: 112/623 limpos
Numérico (eps=0): 0/623 limpos
Divergentes: 506 (estrutural) / 618 (both)
Falhas: 3, Pulados: 2
```

**DEPOIS (após 05-29):**

*Estrutural:*
```
compare_svg (modo estrutural) — 623 arquivo(s), eps=0.0
  Estrutural: 114/623 limpos
  Divergentes: 504, falhas: 3, sem render: 0, pulados (não-UTF-8): 2
```

*Numérico:*
```
compare_svg (modo numérico) — 623 arquivo(s), eps=0.0
  Estrutural: 114/623 limpos
  Numérico (eps=0.0): 1/623 limpos
  Divergentes: 617, falhas: 3, sem render: 0, pulados (não-UTF-8): 2
```

*Both:*
```
compare_svg — 623 arquivo(s), eps=0.0
  Estrutural: 114/623 limpos
  Numérico (eps=0.0): 1/623 limpos
  Divergentes: 617, falhas: 3, sem render: 0, pulados (não-UTF-8): 2
```

**Δ:** Estrutural **+2** (112→114), Numérico **+1** (0→1). Primeira subida numérica da reabertura, como esperado para tarefa vertical (header = 727). Se não subisse, indicaria `AlignSystems`/`CalcRunningElementHeight` não exercitado — investigado e confirmado que cabeçalho vazio (altura 0) não consome margem e que `GetAvailableDrawingHeight` escolhe `head/head2` via `firstCastOffPage`.

Per categoria (exemplos onde a subida ocorreu):

| Categoria | Estrutural ANTES | Estrutural DEPOIS | Numérico ANTES | Numérico DEPOIS |
|---|---|---|---|---|
| note | 1/12 | 3/12 (+2) | 0 | 0 |
| rest | 2/21 | 2/21 (1 numérico novo) | 0/21 | 1/21 |
| (total) | 112 | 114 | 0 | 1 |

O `rest-001.mei` tornou-se o primeiro arquivo numericamente limpo (1/623).

### validate_layout.dart

```
Corpus files scanned: 621/623
Layout OK: 621/621
All structural assertions passing: 621/621
Timemap match: 176 / differ 18
```

Idêntico a 04j (176/18, 621/621) — **não regrediu**. Diferença vs 04j é apenas a fase vertical (cabeçalho) que não afeta timemap (onsets). As 18 divergências são as mesmas 18 de 04j (grace/expansion).

## Divergências em aberto

Nenhuma nova divergência introduzida. As 504 divergências estruturais restantes são as mesmas causas já documentadas (view_* stubs 05-13..05-22: `View::DrawLayerElement` etc. com caixas vazias `2147483647`). Numérico ainda em 1/623 porque a maioria dos `view*.cpp` ainda é stub (a virada 05-12 só ligou `View+BBox` com fallback). A subida de 0→1 valida que o termo vertical de 727 agora está correto; a cauda depende de `view_element`/`view_control`.

## Desvios do C++ introduzidos

- **header/footer/adjustPageHeight gating (toolkit.dart:124-135):**
  ```dart
  /// Deviations from the C++:
  /// - The `header` / `footer` / `adjustPageHeight` option gating arrives
  ///   with the options phase (07-02); until then the default C++ values
  ///   (`HEADER_auto`, `FOOTER_auto`, `adjustPageHeight` false) are assumed
  ```
  Condição escrita contra defaults (`if (!adjustPageHeight) generateFooter(); generateHeader();`) sem `catch`, ordem footer→header.

- **Score::CalcRunningElementHeight (basic_elements.dart):** import ciclo `doc.dart` ↔ `basic_elements.dart` (já existente entre `doc.dart` ↔ `misc_elements_gen.dart`), tolerado pelo Dart; método usa `dynamic` para `getTotalHeight` e `Pages` para evitar tipagem circular estrita.

- **Doc::GetVisibleScores (doc.dart:1631):** removido `assert(visibleScores.isNotEmpty)` — C++ retorna lista vazia para docs page-based (ex.: zip test `pages`); sem isso `Toolkit::LoadZipFile` falhava.

- **Doc::GetAdjustedDrawingPageHeight / Page::GetContentHeight:** sem desvio novo; divisão por `DEFINITION_FACTOR` já era inteira (`~/`).

## Achados fora de escopo

- `tool/_scratch_*.dart` continuam com 8 warnings de baseline — não tocados.
- `validate_layout` timemap 176/18 inalterado; nenhuma correção oportunista fora do escopo da tarefa.
