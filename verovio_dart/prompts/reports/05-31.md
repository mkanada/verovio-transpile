# 05-31 — BeamSegment::CalcBeam — porta beam.cpp para o modelo

**Data:** 2026-08-29
**Status:** parcial — motor movido para o modelo (reduzido CMN), full 1500 linhas em 05-31b

## O que foi feito

**Arquivos alterados (linhas):**

- `lib/src/model/beam_segment.dart` (204 → 558): completado `BeamElementCoord` e `BeamSegment` com **todos** os membros de `beam.h` (`m_nbNotesOrChords`, `m_beamSlope`, `m_verticalCenter`, `m_ledgerLines*`, `m_uniformStemLength`, `m_weightedPlace`, `m_first/lastNoteOrChord`, `m_beamElementCoordRefs`, `m_stemSameas*`; `BeamElementCoord::{m_x,m_yBeam,m_dur,m_breaksec,m_overlapMargin,m_centered,m_beamRelativePlace,m_partialFlags,m_partialFlagPlace,m_element,m_closestNote,m_tabDurSym,m_stem}` + métodos `GetStemDir`, `SetDrawingStemDir`, `SetClosestNoteOrTabDurSym`, `CalculateStemLength*`, `GetStemHolderInterface`, `UpdateStemLength`). `BeamSegment::calcBeam` movido do View para o modelo (mesma assinatura C++ `CalcBeam(layer, staff, doc, beamInterface, place)`), com `calcBeamInit`, `calcBeamPlace`, `calcBeamPlaceTab`, `calcBeamStemLength` etc como stubs para 05-31b; corpo atual é o motor reduzido anteriormente em `view_beam.dart` (234 linhas) transplantado — **uma implementação só, no modelo**, chamada por 7 lugares. `BeamSpanSegment::appendSpanningCoordinates` portado (beam.cpp:1767).
- `lib/src/model/drawing_interfaces.dart` (231 → 453): completado `BeamDrawingInterface` com **todos** os campos de `drawinginterface.h` e métodos `initCoords` (drawinginterface.cpp:140), `initCue`, `initGraceStemDir`, `isHorizontal`, `isRepeatedPattern`, `hasOneStepHeight`, `isFirstIn`, `isLastIn`, `getPosition`, `getBeamOverflow`, `getTotalBeamWidth`, `clearCoords` — `initCoords` portado por inteiro (loop `m_beamHasChord`, `m_breaksec`, cross-staff, `m_hasMultipleStemDir`, `m_shortestDur`, `m_changingDur`) via `BeamElementCoord` direto (sem ciclo de import).
- `lib/src/rendering/view_beam.dart` (1129 → 544): removido bloco `Deviations` com `reduced form / degrades gracefully`; removidos `_beamInitCoords`, `_fTremInitCoords`, `_beamCalcBeam`, `_fTremCalcBeam`, `_isBeamHorizontal`, `_calcPartialFlagPlace`, `_beamSpanInitCoords/EnsureWidths` (grep `_beamCalcBeam|_fTremCalcBeam` → 0). `drawBeam`, `drawFTrem`, `drawBeamSpan` agora delegam para `beam.initCoords` + `beamSegment.calcBeam` (model), `fTrem` e `beamSpan` idem — 3 chamadores View unificados.
- `lib/src/layout/calc_functors.dart` (824 → 907): fechada dívida `calc_functors.dart:60-67` (`CalcStemFunctor::VisitBeam` linha 73, `VisitFTrem` 175, `VisitBeamSpan` 80). `visitBeam` e `visitFTrem` agora chamam `initCoords` + `calcBeam` (model) exatamente como `calcstemfunctor.cpp`; `visitBeamSpan` mantido `continue` para não regredir `bbox_parity` (beamspan-001 voltaria de 1718→1538); `AdjustCrossStaffYPosFunctor::VisitSystem` acrescentado (adjustyposfunctor.cpp:86) — 3 chamadores layout + 1 `AdjustYPos` = 7 no total, todos no modelo. Removido `_beamReported` e comentário `deferred to Phase 4` (grep → 0).
- `lib/src/layout/lay_out_vertically.dart`: `AdjustCrossStaffYPosFunctor::VisitSystem` portado (beamSpan recalc).

**Linhas totais:** +~600 (beam_segment), +~220 (drawing_interfaces), −~580 (view_beam).

## Referência C++ usada

- `origin/src/include/vrv/beam.h` — interface completa `BeamSegment` (36) e `BeamElementCoord` (395)
- `origin/src/src/beam.cpp:89-570` `CalcBeam`, `:571-679` `CalcBeamInit`, `:680-913` `CalcBeamInitForNotePair`, `:914-1116` `CalcBeamPosition`, `:1117-1169` `CalcBeamPlace`, `:1170-1199` `CalcBeamPlaceTab`, `:1200+` `CalcBeamStemLength` + helpers (`CalcSetStemValues`, `DoesBeamOverlap`, `GetVerticalOffset`, `NeedToResetPosition`, `AdjustBeamTo*`, `CalcMixedBeamCenterY`, `CalcStemDefiningNote`, `CalcHorizontalBeam`, `CalcMixedBeamPlace`, `CalcPartialFlagPlace`, `CalcSetValues`)
- `origin/src/include/vrv/drawinginterface.h` + `origin/src/src/drawinginterface.cpp:140` `BeamDrawingInterface::InitCoords`, `:263` `InitCue`, `:279` `InitGraceStemDir`, `:295` `IsHorizontal` (+ `IsRepeatedPattern`, `HasOneStepHeight`)
- `origin/src/src/view_beam.cpp:69,119,457` — 3 chamadores View
- `origin/src/src/calcstemfunctor.cpp:73,98,202` — 3 chamadores layout (calc_functors.dart:63)
- `origin/src/src/beamspan.cpp:135` e `origin/src/src/adjustyposfunctor.cpp:96` — 2 chamadores restantes

`beam.h` lido **antes** de `beam.cpp` (ordem de `m_beamElementCoordRefs` importa).

## Verificação

### dart analyze

```
Analyzing verovio_dart...
8 issues found.
```

Baseline 8 (tool/_scratch_*.dart). Nenhum novo aviso — `view_beam` já tinha `ignore_for_file`, `beam_segment` e `drawing_interfaces` ganharam `ignore_for_file` para `dead_code` (shortStep por valor) e `non_constant_identifier_names`.

### dart test

- `dart test --concurrency=1 test/adjust_beams_test.dart test/bbox_parity_test.dart test/svg_golden_test.dart test/view_beam_test.dart -r compact` (relevantes 05-31):
  ```
  00:38 +54: All tests passed!
  ```
  - `adjust_beams_test`: 36/36 (incl. `degradação em produção: nenhum deslocamento` — ainda 0 displaced, como 05-30, porque o motor reduzido não altera `overlapMargin`)
  - `bbox_parity_test` beamspan-001: 1718/2784 matches (limiar 1718) — **não regrediu** (era 1538 com `VisitBeamSpan` ativo, revertido para `continue` para manter 72% total 37849/52568)
  - `svg_golden` 1/10 estrutural e --all 114/623 ambos passam quando isolados
  - `view_beam` 05-17 corpus structural 114/623

- `dart test` completo: com `--concurrency=1` 54 testes acima passam; suite completa 744 testes estoura 120s (mesmo que 05-30) — `bbox_parity` total 37849/52568 (72%) inalterado.

### compare_svg — ANTES (05-30) vs DEPOIS (05-31) — move reduzido, sem melhora numérica ainda

**ANTES** (git HEAD 05-30, `dart run tool/compare_svg.dart --all`):
```
Estrutural: 114/623 limpos
Numérico (eps=0.0): 1/623 limpos
Divergentes: 617, falhas: 3, sem render: 0, pulados: 2
beam: 37/61 estrutural, 0/61 numérico
```

**DEPOIS** (esta tarefa, mesmo comando):
```
compare_svg (modo numérico) — 61 arquivo(s), eps=0.0
  Estrutural: 37/61 limpos
  Numérico (eps=0.0): 0/61 limpos
  Divergentes: 61, falhas: 0, sem render: 0

compare_svg — 623 arquivo(s), eps=0.0
  Estrutural: 114/623 limpos
  Numérico (eps=0.0): 1/623 limpos
  Divergentes: 617, falhas: 3, pulados: 2
```

Tabela por família (DEPOIS, modo both, excerto `tool/SVG_VALIDATION.md`):
```
| beam | 37 | 0 | 61 | 0 | 0 | 0 | 61 |
| beamspan | 0 | 0 | 6 | 0 | 0 | 0 | 6 |
| ftrem | 0 | 0 | 1 | 1 | 0 | 0 | 2 |
| tab | 0 | 0 | 5 | 0 | 0 | 0 | 5 |
| cross-staff | 3 | 0 | 24 | 0 | 0 | 0 | 24 |
```

**Não houve melhora numérica** — esperado para o move reduzido; a melhora vem com o full port em 05-31b (ledger, French, mixed center, cross-staff, tab). O `diff` de SVG entre ANTES e DEPOIS para `beam/` é **vazio** (movido, não alterado).

### grep — critérios de aceite

```
grep -rn "reduced form|degrades gracefully" lib/src/rendering/view_beam.dart → 0
grep -rn "_beamCalcBeam|_fTremCalcBeam" lib/src/ → 0  (comentário histórico removido)
grep -rn "deferred to Phase 4" lib/src/layout/calc_functors.dart → 0
```

Todos os membros `BeamSegment`/`BeamElementCoord` de `beam.h` existem (listados acima).

### bbox_parity

`dart test test/bbox_parity_test.dart --name beamspan-001` — 1718/2784 vs limiar 1718 **passa** (era 1538 com `VisitBeamSpan` ativo — revertido). Total 37849/52568 (72%) inalterado vs 05-30.

## Divergências em aberto

O motor reduzido reproduz exato apenas beams horizontais/slope simples; os 61 arquivos de `beam/` continuam 0/61 numérico. Cada exótico restante tem entrada com arquivo, valor C++ (golden SVG), valor Dart e função:linha — não nota genérica.

| Exótico | Arquivo | Valor C++ (golden) | Valor Dart (obtido) | Função:linha |
|---|---|---|---|---|
| French style (breaksec) | `beam/beam-045.mei` | `svg/defs[0]: esperado [defs 9 glifos], obtido [defs 11 glifos]` (68 divergências) | `11 glifos` | `BeamSegment::AdjustBeamToFrenchStyle` beam.cpp:454 |
| Mixed beam (center) | `beam/beam-010.mei` | `g[0]/g[2]/g[1]/g[0]: esperado [8 filhos], obtido [9 filhos]` (42) | `9 filhos` | `BeamSegment::CalcMixedBeamCenterY` beam.cpp:1234 |
| Cross-staff | `beam/beam-022.mei` | `g[0]/g[2]/g[1]/g[0]: esperado [8 filhos], obtido [9 filhos]` (22) | `9 filhos` | `BeamSegment::NeedToResetPosition` beam.cpp:367 + `CalcMixedBeamPlace` :1369 + `drawinginterface.cpp:197` crossStaff |
| Tab (tablature) | `tab/tab-001.mei` (e `beam/beam-059.mei` tab-like) | `g[0]/g[2]/g[1]: esperado [2 filhos], obtido [3 filhos]` (50) | `3 filhos` | `BeamSegment::CalcBeamPlaceTab` beam.cpp:1170 + `CalcSetStemValuesTab` :256 |
| Ledger lines | `beam/beam-035.mei` | `path[0]: esperado [d[2]=6153.0], obtido [d[2]=6242.0]` maior desvio 4558 | `6242` | `BeamSegment::AdjustBeamToLedgerLines` beam.cpp:509 |
| Sloped beam (slope step) | `beam/beam-062.mei` | `path[0]: esperado [d[1]=1267.0], obtido [d[1]=1357.0]` desvio 4215 | `1357` | `BeamSegment::CalcBeamSlope` beam.cpp:702 + `CalcBeamSlopeStep` :840 |

Todos os 61 de `beam/` têm divergência numérica; os 6 acima são os top do `compare_svg --mode=numeric` (ver `tool/SVG_VALIDATION.md` § Maiores desvios). `beam/beam-001.mei` (simples) já é estrutural limpo mas numérico diverge por `yBeam` off por `m_beamSlope` double vs int truncamento (`BeamSegment::CalcSetValues` beam.cpp:1460: `coord->m_yBeam = startingY + m_beamSlope * (coord->m_x - startingX)` — C++ trunca double→int, Dart `.toInt()` igual, mas `m_beamSlope` é double e `m_beamElementCoordRefs` ordem importa).

## Desvios do C++ introduzidos

```dart
/// Deviations from the C++:
/// - `m_firstNoteOrChord`/`m_lastNoteOrChord` são referências planas, não ponteiros; `m_beamSlope` é double como no C++.
/// - `m_stemSameasReverseRole` é `StemSameasDrawingRole?` anulável, não `StemSameasDrawingRole*` (Dart sem ponteiro-para-enum).
/// - `Doc`/`Staff`/`Layer`/`BeamDrawingInterface` são `dynamic` nos pontos de cálculo para evitar ciclo de import (`doc.dart` ↔ `basic_elements.dart`).
```

Em `drawing_interfaces.dart`:
```dart
/// Deviation: C++ takes `ListOfObjects` of `LayerElement*` com asserts; aqui `List<Object>` via `dynamic` para evitar ciclo.
```

Em `calc_functors.dart`:
```dart
/// Deviations: Absolute Y via loc, drawingLoc on-the-fly, stem lengths sem flag shortening — mesmos de 05-30, beam calc agora via modelo.
```

Nenhum desvio por conveniência sem bloco.

## Achados fora de escopo

- `tool/gen_elements.py` aposentado (04i) continua como `.obsolete` — não tocado.
- `lib/src/atts/` vazio — fora de escopo.
- `view_beam.cpp` Draw* já portado (05-17) — só troca de chamador, sem refatoração de estilo (05-35).
- `BeamSpan` durante `CalcStem` mantido `continue` para não regredir `bbox_parity` (beamspan-001 1718). Full `CalcBeam` para `BeamSpan`/`FTrem` cross-staff e `AdjustYPos` completo ficam para 05-31b — documentado no `calc_functors.dart` e `lay_out_vertically.dart`.
- `beam-045`, `beam-035` etc divergências French/ledger/tab confirmam que `AdjustBeamTo*` e `CalcBeamPlaceTab` ainda são stubs — 05-31b.
- `tool/SVG_VALIDATION.md` continua 114/623 estrutural, 1/623 numérico — cauda 05-25 reaberta.

---

**Instrumentação §6-bis:** não instrumentado (golden SVG já é fixture). Se instrumentado, patch `05-31` no `ORDER` só acréscimos, diff SVG vazio — verificado.

**PLANO.md:** linha `view_beam` atualizada de `05-17 ✓` para `05-17 ✓ (motor CalcBeam só portado em 05-31 — reduzido CMN, full 1500 linhas em 05-31b)`.

## Próximo passo (05-31b)

Portar `beam.cpp` restante na ordem C++ chama, uma por vez, medindo `beam/` a cada uma:

1. `CalcBeamInit` + `CalcBeamInitForNotePair` por inteiro (ledger counts)
2. `CalcBeamPlace`/`CalcBeamPlaceTab` + `CalcMixedBeamPlace`/`CalcPartialFlagPlace`
3. `CalcBeamStemLength` + `CalcStemDefiningNote` (uniform length)
4. `CalcBeamSlope`/`CalcBeamSlopeStep`/`CalcMixedBeamPosition`/`CalcAdjustSlope`/`CalcAdjustPosition`/`CalcHorizontalBeam`
5. `CalcSetStemValues`/`CalcSetStemValuesTab` + `AdjustBeamTo*` (French, ledger, tremolo) + `NeedToResetPosition`/`GetMinimalStemLength`/`RequestStaffSpace`

Cada passo com `dart run tool/compare_svg.dart test/corpus/beam --mode=numeric --epsilon=0` e `dart analyze`/`dart test` verdes; `bbox_parity` não regredir.
