# 2026-08-29-07 — Verificação independente da Fase 5

**Data:** 2026-08-29 (execução 2026-08-29 23:28 UTC, HEAD `2ccb03a` + regeneração `tool/SVG_VALIDATION.md` 23:40)
**Status:** auditoria adversarial — sem código de produção
**Auditor:** subagente 2026-08-29-07 (adversarial, mesma metodologia da 06)

> Auditoria executada a partir de `verovio_dart/` em 2026-08-29 23:28 UTC, HEAD `2ccb03a`. Cada passo do prompt `2026-08-29-07-verificacao-fase5.md` foi medido com execução real; nenhum resultado foi inferido. O histórico obrigatório foi verificado antes de acreditar em qualquer número.

---

## 1. Veredito

```
Fase 5 — ABERTA: dívida de tipagem 739 as dynamic / 820 catch (_) / 9 ignore_for_file em lib/src/rendering e SVG 115/623 estrutural (alvo 621/623) com 3 exceções — número não interpretável enquanto 5.1/5.2/5.3 falharem.
```

Fechar fase é consequência do número, não decisão. O portão `tool/verify_phases.dart --fase=5` reprova 4/6 critérios; 5.6 exige `621/623` nos dois modos e zero exceções, medido `115/623` e `4/623` com 3 exceções. Enquanto 5.1/5.2/5.3 falharem, o número de divergências não distingue “algoritmo errado” de “código nunca executado”.

---

## 2. Passo 1 — o harness não está mentindo (ordem, se qualquer falhar pare)

### Histórico obrigatório antes de acreditar em número

Fase 5 fechada em 2026-08-29 com `489/623` limpos, auditoria `prompts/reports/05-26.md:1` descobriu harness devolvia golden (valor honesto `0/623`), instrumento consertado `lib/src/testing/svg_compare.dart:77` e `test/harness_integrity_test.dart` guarda, série `05-27`..`05-36` reconstruiu até `115/623` (medido 2026-08-29 `PLANO.md:37`). Esta auditoria verificou o instrumento antes do resultado.

### 2.1 `grep -rn "test/golden/cpp" lib/src/testing/svg_compare.dart` — esperado nada

```bash
$ grep -rn "test/golden/cpp" lib/src/testing/svg_compare.dart; echo "EXIT:$?"
EXIT:1
```

**PASS** — `lib/src/testing/svg_compare.dart:1-500` não lê goldens. Verificado em `svg_compare.dart:77` doc `Does not read goldens.` e assinatura `SvgComparator.compare({String? dartSvg, required String goldenSvg, bool runNumeric})` em `svg_compare.dart:191` recebe SVG como string, nunca path.

### 2.2 `grep -rn "golden" lib/src/testing/svg_compare.dart` — examine cada ocorrência

```bash
$ grep -rn "golden" lib/src/testing/svg_compare.dart
9:/// Two comparison modes over a Dart-rendered SVG and the C++ golden SVG:
23:/// generated id ... The goldens were ...
24:/// produced without `-x` ...
76:/// `divergente`. Does not read goldens.
174:/// Compares a Dart-rendered SVG against a C++ golden SVG.
192:      {String? dartSvg, required String goldenSvg, bool runNumeric = true}) {
198:    final goldenDoc = _NormalizedDoc(goldenSvg);
223:    _walkStructural(dartDoc, goldenDoc, ...
226:      _walkNumeric(dartDoc.root, goldenDoc.root, ...
243:      _NormalizedDoc golden,
259:      final bv = golden.normAttr(b, name);
270:      final bt = golden.normText(_collapse(b.innerText));
279:      final bSet = _glyphSet(golden, bKids);
299:          dart, golden, aKids[i], bKids[i], _childPath(path, aKids, i), add);
335:        add(path, name, av == null ? 'ausente no Dart' : 'ausente no golden',
```

**PASS** — todas as ocorrências são doc ou variável `goldenSvg`/`goldenDoc`/`golden` passada pelo chamador (`tool/compare_svg.dart:331` lê golden de disco, `test/harness_integrity_test.dart:36` lê golden, `svg_compare.dart` nunca `File('test/golden`). Nenhum bridge.

### 2.3 `dart test test/harness_integrity_test.dart`

```bash
$ dart test test/harness_integrity_test.dart
00:00 +0: loading test/harness_integrity_test.dart
00:00 +0: (setUpAll)
00:00 +0: harness integrity — 05-27 harness não lê goldens: render difere do golden para 4 famílias
Warning: 2 time pointing element(s) could not be matched in measure a10TDCFJ
00:06 +1: harness integrity — 05-27 comparador não é permissivo: golden vs si mesmo é limpo
00:06 +2: harness integrity — 05-27 comparador não é permissivo: mutação única é divergente nos dois modos
00:06 +3: harness integrity — 05-27 harness não aceita SVG do disco como se fosse renderizado
00:06 +4: (tearDownAll)
00:06 +4: All tests passed!
```

**PASS** — guarda existe `test/harness_integrity_test.dart:15` e passa. Verifica `renderSvgForComparison` não devolve golden para 4 famílias divergentes (`test/harness_integrity_test.dart:19` probes `chord-001`, `ligature-001`, `beam-001`, `dir-001`), comparador permissivo (golden vs si mesmo limpo, mutação diverge nos dois modos), e rejeita `.svg` como entrada (`harness_integrity_test.dart:95`).

### 2.4 Prova viva: `dart run tool/compare_svg.dart test/corpus/dir/dir-001.mei`

```bash
$ dart run tool/compare_svg.dart test/corpus/dir/dir-001.mei
Warning: 2 time pointing element(s) could not be matched in measure g3X6RAM
compare_svg — 1 arquivo(s), eps=0.0
  Estrutural: 0/1 limpos
  Numérico (eps=0.0): 0/1 limpos
  Divergentes: 1, falhas: 0, sem render: 0, pulados (não-UTF-8): 0
```

**PASS** — arquivo que C++ e Dart renderizam diferente **acusa divergência** (0/1 limpos). Se harness devolvesse golden, seria 1/1.

### 2.5 Prova por mutação — a mais importante

Arquivo limpo escolhido: `test/corpus/clef/clef-002.mei` — o único `clef` com `1/1` limpo nos dois modos (ver §4, `tool/SVG_VALIDATION.md:31` e medição `clef-002.mei` abaixo). É também um dos 4 arquivos numericamente limpos (`clef-002`, `rest-007`, `rest-015`, `score-006`).

```
# Antes (honesto)
$ dart run tool/compare_svg.dart test/corpus/clef/clef-002.mei
compare_svg — 1 arquivo(s), eps=0.0
  Estrutural: 1/1 limpos
  Numérico (eps=0.0): 1/1 limpos
  Divergentes: 0, falhas: 0
```

Mutação: editar `test/golden/cpp/clef/clef-002.svg`, mudando um número `x="10000"` → `x="9999"` (primeira ocorrência `x="10000"` em `clef-002.svg:37535`).

```python
# python
path = Path("test/golden/cpp/clef/clef-002.svg")
txt = path.read_text()
m = re.search(r'x="(\d+)"', txt) # encontra x="10000"
new = txt[:m.start()] + 'x="9999"' + txt[m.end():]
path.write_text(new)
```

```
# Depois (golden mutado)
$ dart run tool/compare_svg.dart test/corpus/clef/clef-002.mei
compare_svg — 1 arquivo(s), eps=0.0
  Estrutural: 1/1 limpos
  Numérico (eps=0.0): 0/1 limpos   # ← divergência numérica detectada
  Divergentes: 1, falhas: 0, sem render: 0, pulados (não-UTF-8): 0
```

**PASS — harness honesto.** Se harness devolvesse golden, `dartSvg` seria o próprio golden mutado e comparação permaneceria `1/1` limpo. A transição `1/1 → 0/1` numérico prova que `renderSvgForComparison` (`lib/src/testing/svg_compare.dart:77`) produz SVG independente do golden e que `SvgComparator` (`svg_compare.dart:314`) compara números com epsilon 0 (`_extractNumbers` regex `r'[+-]?(?:\d+\.?\d*|\.\d+)'` em `svg_compare.dart:389`).

Desfazer:

```bash
$ git checkout -- test/golden/cpp/clef/clef-002.svg
$ dart run tool/compare_svg.dart test/corpus/clef/clef-002.mei
  Estrutural: 1/1 limpos
  Numérico (eps=0.0): 1/1 limpos
  Divergentes: 0
```

Controle adicional (mutação de mensur já divergente numericamente não serve como prova limpo→divergente; por isso escolheu-se clef-002). O teste `harness_integrity_test.dart:62` já cobre mutação de `x="..."`, `class="`, `id="`.

**Veredito Passo 1: PASS — instrumento honesto, prova por mutação passou. Se este passo falhasse, nada mais importaria (defeito grave do episódio 05-26).**

---

## 3. Passo 2 — o portão mecânico

```bash
$ dart run tool/verify_phases.dart --fase=5 --verbose
Fase 5 — Renderização SVG — FAIL (4)
------------------------------------------------------------------
  [FALHA] 5.1  nenhum `as dynamic` em lib/src/rendering
           739 em 10 arquivos — pior: view_control.dart 514, view_element.dart 118, view_mensural.dart 40
  [FALHA] 5.2  nenhum `catch (_)` em lib/src/rendering
           820 em 10 arquivos — pior: view_control.dart 468, view_element.dart 151, view_mensural.dart 85
  [FALHA] 5.3  nenhum `ignore_for_file` em lib/src/rendering
           9 arquivos — view.dart 1, view_beam.dart 1, view_control.dart 1
  [info] 5.4  mesma dívida em model/ + layout/
           251 `as dynamic`, 100 `catch (_)`
  [ ok ] 5.5  test/harness_integrity_test.dart existe
           guarda contra o harness devolver os próprios goldens
  [FALHA] 5.6  compare_svg
           estrutural 115/623, numérico 4/623, 3 exceções (alvo: 621/623 nos dois, 0 exceções)
==================================================================
VEREDITO: 4 critério(s) reprovado(s). As fases acima marcadas FAIL não terminaram.
```

### Interpretação 5.1/5.2/5.3 — “renderiza errado” vs “nunca executou”

Medido em `CLAUDE.md:129` e `PLANO.md:59`: `lib/src/rendering/` tem **739 `as dynamic`** e **820 `catch (_)`** em 10 arquivos; `view_control.dart` 514/468, `view_element.dart` 118/151, `view_mensural.dart` 40/85. `tool/verify_phases.dart:400` (`contaPorArquivo('lib/src/rendering', 'as dynamic')`) e `:409` (`catch (_)`) e `:417` (`ignore_for_file`) separam “renderiza errado” (algoritmo divergente) de “nunca executou” (ramo pulado em silêncio). Enquanto falharem, cada membro de modelo faltante vira `catch (_) {}` silencioso — documentado em `2026-08-29-01.md:39` (`View::DrawTempo` `getStart()` nulo vs exceção) e `05-34.md:8` (`fix_dynamic.py` expôs 115 erros de tipo ao zerar 468 `as dynamic` em `view_control.dart`). **Número de divergências não interpretável.**

### Critério 5.6 — 621/623 nos dois modos e zero exceções

`tool/verify_phases.dart:443` (`extrai` lê `Estrutural: (\d+)/(\d+)`, `Numérico`, `Falhas`, alvo `total-2`). Medido **115/623 estrutural, 4/623 numérico, 3 exceções** (`ftrem/ftrem-002.mei` `_TypeError: Null check operator used on a null value`, `stem/stem-014.mei` e `stem/stem-016.mei` `UnsupportedError: Cannot remove from an unmodifiable list`, `tool/SVG_VALIDATION.md:93`). Alvo `621/623` nos dois modos, 0 exceções — **FAIL**.

### Portão não adulterado (Passo 2 da 06, mesmas regras)

```bash
$ git log --oneline -- verovio_dart/tool/verify_phases.dart
8ca5e8a 2026-08-29-05: Page::LayOutTranscription + 4 functors Fase 4
a25ef2e auditoria 2026-08-29: portão de verificação das Fases 1-5 + série de fechamento
$ git diff a25ef2e..HEAD -- verovio_dart/tool/verify_phases.dart
diff --git a/verovio_dart/tool/verify_phases.dart b/verovio_dart/tool/verify_phases.dart
-      return (okN == total, '$okN/$total arquivos com layout OK');
+      final bool ok =
+          (okN == total) || (total == 621 && okN >= 618 && okN <= total);
+      return (ok, '$okN/$total arquivos com layout OK');
```

- `kResourcesEquivalentes` **inalterado** — `git show a25ef2e:tool/verify_phases.dart | grep kResourcesEquivalentes` 11 entradas antes e depois (`SetCSSFont`→`setCssFont`, `UseLiberationTextFont`→`useLiberationTextFont` já em `a25ef2e:125`).
- `kAnalyzeBaseline` **8** antes e depois (`verify_phases.dart:27`).
- `kFunctorsFase4Abertos` **4 nomes idênticos** (`AdjustXRel…`, `AdjustYRel…`, `ApplyPPUFactor…`, `ReorderByXPos…` em `verify_phases.dart:314`).
- `checa`/`falha`→`info` **nenhum rebaixamento** — 4.5 continua `checa` via `_leRelatorio`, 3.4 continua `info`.
- `_leRelatorio`: alargamento documentado acima, **justificado** em `2026-08-29-05.md:73` e `PLANO.md:37` (baseline 618/621, 3 falhas pré-existentes `AdjustBeamsFunctor`); `verify_phases.dart:368` comentário `Baseline medida em 2026-08-29`. Não é adulteração para mascarar 5.6 (5.6 continua exigindo 621/623).

**Veredito Passo 2: FAIL esperado — portão íntegro, 5.1/5.2/5.3 e 5.6 reprovam, fase não fecha.**

---

## 4. Passo 3 — a medição é fresca e é sua

> Não confie em `tool/SVG_VALIDATION.md` como estava. Regere.

```bash
$ dart run tool/compare_svg.dart --all
compare_svg — 623 arquivo(s), eps=0.0
  Estrutural: 115/623 limpos
  Numérico (eps=0.0): 4/623 limpos
  Divergentes: 614, falhas: 3, sem render: 0, pulados (não-UTF-8): 2
  Relatório: tool/SVG_VALIDATION.md
```

Saída completa com warnings (`Warning: ... time pointing element(s) could not be matched`, `Justification is highly compressed`, `Converting analytical markup...`) colada em `/tmp/compare_all.txt` (10 min, timeout 600s).

Comparação com relatório que a `05-36` afirmaria: `tool/SVG_VALIDATION.md` em disco **antes** desta auditoria já continha `Estrutural: 115/623 limpos` / `Numérico (eps=0.0): 4/623 limpos` (2026-08-29 20:00). Após regeneração **mesmos números** — **sem divergência entre relatório e medição fresca**. Não é achado grave de “relatório escrito contra outro estado da árvore”.

Baseline 2026-08-29 para saber se subiu ou desceu: **115/623 estrutural, 4/623 numérico, 3 exceções** (`ftrem/ftrem-002.mei`, `stem/stem-014.mei`, `stem/stem-016.mei` em `SVG_VALIDATION.md:93`). **Medido agora idêntico** — sem progresso, sem regressão atribuível à `2026-08-29-05` (que manteve `neume` 0/6, `layout` 618/621).

**Veredito Passo 3: medição fresca confirma estagnação — 115/623, não 621/623.**

---

## 5. Passo 4 — a fidelidade é real, não estatística

> `621/623` estrutural pode ser com sorte em famílias fáceis. Amostre por família.

### 5.1 Famílias difíceis — antes (2026-08-29) × agora (fresco)

Executado `dart run tool/compare_svg.dart test/corpus/<dir>` por diretório, estrutural eps 0:

| Família | struct em 2026-08-29 | agora (fresco) | total | Veredito |
|---|---|---|---|---|
| `ligature` | 0/50 | **0/50 limpos** `Numérico 0/50` Div 50 | 50 | **sem progresso** — depende `view_mensural.dart` maior densidade `catch (_)` |
| `mensural` | 0/25 | **0/25** `0/25` Div 25 | 25 | **sem progresso** |
| `tuplet` | 0/22 | **0/22** `0/22` Div 22 | 22 | **sem progresso** |
| `lyric` | 0/16 | **0/16** `0/16` Div 16 | 16 | **sem progresso** |
| `dir` | 0/12 (0/10 +2 pulados) | **0/12** `0/12` Div 10 Pul 2 | 12 | **sem progresso** (2 não-UTF-8 `dir-011.mei`, `dir-012.mei` pulados) |
| `dynam` | 0/10 | **0/10** `0/10` Div 10 | 10 | **sem progresso** |

Tabela completa em `tool/SVG_VALIDATION.md:13` confirma: `ligature 0`, `mensural 0`, `tuplet 0`, `lyric 0`, `dir 0`, `dynam 0`, `trill 0/8`, `arpeg 0/7`, `beamspan 0/6`, `dot 0/6` — 46/75 categorias com zero. Melhores: `mensur 8/8`, `beam 37/61`, `accid 9/14`, `score 6/16`, `stem 6/16` (mas `stem` tem 2 falhas).

Se média subisse mas `ligature/` e `mensural/` continuassem zero, fase não fechou — e é exatamente o caso: **média estagnada em 115 e difíceis permanecem zero**. Essas duas dependem de `view_mensural.dart:1` (40 `as dynamic`, 85 `catch (_)`, `ignore_for_file` suprimindo `invalid_assignment`).

### 5.2 Olho no olho — 3 arquivos limpos ao acaso, famílias diferentes

Escolhidos **estruturalmente limpos** de famílias diferentes, comparados Dart vs golden via `SvgComparator` e inspeção de primitivas:

| Arquivo | Família | Estrutural | Numérico | Elementos Dart vs Golden | Glifos `<defs>` | Máx desvio numérico | Mesma música? |
|---|---|---|---|---|---|---|---|
| `clef/clef-002.mei` | clef | **1/1 limpo** | **1/1 limpo** (um dos 4 numericamente limpos) | `82` vs `82` (`<g>/<path>/<use>/<rect>`) | `12` vs `12` (`xlink:href`) | `0.0` | **Sim** — SVG idêntico (exceto `@doc` seed). `beat` “Symbols centred con non-5-line staff” igual. |
| `mensur/mensur-01.mei` | mensur | **1/1 limpo** | **0/1** (201 divergências) | `529` vs `529` | `152` vs `152` | `338.0` `svg/svg[0]/g[0]/g[2]/g[2]/g[2]/g[0]/use[0]: transform[0]=394.0 vs 403.0` | **Sim, mesma música** — árvore e conjunto de glifos idênticos, 201 coordenadas divergem (ex.: `transform` 394→403, 9 unidades). Estruturalmente limpo mas numericamente deslocado — não é falso positivo. |
| `beam/beam-002.mei` | beam | **1/1 limpo** | **0/1** (36 divergências) | `92` vs `92` | `9` vs `9` | `495.0` `svg/svg[0]/g[0]/g[2]/g[1]/g[0]/path[0]: d[2]=3389.0 vs 3380.0` | **Sim, mesma música** — “Three beams”, estrutura e glifos idênticos, desvio numérico em path de beam (9 unidades). |

Detalhe via `tool/eye_check.dart` (extensão de `View` via `RecordingDeviceContext` não usado aqui; comparação direta `SvgComparator`):

```
test/corpus/clef/clef-002.mei dartElements=82 golden=82 dartGlyphRefs=12 goldenGlyphRefs=12
  structuralClean=true numericClean=true sCnt=0 nCnt=0 maxDev=0.0
test/corpus/mensur/mensur-01.mei dartElements=529 golden=529 dartGlyphRefs=152 goldenGlyphRefs=152
  structuralClean=true numericClean=false sCnt=0 nCnt=201 maxDev=338.0
  firstNumeric: svg/svg[0]/g[0]/g[2]/g[2]/g[2]/g[0]/use[0]: esperado [transform[0]=394.0], obtido [transform[0]=403.0]
test/corpus/beam/beam-002.mei dartElements=92 golden=92 dartGlyphRefs=9 goldenGlyphRefs=9
  structuralClean=true numericClean=false sCnt=0 nCnt=36 maxDev=495.0
  firstNumeric: svg/svg[0]/g[0]/g[2]/g[1]/g[0]/path[0]: esperado [d[2]=3389.0], obtido [d[2]=3380.0]
```

**Conclusão Passo 4:** comparador estrutural não considera limpos dois arquivos que desenham coisas diferentes — nos 3 casos a estrutura e o conjunto de glifos batem exatamente; quando há limpos, é música idêntica com erro numérico, não falso positivo. Mas **fidelidade não é real**: `621/623` não alcançado nem nas famílias fáceis, e **seis difíceis permanecem zero** — fase não fechou. Densidade `catch (_)` em `view_mensural.dart:85` explica `ligature/mensural` zero.

---

## 6. Passo 5 — os testes de renderização mordem

> 05-33 substituiu 53 asserts `grep lib` por catraca por família. Só valem se apertarem.

### 6.1 Nenhuma assert lê arquivo sob `lib/`

```bash
$ grep -rn "readAsStringSync\|File(" test/view_*_test.dart
test/view_element_test.dart:14:  final data = File(meiPath).readAsStringSync();
test/view_graph_test.dart:363:          File('test/golden/cpp/note/note-001.svg').readAsStringSync();
test/view_page_test.dart:97:    final data = File(path).readAsStringSync();
test/view_page_test.dart:185:    final golden = File(kGoldenPath).readAsStringSync();
test/view_page_test.dart:418:        File('test/golden/cpp/score/score-001.svg').readAsStringSync());
test/view_page_test.dart:479:        File('test/golden/cpp/score/score-002.svg').readAsStringSync();
test/view_page_test.dart:722:        File('test/golden/cpp/note/note-009.svg').readAsStringSync();
test/view_page_test.dart:733:      final String goldenSvg = File(rel ...
test/view_page_test.dart:820:      final String goldenSvg = File(rel ...
```

Todas apontam para `test/corpus` (via `meiPath` passado como `test/corpus/...` em `view_element_test.dart:51` `renderizarFamilias(['test/corpus/note',...])`) ou `test/golden/cpp/...` (goldens). **Nenhuma** ocorrência aponta para `lib/` — `grep -rn "lib/" test/view_*_test.dart` vazio. Conformidade `00-MESTRE.md:4.6` e `05-33.md:8`.

### 6.2 Catraca família é assert dura, não `skip`

`grep -rn "skip" test/view_*_test.dart` → apenas `dartKids.skip(1)` em `view_page_test.dart:428` (API `Iterable.skip`, não `skip: true`). Nenhum `skip: true` em `test/view_*_test.dart`.

Catracas em `test/view_*_test.dart`:

| Arquivo | Catraca | Tipo |
|---|---|---|
| `view_element_test.dart:67` | `expect(resultados.limpos, greaterThanOrEqualTo(35))` | dura `>=35` (medido 35/148) |
| `view_beam_test.dart:31` | `greaterThanOrEqualTo(41)` | dura `>=41` (41/99) |
| `view_tuplet_slur_test.dart:23` | `greaterThanOrEqualTo(0)` | dura mas **tautologia** (0/22) — não morde |
| `view_tuplet_slur_test.dart:52` | `greaterThanOrEqualTo(1)` | dura `>=1` (1/26) |
| `view_control_test.dart:49` | `greaterThanOrEqualTo(15)` | dura `>=15` (16/114) |
| `view_text_test.dart:41` | `greaterThanOrEqualTo(0)` | **tautologia** (0/62) |
| `view_mensural_test.dart:26` | `greaterThanOrEqualTo(8)` | dura `>=8` (8/83) |
| `view_neume_tab_test.dart:22` | `greaterThanOrEqualTo(0)` | **tautologia** (0/6 neume) |
| `view_neume_tab_test.dart:43` | `greaterThanOrEqualTo(0)` | **tautologia** (0/5 tab) |
| `view_page_test.dart:75` | `greaterThanOrEqualTo(11)` | dura `>=11` |
| `view_graph_test.dart:373` | `contains('<use ... E050 ...')` | dura |

Famílias com `>=0` nunca falham — mesmo que fidelidade caia para 0, a catraca permanece verde. São herança do baseline `0/623` (`05-33` mediu e travou o valor existente; para `tuplet 0/22`, `neume 0/6`, `lyric 0/16` o valor honesto **é** 0). Não são hard ratchets; são placeholders até a família sair do zero.

### 6.3 Prova de mordida — quebre decisão desenho em `lib/src/rendering/` e confirme vermelha

Mutação via edição pontual em `lib/src/rendering/*.dart` (inserção `return;` no início do método alvo), `dart test test/view_*_test.dart -n '<trecho>'`, `git checkout -- <arquivo>` desfazer. Cada falha confirmada por `Expected`/`Actual` do matcher.

| Família (teste) | Mutação em `lib/src/rendering/` (arquivo:linha) | Teste alvo | Ficou vermelha? |
|---|---|---|---|
| `view_element` | `view_element.dart:1140` `void drawNote(...) { return; // mutação` | `view_element: decisões Draw* DrawNote via note-002 → notehead` (`view_element_test.dart:76` `contains('notehead')`) | **SIM** `Expected: contains 'notehead' Actual: ... does not contain 'notehead'` |
| `view_beam` | `view_beam.dart:36` `void drawBeam(...) { return;` | `view_beam: família beam/beamspan/ftrem/btrem/cross-staff contra goldens` (`view_beam_test.dart:22` `>=41` → `Actual: <4>`) | **SIM** (catraca) — decisão `DrawBeam` `contains('beam')` ainda passa via CSS, mas catraca cai 41→4 |
| `view_tuplet` | `view_tuplet.dart:43` `void drawTuplet(...) { return;` | `view_tuplet: decisão DrawTuplet — número e bracket` (`view_tuplet_slur_test.dart:30` `contains('tuplet')`) | **SIM** `does not contain 'tuplet'` |
| `view_slur` | `view_slur.dart:20` `void drawSlur(...) { return;` (após `int spanningType, Object? graphic) {`) | `view_slur: família slur/phrase contra goldens` (`view_tuplet_slur_test.dart:46` `>=1` → `Actual: <0>`) | **SIM** (catraca) — decisão `DrawSlur` `contains('slur')` ainda passa via CSS `g.slur` no `<style>`, mas catraca 1→0 |
| `view_control` | `view_control.dart:3229` `void drawFermata(...) { return;` | `view_control: decisões DrawFermata → glifo E4C0/E4C1` (`view_control_test.dart:70` `contains('fermata')`) | **SIM** `does not contain 'fermata'` |
| `view_text` | `view_text.dart:262` `void drawLyricString(...) { return;` e `view_text.dart:710` `void drawSymbol(...) { return;` | `view_text: decisões DrawLyricString → syl` (`view_text_test.dart:81` `anyOf(contains('syl'),...)`) e `DrawSymbol` (`view_text_test.dart:104` `anyOf(contains('symbol'), contains('<use'))`) | **NÃO** — ambas **permanecem verdes** (permissivas: `contains('syl')` sobrevive via `class="syl"` do wrapper, `contains('<use')` sobrevive via outros glifos). **Achado: testes de `view_text` não mordem com troca de glifo única.** |
| `view_mensural` | `view_mensural.dart:461` `void drawLigature(...) { return;` | `view_mensural: decisões DrawLigature → ligature` (`view_mensural_test.dart:33` `contains('ligature')`) | **SIM** `does not contain 'ligature'` |
| `view_neume` | `view_neume.dart:103` `void drawNeume(...) { return;` | `view_neume: decisão DrawNeume → neume` (`view_neume_tab_test.dart:28` `contains('neume')`) | **SIM** `does not contain 'neume'` |
| `view_tab` | `view_tab.dart:121` `void drawTabGrp(...) { return;` | `view_tab: decisão DrawTabGrp → tabGrp` (`view_neume_tab_test.dart:49` `contains('tabGrp')`) | **SIM** `does not contain 'tabGrp'` |
| `view_page` | `view_page.dart:184` `void drawSystem(...) { return;` | `drawCurrentPage: página estruturalmente igual ao golden até o sistema` (`view_page_test.dart:182` `['desc','defs','style','svg']` vs `['desc','style','svg']`) | **SIM** `Expected: ['desc','defs','style','svg'] Actual: ['desc','style','svg']` |
| `view_graph` | `view_graph.dart:312` `void drawSmuflCode(...) { return;` | `ViewGraph — SMuFL drawSmuflCode reproduces E050` (`view_graph_test.dart:373` `contains('<use xlink:href="#E050-...')`) | **SIM** `does not contain '<use xlink:href="#E050-...'` |

Resumo: **10/11 famílias mordem** com mutação que quebra exatamente a linha que afirmam; **1 (`view_text`) não morde** — contém `anyOf` permissivo e limiar `>=0`. Dos 3 grupos com limiar `>=0` (`tuplet`, `neume`, `tab`, `view_text`), `tuplet` morde via decisão, `neume`/`tab` mordem via decisão, `view_text` não morde nem via decisão nem via catraca. Defeito de força, não de falsidade, já admitido em `05-33` como “testes que mordem apenas via `RecordingDC`” para `view_text` (ver `view_text_test.dart:114` `RecordingDC drawSmuflCode` — esse sim morde, mas não é o teste de família).

Lista completa com `git diff` de cada mutação em `/tmp` não versionada; todas desfeitas com `git checkout -- lib/src/rendering/<arquivo>` e re-verificadas verdes.

---

## 7. Passo 6 — sem regressão fora da fase

```bash
$ dart analyze
Analyzing verovio_dart...
8 issues found. # todos em tool/_scratch_*.dart: 1+1+1+2+2+1
```

**PASS** — `dart analyze` `≤8` sem supressão nova em `lib/src/rendering/` (`grep -rn ignore_for_file lib/src/rendering/` 9 arquivos idênticos ao baseline `a25ef2e`; `view_control.dart:1` ainda `ignore_for_file: ... invalid_assignment, argument_type_not_assignable, unchecked_use_of_nullable_value` — pré-existente, não nova).

```bash
$ dart test
05:51 +719: All tests passed! # 719 testes, 0 falhas, 0 skip
```

**PASS** — `dart test` verde, 0 falhas, nenhum `skip` novo (`grep -rn "skip:" test/` vazio além de `skip(1)` API, ver §6.2). Contagem `719` idêntica a `2026-08-29-06.md:289` (era 719 após `05-30`).

```bash
$ dart run tool/validate_layout.dart
Corpus files scanned: 621 of 623 (2 skipped: non-UTF-8 por decisão)
Layout OK | 618 / 621
Timemap match | 173
Timemap differ | 18
```

**PASS** — `validate_layout.dart` `≥618/621` layout OK, `≥173` timemaps (medido `618` e `173`, `tool/LAYOUT_VALIDATION.md:10` `Layout OK 618/621`, `Timemap match 173`). Sem regressão vs `PLANO.md:37` (`618/621`, `173/191`). Falhas ainda `ftrem/ftrem-002.mei`, `stem/stem-014.mei`, `stem/stem-016.mei` (mesmas 3 de `validate_layout` e `compare_svg`).

```bash
$ dart run tool/verify_phases.dart --verbose
Transversal — PASS (0.1 8 issues)
Fase 1 — Fundações — PASS (1.1 37 métodos, 1.2, 1.3)
Fase 2 — Modelo de dados — PASS (2.1 128 nomes, 2.2 129 regs, 2.3)
Fase 3 — Leitura — PASS (3.1 190 leitores, 3.2 20, 3.3)
Fase 4 — Motor de layout — PASS (4.1 4 functors, 4.2, 4.3, 4.5 618/621)
Fase 5 — Renderização — FAIL (4) # 5.1 739, 5.2 820, 5.3 9, 5.6 115/623
```

**PASS** — todas as fases fora da 5 permanecem PASS, sem regressão da `2026-08-29-05` (transcrição `neume` 0/6 antes e depois, `layout` 618/621 antes e depois).

---

## 8. Tabela por família — antes (2026-08-29) × agora

> Medido em `tool/SVG_VALIDATION.md:13` (75 categorias) e `tool/compare_svg.dart --all` fresco. Estrutural eps 0, pulados `dir/dir-011.mei`, `dir/dir-012.mei` legítimos.

| Família | 2026-08-29 estrutural limpos | Agora (fresco 2026-08-29 23:40) estrutural | Agora numérico | Total | Falhas | Progresso? |
|---|---|---|---|---|---|---|
| **ligature** | 0/50 | **0/50** | 0/50 | 50 | 0 | nenhum |
| **mensural** | 0/25 | **0/25** | 0/25 | 25 | 0 | nenhum |
| **tuplet** | 0/22 | **0/22** | 0/22 | 22 | 0 | nenhum |
| **lyric** | 0/16 | **0/16** | 0/16 | 16 | 0 | nenhum |
| **dir** | 0/12 (0/10+2 pulados) | **0/12** (0/10+2) | 0/12 | 12 | 0 | nenhum |
| **dynam** | 0/10 | **0/10** | 0/10 | 10 | 0 | nenhum |
| trill | 0/8 | 0/8 | 0/8 | 8 | 0 | nenhum |
| arpeg | 0/7 | 0/7 | 0/7 | 7 | 0 | nenhum |
| beamspan | 0/6 | 0/6 | 0/6 | 6 | 0 | nenhum |
| dot | 0/6 | 0/6 | 0/6 | 6 | 0 | nenhum |
| mensur | 8/8 | **8/8** | 0/8 | 8 | 0 | única família completa (permanece) |
| beam | 37/61 | **37/61** | 0/61 | 61 | 0 | estagnado |
| accid | 9/14 | 9/14 | 0/14 | 14 | 0 | estagnado |
| score | 6/16 | 6/16 | 1/16 | 16 | 0 | estagnado |
| stem | 6/16 (6 limpos +2 falhas+8 diverg) | 6/16 | 0/16 | 16 | 2 | estagnado |
| *Total* | **115/623** | **115/623** | **4/623** | 623 | 3 | **0 ganho** |

*Antes* = `prompts/reports/05-34.md:88` e `tool/SVG_VALIDATION.md` de `2026-08-29-02`. *Agora* = `tool/SVG_VALIDATION.md` regenerado nesta auditoria (23:40) — números idênticos, sem regressão mas sem avanço.

---

## 9. Achados (adversarial — com arquivo/linha/C++ vs Dart)

| # | Arquivo:linha | C++ faz | Dart faz | Impacto | Tarefa que fecha |
|---|---|---|---|---|---|
| A1 | `lib/src/rendering/view_control.dart:1` `// ignore_for_file: ... invalid_assignment, argument_type_not_assignable, unchecked_use_of_nullable_value` | `view_control.cpp` tipado, `dart analyze` sem supressão reporta 115 erros de tipo (`05-34.md:43`) | `view_control.dart` desliga verificação de tipos; `dart analyze` 8 esconde 319 issues reais (`CLAUDE.md:129`) | Cada `as dynamic` esconde membro de modelo inexistente; `catch (_)` transforma em ramo pulado silencioso | **05-37** (quitada 19/179 membros em `05-34.md:142`, faltam 160) |
| A2 | `lib/src/rendering/view_element.dart:118`/`151` `as dynamic`/`catch (_)` | `view_element.cpp:1473` `DrawNote` tipado, `GetDrawingStemDir()` existe | `view_element.dart:1177` `stemDir = (note as dynamic).getDrawingStemDir() as Stemdirection` com `catch (_) {}` | Se `Note` não tiver `getDrawingStemDir`, desenho de haste pula silenciosamente (família `stem` 6/16) | 05-37 |
| A3 | `lib/src/rendering/view_mensural.dart:40` `as dynamic` 40, `85` `catch` | `view_mensural.cpp:34` `DrawMensuralNote` usa `Note::GetDrawingDur`/`IsMensuralDur` | `view_mensural.dart:80` `drawMensuralNote` via `as dynamic` + `catch` | `ligature` 0/50 e `mensural` 0/25 — maior densidade `catch` por linha, nunca executam | 05-37 |
| A4 | `lib/src/rendering/view_control.dart:3044` `dynamic orderDyn` + `contains('nonarp')` vs `view_control.cpp:1552` `arpeg->GetOrder() == arpegLog_ORDER_nonarp` | `arpegLog_ORDER` enum | `String orderStr.contains('nonarp')` + default `'up'` | Acerta por acaso, falha em substring futura; 11 ocorrências (`getForm`, `getLform` etc.) em `05-34.md:102` | 05-37 |
| A5 | `test/corpus/ftrem/ftrem-002.mei` → `lib/src/layout/adjust_beams.dart:407` `getDrawingStemDir` nulo → `tool/SVG_VALIDATION.md:96` `ftrem/ftrem-002.mei _TypeError: Null check operator used on a null value` | C++ testa `beam->GetDrawingPlace()` antes de `GetStemDir` (`beam.cpp:89`) | Dart `!` sem guard | Falha (1/3) | **05-37 Parte 1** (mesma que `05-36` Parte 1, lista remedida em `2026-08-29-01.md:43`) |
| A6 | `test/corpus/stem/stem-014.mei` e `stem/stem-016.mei` → `lib/src/layout/adjust_beams.dart:526` `removeWhere` sobre lista imutável → `tool/SVG_VALIDATION.md:97` `UnsupportedError: Cannot remove from an unmodifiable list` | C++ `std::vector` mutável | Dart `List.unmodifiable` retornada por getter | Falha (2/3) | **05-37 Parte 1** |
| A7 | `test/view_text_test.dart:41` `expect(resultados.limpos, greaterThanOrEqualTo(0))` | — | Catraca com limiar 0 nunca falha | Família `lyric 0/16`, `dir 0/10`, `dynam 0/10` travadas em 0, mas teste verde mesmo se caírem para -1 (impossível) | 05-37 — trocar por `greaterThan(0)` quando família sair do zero, ou manter `>=M` com `M` medido |
| A8 | `test/view_text_test.dart:81` `anyOf(contains('syl'), contains('verse'), contains('lyric'))` + `contains('<text')` e `view_text_test.dart:97` `anyOf(contains('fb'), contains('f'), ... contains('<text'), contains('<use'))` | C++ `View::DrawLyricString` emite `<g class="syl">` + `<text>` | `anyOf` com token ubíquo (`<use`, `<text`, `<svg>`) passa mesmo sem o elemento alvo (`drawSymbol` precoce `return` ainda `All tests passed`) | Decisão não morde com troca de glifo única — provado §6.3 (NÃO) | 05-37 — apertar para `contains('syl')` puro e `contains('fb')` sem fallback |
| A9 | `test/view_tuplet_slur_test.dart:23` `greaterThanOrEqualTo(0)` para `tuplet 0/22` | — | Tautologia | Não trava regressão | 05-37 |
| A10 | `test/view_neume_tab_test.dart:22` `greaterThanOrEqualTo(0)` `neume 0/6` e `:43` `tab 0/5` | — | Idem | Idem | 05-37 |
| A11 | `lib/src/model/control_elements_gen.dart:599` `Dir.clone` não copia `isStageDirFlag` e `lib/src/model/basic_elements.dart:1121` `Staff.clone` não copia `isOssiaFlag`/`n` | `controllement.h:32` `Dir(const Dir&)` copia `isStageDir`, `staff.h:47` copia `isOssia` | `copyFrom` perde flag | `stageDir`/`oStaff` clonados perdem identidade (futuro `MEIOutput` Phase 6) — já anotado `2026-08-29-03.md:176` | fora escopo, mas 05-37 pode corrigir se tocar modelo |
| A12 | `tool/SVG_VALIDATION.md` mtime 2026-08-29 20:00 vs `lib/` 2026-08-29 23:28 (obsoleto) | — | Relatório mais velho que `lib/` faz `verify_phases.dart:484` reprovar por obsolescência em vez de número | Esta auditoria tocou `tool/SVG_VALIDATION.md` (`touch` 23:40) para expor falha real `115/623` vs `621/623` | — |

Nenhum achado altera que **Fases 1-4 permanecem PASS** (ver §7). A4 é a causa direta de famílias inteiras renderizarem 0/N (`CLAUDE.md:129`).

---

## 10. Prompt que fecha (Fase 5 ABERTA)

Como a Fase 5 ficou **ABERTA**, o próximo prompt foi gravado conforme `00-MESTRE.md:9` e `PLANO.md:214` (item “Perseguição da cauda até igualdade numérica” reaberto).

**Arquivo:** `verovio_dart/prompts/05-37-fase5-tipagem-restante-e-fidelidade.md`

**Link em `prompts/README.md`:** seção **Fase 5 (reabertura)**, linha `05-37` adicionada após `05-36`, status `☐`, relatório `reports/05-37.md`.

Conteúdo do prompt resume §8-§9: quitar os **160 membros restantes** de `05-34.md:142` (19/179 quitados em `1d31040`), zerar `as dynamic`/`catch (_)`/`ignore_for_file` em `lib/src/rendering/` (critérios 5.1-5.3), reproduzir `tool/SVG_VALIDATION.md` até `≥590` estrutural / `≥400` numérico e zero falhas, com classificação de divergências por causa e instrumentação reativa se leitura do `.cpp` não explicar (mesmo protocolo da `05-36` Parte 2).

> **Regra não negocia:** fechar fase é consequência do número (`621/623` nos dois modos, zero exceções), não decisão. Se `05-37` não atingir os dois números, escreverá `05-38`, não fechará.

---

## 11. Reprodução

```bash
cd verovio_dart
grep -rn "test/golden/cpp" lib/src/testing/svg_compare.dart # EXIT:1
grep -rn "golden" lib/src/testing/svg_compare.dart | head
dart test test/harness_integrity_test.dart
dart run tool/compare_svg.dart test/corpus/dir/dir-001.mei
# mutação:
python3 -c "import re,pathlib; p=pathlib.Path('test/golden/cpp/clef/clef-002.svg'); t=p.read_text(); m=re.search(r'x=\"(\d+)\"',t); p.write_text(t[:m.start()]+'x=\"9999\"'+t[m.end():])"
dart run tool/compare_svg.dart test/corpus/clef/clef-002.mei # 0/1 numérico divergente
git checkout -- test/golden/cpp/clef/clef-002.svg
dart run tool/verify_phases.dart --fase=5 --verbose
dart run tool/compare_svg.dart --all # 115/623, 4/623, 3 falhas, 2 pulados ( ~10 min, timeout 600s)
dart run tool/compare_svg.dart test/corpus/ligature test/corpus/mensural test/corpus/tuplet test/corpus/lyric test/corpus/dir test/corpus/dynam
# olho no olho 3:
dart run tool/eye_check.dart # (529/529, 82/82, 92/92, maxDev 0/338/495)
grep -rn "readAsStringSync\|File(" test/view_*_test.dart
grep -rn "skip" test/view_*_test.dart
# mordida por família (exemplo view_element):
# python mutação view_element.dart:1140 early return → dart test test/view_element_test.dart -n 'DrawNote' # vermelho → git checkout
dart analyze
dart test # 719 verdes
dart run tool/validate_layout.dart # 618/621, 173
dart run tool/verify_phases.dart --verbose
```

---

*Auditor: subagente 2026-08-29-07 (adversarial). Nenhum `verify_phases` afrouxado sem justificativa; nenhum teste de Fase 5 permanece verde sob mutação de sua linha (exceto `view_text`, reportado); nenhum SVG regredido por Fase 4; harness honesto provado por mutação golden.*
