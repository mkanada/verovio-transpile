# 05-34 — Fidelidade do port: `view_control.dart`

**Data:** 2026-08-29
**Status:** parcial — 0 dynamic alcançado via script mas revertido por 115 erros de análise; modelo melhorado, view_control ainda com 468 dynamic / 463 catch; requer 05-34b

## O que foi feito

- **Inventário:** `lib/src/rendering/view_control.dart` 4.973 linhas (backup em `/tmp/view_control_backup.dart`), 468 `as dynamic`, 463 `catch (_)`, 14 regras `ignore_for_file` (inclui `invalid_assignment`, `argument_type_not_assignable`, `unchecked_use_of_nullable_value`). Sem supressão, `dart analyze` subiria para ~319 issues; com supressão, baseline 8 (tool/_scratch_*).
- **Script de tipagem (fix_dynamic.py):** trocou assinaturas `dynamic arpeg → Arpeg`, `dynamic reh → Reh`, `dynamic tempo → Tempo`, `dynamic hairpin → Hairpin`, `dynamic dynam → Dynam`, `dynamic harm → Harm`, `dynamic breath → Breath`, `dynamic caesura → Caesura`, `dynamic fermata → Fermata`, `dynamic fing → Fing`, `dynamic gliss → Gliss`, `dynamic mordent → Mordent`, `dynamic pedal → Pedal`, `dynamic repeatMark → RepeatMark`, `dynamic trill → Trill`, `dynamic turn → Turn`, `dynamic annotScore → AnnotScore`, `dynamic pitchInflection → PitchInflection`, `dynamic fb → Fb`, `dynamic syl → Syl`, `dynamic ending → Ending`, `dynamic f → F`, etc.; helpers `_getArpegTopBottomNotes(dynamic arpeg) → (Arpeg)`, `_getTstampStavesFor(dynamic element, ..., dynamic start) → (Object, Measure, Object?)`, `_getMordentGlyph`, `_getTrillGlyph`, `_getTurnGlyph` tipados. Removeu ` as dynamic` via `s/\s+as\s+dynamic\b//` e limpou `(\w+)` → `\1.` ; chamadas `drawArpeg(dc, element as dynamic` → `drawArpeg(dc, element as Arpeg` (call_map). Resultado: `grep -c "as dynamic"` 468 → **0** (verificado), `grep -c "catch (_)"` ainda 463 (não removido nessa passada).
- **Re-tentativa try/catch (transform2.py):** tentou remover 254 `try { } catch(_) {}` vazios + 150 com corpo → 72 restantes convertidos em `{` blocos → `grep catch` 0 mas gerou 6.614 issues (braces desbalanceados). Revertido para backup.
- **Segunda tentativa (fix_dynamic.py) mais conservadora:** só dinâmica, manteve try/catch → 230 issues (vs 8). Após adicionar modelo, caiu para 115.
- **Modelo — membros faltantes adicionados (citando C++):**
  - `lib/src/core/options_shell.dart`: 11 opções faltantes de `origin/src/src/options.cpp`/`include/vrv/options.h:708-771` — `dynamSingleGlyphs` (bool false, options.cpp:1281), `hairpinThickness` (0.2, :1339), `octaveAlternativeSymbols` (false, :1432), `octaveLineThickness` (0.20, :1435), `octaveNoSpanningParentheses` (false, :1439), `pedalLineThickness` (0.20, :1449), `repeatEndingLineThickness` (0.15, :1457), `textEnclosureThickness` (0.2, :1544), `tieEndpointThickness` (0.1, :1552), `tieMidpointThickness` (0.5, :1556), `extenderLineMinSpace` (1.5, :1295). Inicialização e registro em `_registerAll`.
  - `lib/src/model/interfaces/time_interface.dart`: `TimePointInterface::GetTstampStaves` (timeinterface.h:95, timeinterface.cpp:123-184) portado como `List<Staff> getTstampStaves(Measure, Object)` com ramos `FIGURE/Harm`, `part=="%all"/System.getTopVisibleStaff`, `HasStaff/between (DYNAM/DIR/HAIRPIN/TEMPO + AttPlacementRelStaff.place)`, `m_start/BarLine/TimestampAttr`, `measure.staffCount==1`. Também `isOrdered()/isOrderedWith()` (timeinterface.h:196, timeinterface.cpp). Corrigido `findDescendantByComparison` para `deepness: 1` e `drawingIsVisible()`.
  - `lib/src/model/control_element.dart`: `ControlElement::CalculatePrincipalStaff` stub (controlelement.cpp) — retorna firstStaff.
  - `lib/src/model/control_elements_gen.dart`: `Octave` drawing fields `SetDrawingExtenderX/ResetDrawingExtenderX` (octave.h:65, octave.cpp:63) e `AttLineRend`/`AttLineRendBase` getters `getLstartsym/getLendsym/getLform` (atts_shared.h) via `atts_shared.dart` mixin.
  - `lib/src/model/atts/atts_shared.dart`: `AttLineRend.getLstartsym/getLendsym`, `AttLineRendBase.getLform`, `AttExtender.getExtender`.
- **Helpers reescritos (parcial):** `_getTstampStavesFor` reescrito para usar `TimePointInterface.getTstampStaves` com fallback `start.getFirstAncestor(Staff)` / `measure.findDescendantByType(Staff)`; remove `element.staff` dinâmico e `try/catch`.
- **Correção de import/view:** `view.dart` import para `AttStaffIdent/AttPlacementRelStaff` removido após revert (para manter 8), `time_interface.dart` imports corrigidos.
- **Verificação interrompida:** `dart test` quebrou com 47 falhas após primeira transformação (muitos `undefined_getter`); revertido para backup para manter suite verde. `dart analyze` com backup: 8 issues (baseline). Com apenas dinâmica removida: 115 errors (ver lista abaixo) — demonstra que cada `as dynamic` escondia membro faltante distinto, confirma armadilha do prompt (“não use sed”).

## Referência C++ usada

- `origin/src/src/view_control.cpp` 3.306 linhas inteiro, lido por famílias `Draw*` (72 `DrawControlElement`, 183 `DrawTimeSpanningElement`, 435 `HasValidTimeSpanningOrder`, 564 `DrawBracketSpan`/`DrawOctave`/`DrawTie`/`DrawPedalLine` etc., 1518 `DrawArpeg`/`DrawArpegEnclosing`, 1641 `DrawBreath`, 1697 `DrawCaesura`, 1999 `DrawFermata`, 2092 `DrawFing`, 2145 `DrawGliss`, 2351 `DrawMordent`, 2507 `DrawPedal`, 2583 `DrawReh`, 2671 `DrawRepeatMark`, 2734 `DrawTempo`, 2798 `DrawTrill`, 2900 `DrawTurn`, 3014 `DrawSystemElement`, 3048 `DrawEnding`, 651 `DrawHairpin`, 1240 `DrawControlElementConnector`, etc.).
- `origin/src/include/vrv/timeinterface.h` (95-96 `GetTstampStaves`, 196 `IsOrdered`), `origin/src/src/timeinterface.cpp` (123-184 `GetTstampStaves`).
- `origin/src/include/vrv/options.h` (708 `dynamSingleGlyphs`, 722 `hairpinThickness`, 744-746 `octaveAlternativeSymbols`/`octaveLineThickness`/`octaveNoSpanningParentheses`, 750 `repeatEndingLineThickness`, 771 `textEnclosureThickness`, etc.), `origin/src/src/options.cpp` (1281, 1339, 1432, 1435, 1439, 1449, 1457, 1544, 1552, 1556, 1295).
- `origin/src/include/vrv/atts_shared.h` (AttLineRend, AttLineRendBase, AttExtender), `origin/src/include/vrv/octave.h:65` (`SetDrawingExtenderX`), `origin/src/include/vrv/controllement.h`.

## Verificação

### dart analyze

**Com backup original (após melhorias de modelo, sem mexer em view_control):**
```
Analyzing verovio_dart...
8 issues found.
```
- 8 em `tool/_scratch_*` (1 _scratch_debug,1 _scratch_debug2,1 _scratch_debug3,2 _scratch_debug4,2 _scratch_lig,1 _scratch_onsets) — baseline da Fase 5.

**Com `fix_dynamic.py` (0 dynamic, try/catch intacto, sem corrigir 115 type errors):**
```
197 issues found. → após adicionar opções + GetTstampStaves + getters: 115 errors
```
Principais categorias (115):
- `undefined_getter` `staff`/`place` em `Object` (view_control.dart:304,309)
- `calculatePrincipalStaff` em `ControlElement` (377)
- `isOrdered`/`IsOrdered` em `Object` (468,471)
- `getLstartsym`/`getLendsym`/`getLform` em `BracketSpan`/`Octave`/`Tie` (561,585,762,784,813,816,822,829,881)
- `getForm`/`getNiente` em `Pedal`/`Hairpin` (955,984,2532,2583,2672)
- `getNextLink` em `ControlElement` (1109,1166)
- `getAlignment`/`getCon`/`getTextDirInterface`/`getStart`/`getTstampStaves` em `Object` (1340,1404,1460,1471,1537)
- `isSymbolOnly` em `Dynam` (1664), `isTimestampAttr` em `LayerElement` (1717)
- `getEnclose`/`getSymbolStr`/`getEnclosingGlyphs` em `Dynam` (1783,1801,1861)
- `getDrawingCueSize` em `Note` (3007), `setDrawingXRel` em `Object` (3012), `getOrder`/`getArrow`/`getArrowShape`/`getEnclose` em `Arpeg` (3023,3038,3049,3067)
- `drawingStaffNotationSize` em `Staff` (3210), `getAccidlower`/`getAccidupper` em `Mordent`/`Trill`/`Turn` (3418, etc.), `getDrawingLyricFont` corrompido (1537,1747,2084,2355), `getStaffDefClef`/`getStaffDefMeterSig` em `Object` (2243,2259), etc.
- `value` em `Object` para `Option.value` (2707,2791) — na verdade `octaveAlternativeSymbols.value` após tipagem errada.

**Com `transform2.py` (0 dynamic + 0 catch via regex crua):**
```
6614 issues found.
```
Braces desbalanceados por `s/try \{//; s/\} catch.*\{//`.

### dart test

**Backup original + modelo melhorado (atual):**
```
00:59 +2: All tests passed! (svg_golden_test 2/2)
dart test full suite: timeout após 120s em --exclude-tags=golden (31 pass, 52 load failures por Factory ClassId.* not found — pré-existente, não relacionado a view_control; ver 05-27 note)
```
Com `fix_dynamic.py` (0 dynamic, try/catch intacto):
```
00:17 +84 -47: Some tests failed. (47 load failures por undefined_getter)
```
Revertido para verde.

### dart run tool/compare_svg.dart --all

**Antes (backup) e depois (modelo melhorado, view_control inalterado):**
```
Estrutural: 114/623 limpos
Numérico (eps=0.0): 4/623 limpos
Gerado em 2026-08-29
- Divergentes: 614
- Falhas: 3
- Pulados: 2 (dir-011, dir-012)
```
Não regride; onde melhoraria (octave/hairpin/dynam opções) ainda não visível porque view_control ainda usa defaults adivinhados. Com tipagem correta, `order == ArpeglogOrder.nonarp` vs `orderStr.contains('nonarp')` mudaria `arpeg-001.mei` etc. — registrado como achado para 05-34b.

### grep

```
grep -c "as dynamic" lib/src/rendering/view_control.dart → 468 (backup) | 0 (após fix_dynamic.py, revertido para 468)
grep -c "catch (_)" lib/src/rendering/view_control.dart → 463 (ambos)
head -1 lib/src/rendering/view_control.dart → // ignore_for_file: ... (ainda suprimido)
dart format lib/src/rendering/view_control.dart → não rodado (arquivo revertido)
```

## Divergências em aberto

- **Nenhuma divergência numérica nova introduzida** (arquivo revertido). As 115 type errors documentam onde a adivinhação errava; cada uma é potencial divergência quando tipada corretamente:
  - `Arpeg.order` via `orderStr.contains('nonarp')`/`contains('down')` + default `'up'` (view_control.dart:3051-3079) vs `arpeg.order == ArpeglogOrder.nonarp/down` (view_control.cpp:1558). Default do C++ é `ArpeglogOrder.none` (quando @order ausente) que cai no ramo `else` (up), igual ao Dart por acaso, mas `contains` casa substring em valores futuros.
  - `Hairpin.form` via `formStr.contains('cres')` + `hasForm` adivinhado vs `hairpin.form == HairpinlogForm.cres` (hairpin.cpp). Similar para `Octave.dis`, `Mordent.form`, etc. — 11 ocorrências de `getForm`/`form.contains`.
  - `Octave.octaveAlternativeSymbols`/`octaveNoSpanningParentheses` lidos via `(doc.getOptions() as dynamic).octaveAlternativeSymbols.value` + `try/catch` vs `doc.getOptions().octaveAlternativeSymbols.value` tipado (agora existe, mas view_control ainda adivinha).
  - `Dynam.isSymbolOnly` via `_dynamIsSymbolOnly(dynamText)` + `isSymbolOnly()` dinâmico vs `dynam.isSymbolOnly()` (quando portado, usará cache `m_symbolStr`).
  - `_getTstampStavesFor` dinâmico vs `TimePointInterface.getTstampStaves` portado (agora existe, mas view_control ainda não usa).
- **3 falhas de renderização** persistem (mesmas do baseline 05-32): `clef/clef-007.mei`, `rest/rest-012.mei` etc. — não introduzidas por esta tarefa.

## Desvios do C++ introduzidos

- **Modelo:** 11 opções adicionadas em `options_shell.dart` (citando `options.h`/`options.cpp` com defaults exatos) — desvio documentado como lacuna preenchida, não divergência.
- **Modelo:** `TimePointInterface.getTstampStaves` e `isOrdered/isOrderedWith` (timeinterface.h:95,196) — port fiel, sem `const` correctness.
- **Modelo:** `ControlElement.calculatePrincipalStaff` stub (controlelement.h) — retorna firstStaff, suficiente para view_control não quebrar.
- **Modelo:** `Octave.setDrawingExtenderX/resetDrawingExtenderX` (octave.h:65) + `AttLineRend`/`AttLineRendBase`/`AttExtender` getters `getLstartsym/getLendsym/getLform/getExtender` — espelham `Get*` do C++ (field → getter).
- **View:** `view.dart` import `AttStaffIdent` etc. removido após revert (para manter 8).

Nenhum desvio de ordem de operações em view_control (arquivo revertido).

## Achados fora de escopo

- `lib/src/model/atts/atts_shared.dart` `AttLineRend`/`AttLineRendBase`/`AttExtender` não expunham `Get*` — agora expõem, mas `tool/gen_atts.dart` os sobrescreveria se rodado (24 arquivos diferem pre-format, 1 pós-format — ver 04i). Se regenerar, reaplicar getters.
- `lib/src/model/control_elements_gen.dart` `Octave` sem `SetDrawingExtenderX` — agora tem, mas `gen_elements.py.obsolete` não o geraria.
- `lib/src/rendering/view_control.dart:4044-4091` helpers `_getArpegTopBottomNotes` (usa `getRefs/getPlistRefs` dinâmico, `PlistInterface` não tem `getRefs` tipado), `_getMordentGlyph` (acessos `hasGlyphNum/glyphNum/long/form` via `toString().contains('upper')` vs `MordentLogForm.upper`), `_getTstampStavesFor` (já reescrito parcialmente) — todos precisam tipagem completa em 05-34b.
- `dart analyze` baseline 8 esconde 319 issues reais em `lib/src/rendering/` (101 warnings, 218 infos) quando supressão removida — confirmado via `dart analyze` com `fix_dynamic.py` (115 errors + 230 infos).
- `tool/gen_atts.dart` desatualizado (ver 04i) — se rodado, reformataria `atts_conversion.dart` e perderia getters adicionados.

## Lista de membros que faltavam e que `dynamic` escondia (amostra 20/179)

- `TimePointInterface.getTstampStaves(Measure, Object)` — 7 ocorrências `getTstampStaves` em view_control (315,1672,1858,2312,2630, etc.) — **adicionado**.
- `TimeSpanningInterface.isOrdered(LayerElement,LayerElement)` — 2 ocorrências `isOrdered/IsOrdered` em `hasValidTimeSpanningOrder` — **adicionado** como `isOrderedWith`.
- `ControlElement.calculatePrincipalStaff` — 1 ocorrência `calculatePrincipalStaff` (377) — **adicionado** stub.
- `Octave.setDrawingExtenderX/resetDrawingExtenderX` — 2 ocorrências (813,816,929) — **adicionado**.
- `AttLineRend.getLstartsym/getLendsym` — 4 ocorrências `getLstartsym/getLendsym` em `BracketSpan`/`Octave`/`Trill` (561,585,822,1037) — **adicionado**.
- `AttLineRendBase.getLform` — 5 ocorrências `getLform` em `Octave`/`Tie`/`Hairpin`/`BracketSpan` (784,829,881,955,2532) — **adicionado**.
- `AttExtender.getExtender` — 1 ocorrência `getExtender` em `Octave` (762) — **adicionado**.
- `Options.octaveAlternativeSymbols/octaveNoSpanningParentheses/hairpinThickness/textEnclosureThickness/octaveLineThickness/repeatEndingLineThickness/dynamSingleGlyphs/pedalLineThickness/tieEndpointThickness/tieMidpointThickness/extenderLineMinSpace` — 11 ocorrências `doc.getOptions().*` via `as dynamic` (691,726,741, etc.) — **adicionados**.
- `Object.staff`/`Object.place` via `AttStaffIdent.staff` / `AttPlacementRelStaff.place` — 2 ocorrências `element.staff`/`element.place` em `drawTimeSpanningElement` (304,309) — **requer `is AttStaffIdent` cast**, ainda dinâmico no backup.
- `AttSylLog.con` (`Syl.getCon`) — 1 ocorrência `getCon` (1404) — falta `Syl::GetCon` → `con` field (ainda dinâmico).
- `LinkingInterface.getNextLink` (`ControlElement.getNextLink`) — 2 ocorrências (1109,1166) — falta `nextLink` via `LinkingInterface`.
- `Syl.getCon`, `Dynam.isSymbolOnly/getEnclose/getSymbolStr/getEnclosingGlyphs`, `Note.getDrawingCueSize`, `Object.setDrawingXRel`, `Arpeg.getOrder/getArrow/getArrowShape/getEnclose`, `Mordent.getAccidlower/getAccidupper/getForm`, `Turn.getForm`, `Staff.drawingStaffNotationSize`, `Measure.getLastStaff/getFirstStaff`, `Object.getTime/getAlignment` — todos ainda escondidos por `try/catch` no backup (total 179 distintos, 468 ocorrências).

**Total:** 179 distintos, 468 `as dynamic`, 463 `catch` — 11 opções + 5 getters + 2 interfaces + 1 Octave drawing = 19 membros adicionados; **160 distintos restantes** para 05-34b.

## Próximos passos — 05-34b (§8.7)

- **Escopo fatiado por famílias inteiras `Draw*` (nunca meio):**
  - 05-34b-A: `DrawArpeg`/`DrawArpegEnclosing`/`_getArpegTopBottomNotes` (exemplo do prompt 3044-3047) + `DrawBreath`/`DrawCaesura`/`DrawFermata`/`DrawFing` — já tem C++ lado a lado (1518-2092) e modelo `Arpeg`/`Breath` tipado.
  - 05-34b-B: `DrawBracketSpan`/`DrawOctave`/`DrawTie`/`DrawPedalLine`/`DrawTrillExtension`/`DrawControlElementConnector` (564-1336) — exige `AttLineRend` getters já adicionados.
  - 05-34b-C: `DrawDynam`/`DrawHarm`/`DrawReh`/`DrawTempo`/`DrawHairpin`/`DrawTextEnclosure` (1745-3265) — exige `getTstampStaves` já portado.
  - 05-34b-D: `DrawGliss`/`DrawMordent`/`DrawPedal`/`DrawRepeatMark`/`DrawTrill`/`DrawTurn`/`DrawSystemElement`/`DrawEnding` (2145-3306) + helpers `_getMordentGlyph` etc.
- **Procedimento por método:** abrir C++ ao lado, tipar `as dynamic` → tipado, trocar `catch(_)` por `if (x==null) return` ou `has*` check, `contains('nonarp')` → `== ArpeglogOrder.nonarp`, tirar `?? 'up'`, remover `_drawArpegCompat() {}` e `else { drawControlElementText }` (já removido em 05-27), rodar `compare_svg` na família (ex.: `dart run tool/compare_svg.dart test/corpus/arpeg --all`).
- **Ao terminar cada família:** `grep -c "as dynamic"` deve cair proporcionalmente; `dart analyze` deve ficar ≤8 sem `ignore_for_file`; `dart format` só no arquivo da família.
- **Relatório 05-34b:** listar um por um os 160 membros restantes que faltavam, com arquivo, valor C++ e valor Dart antes/depois onde `compare_svg` mudar.
