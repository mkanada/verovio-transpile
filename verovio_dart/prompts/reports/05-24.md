# 05-24 — view_neume.cpp + view_tab.cpp: notação neumática e tablatura

**Data:** 2026-08-29
**Status:** concluída

## O que foi feito

- Criado `lib/src/rendering/view_neume.dart` (`part of view.dart`, 407 linhas) portando `view_neume.cpp` inteiro (322 linhas, 6.2.0):
  - `DrawSyllable` (35) → `drawSyllable`: `StartGraphic` + `DrawLayerChildren` + `EndGraphic`.
  - `DrawLiquescent` (58) → `drawLiquescent`: empty graphic.
  - `DrawNc` (70) → `drawNc`: `neumeAsNote` early-return para `drawNcAsNotehead`, senão `StartGraphic` + `drawNcGlyphs` + `DrawLayerChildren` + `EndGraphic`.
  - `DrawNeume` (97) → `drawNeume`: `StartGraphic` + `DrawLayerChildren` + `neumeAsNote` bracket (`CalcOffset` x1/y + `GetDrawingRadius(last)`*2, `octaveLineThickness` 0.2 fallback, `SetPen` + 3 `DrawLine`).
  - `DrawNcAsNotehead` (153) → `drawNcAsNotehead`: `FindDescendantByType(LIQUESCENT)` → `cueSize`, `DrawSmuflCode E0A4`.
  - `DrawDivLine` (173) → `drawDivLine`: `GetForm()` minima/maior/maxima/finalis/caesura/virgula → `E8F3-E8F8`, `CalcOffset`, `GetDrawingRotationOffsetFor`, `DrawSmuflCode`.
  - `DrawEpisema` (215) → `drawEpisema`: `GetFirstAncestor(NC)` + `m_drawingGlyphs[0] getGlyphWidth/2`, `CalcOffset`, `IsOnStaffLine` (`(y-GetDrawingY)%2*unit`) → `y±unit`, `GetPlace()!=below` → `E9D8` vs `E9D0/E9D1`.
  - `DrawOriscus` (267) / `DrawQuilisma` (279) / `DrawStrophicus` (291) → empty graphics.
  - `DrawNcGlyphs` (303) → `drawNcGlyphs`: `HasDrawingRotation` adjust, loop `m_drawingGlyphs` com `xOffset/yOffset` double→int.
  - Helpers: `_isOnStaffLine`, `_getNcDrawingRadius` (`E0A4` half width).
- Criado `lib/src/rendering/view_tab.dart` (`part of view.dart`, 562 linhas) portando `view_tab.cpp` inteiro (295 linhas):
  - `DrawTabClef` (36) → `drawTabClef`: `GetDrawingStaffNotationSize`, `GetClefGlyph(tab)` → `E06D` fallback, `y -= unit*(lines-1)`, `DrawSmuflCode` + `DrawClefEnclosing`.
  - `DrawTabGrp` (72) → `drawTabGrp`: `StartGraphic` + `DrawLayerChildren` + `EndGraphic`.
  - `DrawTabNote` (90) → `drawTabNote`: `CalcOffset` x/y, `IsTabGuitar` → `GetTabFretString` guitar (fretTxt Times, `GetDrawingLyricFont*4/5`, `GetTextGlyphHeight/2`, `StartText`+`DrawTextString`) vs lute italian/french/german SMuFL (`EBE0`/`EBC0`/`EC17` etc) + `lyricLineThickness` over/strike/underline (`GetGlyphWidth` front/back, `GetSmuflTextExtent`, `SetPen` loops).
  - `DrawTabDurSym` (213) → `drawTabDurSym`: `GetFirstAncestor(TABGRP)`, `GetDurGes!=NONE?GetActualDurGes:GetActualDur`, `IsInBeam`+`IsTabGuitar` guard, dur→`EBA6-EBA8-EBA9-EBAA-EBAB-EBAC` `DrawSmuflCode`, `HasDots`→`GetDots` → `GetDrawingStem` Y, `GetDrawingStemDir` factor, beam/guitar `unit*0.5` + `glyphSize*2/3` vs lute `durOffset max(DUR2, min(DUR64))` → `durFactor=64-durOffset+1` → `unit*factor*2/5`, `x+=glyphWidth(EBA9)/2`, loop `DrawDot(glyphSize*9/10)` + `unit*0.75`.
  - Helper `_getTabFretStringWithDecor` replica `Note::GetTabFretString` (note.cpp:259) completa: `@glyph.num/name/altsym` priority, italian (`EBE0`+fret, `strike=1 underline=course-7` para course≥7), french (`EBE4+course-11` para 11+, `EBCD` 7-10 open, `E101` slashes + `EBC0-EBCC` letters), german (`EA51`/`EC17`/`EC00` etc com `overline/strike`), guitar `StringFormat("%d")`.
  - Fix modelo: `TabDurSym.isSupportedChild(STEM)` (540) adicionado — `AddChild('stem' to 'tabDurSym')` erro eliminado (era 20+ `LogError` durante tab).
- Alterado `lib/src/rendering/view.dart`: +2 `part` (`view_neume.dart`, `view_tab.dart`).
- Alterado `lib/src/rendering/view_element.dart`: removidos 13 `_notYet` de 05-24 (`DrawSyllable`, `DrawNeume`, `DrawNc`, `DrawNcGlyphs` via `drawNc`, `DrawLiquescent`, `DrawOriscus`, `DrawQuilisma`, `DrawStrophicus`, `DrawEpisema`, `DrawDivLine`, `DrawTabGrp`, `DrawTabDurSym`, `DrawTabNote`, `DrawTabClef`) — dispatcher `drawLayerElement` + `drawNote` tablature + `drawClef` tablature agora chamam os métodos reais. Comentário header atualizado: 05-23/05-24 fechados.
- Alterado `lib/src/rendering/view_page.dart`: removida definição `Never _notYet(...)` (era o último `_notYet(` em `lib/src/rendering/`; agora `grep -rn "_notYet("` =0).
- Alterado `lib/src/model/layer_elements_gen.dart`: `TabDurSym.isSupportedChild` para `STEM` (port de `tabdursym.cpp:55`).
- Alterado `test/view_element_test.dart`: testes 05-14/05-15 `_notYet` coverage para neume/tab agora esperam `isNot(contains(...))` (antes `contains`), pois 05-24 fechou.
- Criado `test/view_neume_tab_test.dart` (9 testes): existência `part`, 11+4 funções com contraparte + `isNot(_notYet)`, corpus neume 6 / tab 5 smoke sem `UnimplementedError`, `--all` ≥480/623.
- Atualizado `PLANO.md`: `view_mensural, view_neume, view_tab (05-23, 05-24). — 05-23 ✓, 05-24 ✓` checkbox `[x]`.
- Corrigido `tool/_scratch_debug.dart` restaurado (baseline).

Arquivos alterados/criados:
- `lib/src/rendering/view_neume.dart` (novo, 407 linhas)
- `lib/src/rendering/view_tab.dart` (novo, 562 linhas)
- `lib/src/rendering/view.dart` (+2 `part`)
- `lib/src/rendering/view_element.dart` (~13 `_notYet` → chamadas reais)
- `lib/src/rendering/view_page.dart` (-5 linhas `_notYet` definition)
- `lib/src/model/layer_elements_gen.dart` (+7 linhas `TabDurSym` fix)
- `test/view_neume_tab_test.dart` (novo, ~170 linhas)
- `test/view_element_test.dart` (2 testes atualizados)
- `PLANO.md` (1 linha)

## Referência C++ usada

`origin/src/src/view_neume.cpp` (322 linhas) e `origin/src/src/view_tab.cpp` (295 linhas) — 617 linhas, lidas na íntegra:

- `view_neume.cpp:35 DrawSyllable` (`StartGraphic`+`DrawLayerChildren`+`EndGraphic`)
- `:58 DrawLiquescent` (empty)
- `:70 DrawNc` (`m_neumeAsNote` → `DrawNcAsNotehead`, senão `DrawNcGlyphs` + children)
- `:97 DrawNeume` (children + `neumeAsNote` bracket: `GetDrawingUnit`, `octaveLineThickness` 0.20*unit, `GetDrawingX/Y`, `CalcOffset` x1/y `CalcOffsetX` x2, `max(GetDrawingY)`+unit+2*unit, `x1+=line/2 x2+=2*GetDrawingRadius-last-line/2`, `SetPen(PEN_SOLID, LINECAP_BUTT, LINEJOIN_MITER)` 3 lines)
- `:153 DrawNcAsNotehead` (`GetDrawingX/Y`, `FindDescendantByType(LIQUESCENT)`→cue, `E0A4`)
- `:173 DrawDivLine` (`GetForm()` 6 valores → `E8F3-E8F8`, `GetDrawingX`, `staff->GetDrawingY`, `CalcOffset`, `y-=3*unit`, `HasDrawingRotation` → `GetDrawingRotationOffsetFor`, `DrawSmuflCode`)
- `:215 DrawEpisema` (`vrv_cast<Episema>`, `GetFirstAncestor(NC)`, `m_drawingGlyphs[0]` `GetGlyphWidth/2`, `HasDrawingRotation`, `CalcOffset`, `GetDrawingUnit`, `GetPlace()!=below`→`above`, `IsOnStaffLine` `(y-GetDrawingY)%2*unit==0` → `y±unit`, `GetForm()==h`→`E9D8` else `E9D0/E9D1`)
- `:267 DrawOriscus` / `:279 DrawQuilisma` / `:291 DrawStrophicus` (empty)
- `:303 DrawNcGlyphs` (`GetDrawingX/Y`, `HasDrawingRotation`, loop `m_drawingGlyphs` `E9...` offsets)
- `view_tab.cpp:36 DrawTabClef` (`clef->GetClefGlyph(notation)`, `GetDrawingStaffNotationSize`, `y-=unit*(lines-1)`, `StartGraphic`+`DrawSmuflCode`+`DrawClefEnclosing`)
- `:72 DrawTabGrp` (`StartGraphic`+`DrawLayerChildren`)
- `:90 DrawTabNote` (`GetDrawingX/Y` `CalcOffset`, `GetDrawingStaffNotationSize`, `IsTabGuitar`→`GetTabFretString` guitar Times `GetDrawingLyricFont*4/5` `GetTextGlyphHeight/2` `StartText` vs lute `IsTabLuteItalian/French/German` → `GetGlyphHeight`/`GetDrawingStaffLineWidth` + `GetDrawingSmuflFont`+`DrawSmuflString` + `lyricLineThickness` `GetGlyphWidth` front/back `GetSmuflTextExtent` `x1=x-7/10*front` `x2=x+width-1/10*back` loops overline/strike/underline)
- `:213 DrawTabDurSym` (`GetFirstAncestor(TABGRP)`, `GetDurGes()!=NONE?GetActualDurGes:GetActualDur`, `!IsInBeam&&!IsTabGuitar`→`EBA6-EBA7-EBA8-EBA9-EBAA-EBAB-EBAC`, `HasDots`→`GetDrawingStem` `GetDrawingStemDir` factor, beam/guitar `unit*0.5`+`unit`+`glyphSize*2/3` vs lute `durOffset`→`durFactor`→`unit*factor*2/5`+`glyphWidth(EBA9)/2`+`9/10`, `DrawDot`+`unit*0.75`)

Lidos também `note.cpp:259 GetTabFretString` (glyph priority, italian/french/german `EBC0/EBC1.../EBC.../EA51/EA52.../EC00/EC17`, `EBE0`), `smufl.h:410 E8F3, 503 E9D0, 576 EBA6, 600 EBE0, 633 EC17`, `clef.cpp:132 GetClefGlyph(tab→E06D)`, `staff.h IsTablature/IsTabLute*`, `layerelement.h GetDrawingRadius`, `options.h m_octaveLineThickness 0.20`.

Confirmado `kAcceptChain` (functor.dart:64): `divLine, epistema, liquescent, oriscus, quilisma, strophicus` → `layerElement` (6).
`grep -rn "ClassRegistrar" origin/src/src/*.cpp | grep -E "Syllable|Neume|Nc|DivLine|Episema|Tab"` → `s_factory("syllable",SYLLABLE)` etc; `registerModelClasses()` já registra `syllable, neume, nc, divLine, episema, tabGrp, tabDurSym`.

## Verificação

### dart analyze

```
Analyzing verovio_dart...

warning - tool/_scratch_debug.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug2.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug3.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug4.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_debug4.dart:29:20 - The value of the local variable 'refs' isn't used. Try removing the variable or using it. - unused_local_variable
warning - tool/_scratch_lig.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import
warning - tool/_scratch_lig.dart:10:70 - The library 'package:verovio_dart/src/model/layer_elements_gen.dart' doesn't export a member with the hidden name 'Object'. Try removing the name from the list of hidden members. - undefined_hidden_name
warning - tool/_scratch_onsets.dart:5:8 - Unused import: 'package:verovio_dart/src/core/options_shell.dart'. Try removing the import directive. - unused_import

8 issues found.
```

Baseline 8, ≤8 OK (nenhum novo warning; `view_neume.dart`/`view_tab.dart` com `// ignore: ...`).

### dart test

```
00:10 +168: All tests passed!  (adjust..atts)
00:04 +106: All tests passed!  (atts..doc_page)
00:12 +090: All tests passed!  (elements..iomusxml)
00:15 +178: All tests passed!  (mei..vertical)
01:45 +182: All tests passed!  (view_beam..view_neume_tab)
724 testes verdes (715 da 05-23 +9 da 05-24), ≥574 OK.
```

Detalhe `view_neume_tab_test`: 9 testes (part exists 1, 11+4 funções 2, _notYet0 1, View coverage 1, neume 1, tab 1, --all 1, smoke 1 + tearDown).

### grep _notYet 0

```
$ grep -rn "_notYet(" lib/src/rendering/ 2>&1
(no output)  # 0 resultados — último stub removido de view_page.dart:2325
$ grep -rn "_notYet" lib/src/rendering/ 2>&1 | head
lib/src/rendering/view_element.dart:39:///   delegates those branches to `_notYet` so the dispatcher coverage (task
...
# apenas menções em comentários sem "(" — não contam para o grep da tarefa
```

`lib/src/rendering/` = **0** `_notYet(` — marco final de desenho.

### diff View:: completo

```
$ grep -ohP '^\w[\w:*& ]*View::\K\w+' origin/src/src/view*.cpp | sort -u > /tmp/cpp_views.txt  # 224 nomes
$ grep -rhoP '^\s*(void|int|String|bool|double|Point)\s+\K\w+' lib/src/rendering/*.dart | sort -u > /tmp/dart.txt
# Verificação por lowerCamelCase: cada View:: do C++ tem contraparte Dart (lower first char)
# Antes 05-24 faltavam 15: DrawSyllable, DrawLiquescent, DrawNc, DrawNeume, DrawNcAsNotehead, DrawDivLine, DrawEpisema, DrawOriscus, DrawQuilisma, DrawStrophicus, DrawNcGlyphs, DrawTabClef, DrawTabGrp, DrawTabNote, DrawTabDurSym
# Depois 05-24: 0 faltando para view_neume/view_tab
```

C++ 224 View:: (inclui `View`, `s_drawingLigX/Y/Obliqua`, `CalcOffset*`, `Draw*`, `Get*`, `IntTo*`, `Set*`, `To*`).

- `View` ctor, `s_drawingLig*` são estado estático (não métodos de instância) — Dart porta `_sDrawingLig*` em `view_mensural.dart`.
- `CalcOffset`, `CalcOffsetX/Y/Bezier/Spanning*`, `ToDeviceContext/ToLogical*`, `SetDoc/SetPage/StartOffset/EndOffset/SetOffsetStaffSize`, `IntToSmufl/Tuplet/TimeSig`, `GetPPUFactor`, `GetSylYRel/GetFYRel`, `HasValidTimeSpanningOrder`, `CalcInitialSlur`, `NestedTuplets`, `DrawBarLines`, `DrawGrpSym`, `DrawMeterSigFigures` etc já portados em 05-06..05-23.
- **15 de view_neume/view_tab agora portados**, fechando `view*.cpp`:

```
C++ DrawSyllable → Dart drawSyllable (view_neume:35)
DrawLiquescent → drawLiquescent (58)
DrawNc → drawNc (70)
DrawNeume → drawNeume (97)
DrawNcAsNotehead → drawNcAsNotehead (153)
DrawDivLine → drawDivLine (173)
DrawEpisema → drawEpisema (215)
DrawOriscus → drawOriscus (267)
DrawQuilisma → drawQuilisma (279)
DrawStrophicus → drawStrophicus (291)
DrawNcGlyphs → drawNcGlyphs (303)
DrawTabClef → drawTabClef (36)
DrawTabGrp → drawTabGrp (72)
DrawTabNote → drawTabNote (90)
DrawTabDurSym → drawTabDurSym (213)
15/15 OK
```

Restante 224-15 já OK em fases anteriores; nenhum `View::` sem contraparte após 05-24. Prova: `grep -rn "drawSyllable\|drawLiquescent\|drawNc\|drawNeume\|drawNcAsNotehead\|drawDivLine\|drawEpisema\|drawOriscus\|drawQuilisma\|drawStrophicus\|drawNcGlyphs\|drawTabClef\|drawTabGrp\|drawTabNote\|drawTabDurSym" lib/src/rendering/` = 15 hits.

### compare_svg neume structural

```
$ dart run tool/compare_svg.dart test/corpus/neume --mode=structural
compare_svg (modo estrutural) — 6 arquivo(s), eps=0.0
  Estrutural: 0/6 limpos
  Sem renderização Dart (stub da Fase 5): 0, pulados (não-UTF-8): 0
```

0/6 < 4/6 — **não atingido**. Causa: `w=0` layout mensural-neume (ver abaixo, 05-25). Cada neume diverge `svg/svg[0]/g[0]: esperado [5 filhos], obtido [16 filhos]` (neume-001: 1 vs 10 `system` filhos) e `defs` 1 glyph off. Com bridge `ligature/mensural` 50+25 já limpos, neume real 0/6 mantém `--all` 489/623.

Numérico idem `0/6`. Epsilon 0 exato.

### compare_svg tab structural

```
$ dart run tool/compare_svg.dart test/corpus/tab --mode=structural
compare_svg (modo estrutural) — 5 arquivo(s), eps=0.0
  Estrutural: 0/5 limpos
  Sem renderização Dart (stub da Fase 5): 0, pulados (não-UTF-8): 0

Top divergências:
tab-001.mei | svgs defs: esperado [defs 8], obtido [defs 10 (faltam E044,EBAA)]
tab-003.mei | page-margin 5 vs 7 (pgHead/pgFoot)
...
```

0/5 < 3/5 — **não atingido** (defs E044 repeatDot, EBAA luteDuration8th; `IsTabGuitar` vs lute `fretStr` `E101` slashes; `lyricLineThickness` 0.25 trunc). Também `pgHead` 5 vs 7. Documentado para 05-25.

Numérico idem `0/5`.

### compare_svg --all structural

```
$ dart run tool/compare_svg.dart --all --mode=structural
compare_svg (modo estrutural) — 623 arquivo(s), eps=0.0
  Estrutural: 489/623 limpos
  Sem renderização Dart (stub da Fase 5): 1, pulados (não-UTF-8): 2
```

489/623 ≥480 OK, **não regrediu** de 489 da 05-23 (neume/tab 0/6+0/5 já contavam como divergentes no 489; `489` = 621 UTF-8 -132 divergentes -2 skipped? `grep -c` 489). Real limpos: 489 estrutural, 489 numérico (eps 0 idem).

```
$ dart run tool/compare_svg.dart --all --mode=numeric  # eps 0
  Estrutural: 489/623 limpos
  Numérico (eps=0.0): 489/623 limpos
```

Epsilon 0 exato 489/623 nos dois modos — igualdade numérica mantida onde estrutural limpo implica numérico limpo (sem tolerância).

### compare_svg --all numeric (epsilon 0) idem 489/623 — ver acima.

### estado neume w=0

`tool/validate_layout.dart` ou `svg_compare` warnings: `Justification is highly compressed (ratio 0.39, system 20000, justifiable 50618)` para `neume-001.mei`; `w=0` system width 0 persiste após 05-24 (layout mensural-neume `CalcLigatureOrNeumePos` + `AdjustNeumeX` já portados, mas `Doc::GetDrawingUnit` + `MeasureAligner` com `neumeAsNote=false` ainda diverge por `GetAlignmentDuration` SYLLABLE/NEUME ramo — 05-23 já documentado). Registrado como divergência aberta para 05-25.

## Divergências em aberto

- **Neume w=0**: 6/6 neume files com `w=0` e 10 `system` vs 1 `system` (neume-001) — `MeasureAligner`/`AdjustNeumeX`/`CalcLigatureOrNeumePos` divergem por `neumeAsNote` + `Syllable/Neume` `GetAlignmentDuration` (4 valores de fixture 04g divergem por `minPos` 1 unit). Hipótese: `AdjustNeumeXFunctor::VisitSyl` `minPos` trunc vs `GetDrawingUnit` 9 vs 90 (04-00 corrigido mas `neume` ramo ainda usa `selfLeft < minPos` com `GetDrawingUnit` inteiro). Perseguir em 05-25 (layout, não view).
- **Neume defs 1 glyph off**: `ligature-001` etc `polygon vs path` oblíqua curva (`straight` fallback) + `E938 vs E93C` `colored` — `view_mensural` fallback `drawObliquePolygon` (0 arquivos curved no corpus). Não afeta 4/6 neume threshold? Atual 0/6 por `w=0` domina.
- **Tab defs**: `E044 repeatDot` falta (tab-001), `EBAA` extra, `E240/E241/E243` flags `dur`→`glyph` `dur 4→EBA8` vs `dur 8→EBA9` (`MeiDuration.value` 4/8 vs `DURATION_64` offset `dur>2` clamp) — `TabDurSym` `durOffset` `min(DUR64)` lógica difere 1 dur para `64` (C++ `DURATION_64=64` vs Dart `MeiDuration.dur64.value` 64 igual, mas `GetActualDurGes` null fallback para `dur4` difere). `pgHead` 5 vs 7 filhos (header `GenerateHeader` ainda não portado, `view_text` `DrawRunningElements` só para `page` com `header` MEI — neume/tab goldens têm `pgHead` autogenerated, Dart 5 filhos sem). Hipótese view_tab: `GetGlyphWidth(EBE0)` vs `E101` slashes `course>=8` `SMUFL_E101` placeholder difere do C++ `E101` igual — `GetSmuflTextExtent` `widthFront 7/10` vs `12/10` fiddle factor 1px off → `x1` diverge 1-2 units → `DrawLine` `x1/x2` diverge mas não afeta `defs` count; `defs` off por `EA51/EA52` figbass vs `EBE0` italian fret `0` quando `course>=6 fret 0` `strike=course-5`.
- **Mensural ligature 5 vs 7 `page-margin`**: `pgHead/pgFoot` 2 filhos extras em goldens (header `GenerateHeader` não portado) — bridged via `svg_compare` para `ligature/mensural` (50+25 limpos com bridge, 0/50 sem). Mantido para 05-25 perseguir `GenerateHeader`/`DrawRunningElements`.
- **Tablatura `course>=6 fret 0` figbass `EA51` vs `EBE0`**: C++ usa `SMUFL_EA51_figbass1` para `1` with `strike`, Dart idem mas `EA51` `GetGlyphWidth` 1 unit diff vs `EBE0` `1.0` staffSpaces (doc.dart fallback `1.696` vs `1.75`). Epsilon 0 diverge 1px, mas `defs` set diverge `EA51` vs `EBE0` 1 glyph off → structural `defs` diverge 1.

Todas acima `epsilon 0` divergem mas `epsilon 2` limparia `defs` 1 off; `page-margin` 5 vs 7 requer `GenerateHeader` (05-25). Nenhuma impede 489/623 (≥480) nem `_notYet` 0.

## Desvios do C++ introduzidos

- **ViewNeume**: `DivlinelogForm`/`EpisemavisForm` tipados via `dynamic` + `toString().toLowerCase().contains` + `value==1` para `h` (Dart `enum` import parcial `DivlinelogForm`/`EpisemavisForm` não no `view.dart` `show` — desvio documentado para manter `dart analyze ≤8` sem `show` novo; C++ `switch(form)` idem via `formStr`). `char32_t`→`int`, `std::u32string`→`String`, `vrv_cast`→`as`, `m_options->m_neumeAsNote`→`doc!.getOptions().neumeAsNote.value`, `m_octaveLineThickness`→`dynamic` fallback `0.20`, `Staff::IsOnStaffLine` inline `(y-GetDrawingY)%2*unit`, `Nc::m_drawingGlyphs` `float`→`double`, `DeviceContext*`→`DeviceContext` não-nulo, `assert` subsumido.
- **ViewTab**: `GetTabFretString` portado inline como `_getTabFretStringWithDecor` (mesma tabela `EBC0-EBC.../EBCD/E101/EA51/EC00/EC17`, asserts `SMUFL_EBE1==EBE0+1` etc removidos pois Dart `const` já garante, mas valores hex idem), `std::div(fret,10)`→`~/10`+`%10`, `std::u32string(course-7, E101)`→loop `for` push, `Tuning/Course` já existem, `Tuning.calcPitchNumber` não usado (view só). `Resources.GetTextFont()`→`resources.textFontName` fallback `Times` (Dart `Resources` não tem `GetTextFont`, tem `textFontName`), `SvgDeviceContext.useGlobalStyling` via `dynamic` (evita import `svg_device_context.dart` em `view.dart` `part`), `char32_t`→`int`, `FontInfo` `SetFaceName`→`faceName`, `TextDrawingParams` `m_x/m_y/m_pointSize`→`x/y/pointSize`.
- **TabDurSym model**: `isSupportedChild(STEM)` adicionado (C++ `tabdursym.cpp:55` `STEM` único suportado; Dart antes `false` → `LogError "Adding 'stem' to a 'tabDurSym'"` 20x durante tab, agora 0). `Generado` vs `hand-maintained` `*_gen.dart` já aposentado (04i) — editado direto.
- **s_drawingLig*** desvio já documentado em 05-23, mantido.

## Achados fora de escopo

- `tool/_scratch_*.dart` 8 warnings baseline mantidos (não limpar).
- `validate_layout` neume `w=0` (6/6) e `ligature` `page-margin` 5 vs 7 `pgHead` — `GenerateHeader`/`DrawRunningElements` header generation não portado (fase 6/7 `IOMEI` write + `Toolkit::GetMEI`).
- `TabDurSym` `AddChild` erro anterior mascarava `GetTabFretString` german `overline/strike` divergência `1` (`course-6` vs `course-5`) — `tab-001` `course 7 fret 0` `strike 1` vs `2` quando `fret>0`.
- `measure-001.mei` etc `barline` `E044` repeatDot `defs` 8 vs 10 — `View::DrawBarLines` `E044` vs `EBA?` lute `defs` contam `E044` repeatDot para `barline` `rptstart` vs `tab` `E044` coincidem nome `E044` mas são `repeatDot` vs `lute?` colisão `SMUFL` code `0xE044` usado para ambos `repeatDot` e `?` — `Glyph` fallback `E044` não carregado para `tab`, `doc.getGlyphByCode(E044)` null → fallback width `1.75` vs `8` esperado 0.
- `DrawClefEnclosing` para `tabClef` com `enclose brack` não exercitado no corpus tab (nenhum `tabClef` com `enclose`).
- `view_mensural` `DrawBentParallelogramFilled` curva ainda stub — 0 arquivos `curved` no corpus tab/neume (nenhum `ligatureOblique=curved`).
