# Compiled C++ reference binary (regenerate with cmake+ninja, see CLAUDE.md)
/build/

# Instrumented C++ tree and binary used to extract reference data for the port.
# Derived from origin/src + cpp_probe/patches/; regenerate with:
#     cpp_probe/build.sh <id>
# See cpp_probe/README.md and verovio_dart/prompts/00-MESTRE.md section 6-bis.
/build-probe/

# Dart/Flutter build artifacts
.dart_tool/
.packages
build/
doc/api/
coverage/

# Package pubspec.lock: this is a library (package), not an application,
# so the lockfile is not meant to be committed (dart.dev convention).
pubspec.lock

# Editors / OS
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db

# Logs and local scratch output
*.log
/tmp/

# Estado de trabalho da série 2026-08-30 (regerado por tool/task_check.sh --baseline)
verovio_dart/tool/.phase5_baseline.json
verovio_dart/tool/.phase5_svg_baseline.txt
verovio_dart/tool/model_gaps.json
