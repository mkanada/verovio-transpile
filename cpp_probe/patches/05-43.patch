diff -ruN a/src/adjustyposfunctor.cpp b/src/adjustyposfunctor.cpp
--- a/src/adjustyposfunctor.cpp	2026-05-20 11:59:15.000000000 -0300
+++ b/src/adjustyposfunctor.cpp	2026-09-06 20:38:33.311466245 -0300
@@ -14,6 +14,7 @@
 #include "calcstemfunctor.h"
 #include "div.h"
 #include "system.h"
+#include "vrvprobe.h"
 
 //----------------------------------------------------------------------------
 
@@ -47,6 +48,13 @@
 
     staffAlignment->SetYRel(staffAlignment->GetYRel() - m_cumulatedShift);
 
+    probe::Emit("{\"fn\":\"AdjustYPosVisitStaffAlignment\",\"staffN\":%d,\"defaultSpacing\":%d,"
+                "\"minSpacing\":%d,\"requestedSpacing\":%d,\"overflowAbove\":%d,\"overflowBelow\":%d,"
+                "\"overlap\":%d,\"cumulatedShift\":%d,\"yRel\":%d}\n",
+        staffAlignment->GetStaff() ? staffAlignment->GetStaff()->GetN() : -1, defaultSpacing, minSpacing,
+        staffAlignment->GetRequestedSpacing(), staffAlignment->GetOverflowAbove(), staffAlignment->GetOverflowBelow(),
+        staffAlignment->GetOverlap(), m_cumulatedShift, staffAlignment->GetYRel());
+
     return FUNCTOR_CONTINUE;
 }
 
diff -ruN a/src/calcbboxoverflowsfunctor.cpp b/src/calcbboxoverflowsfunctor.cpp
--- a/src/calcbboxoverflowsfunctor.cpp	2026-05-20 11:59:15.000000000 -0300
+++ b/src/calcbboxoverflowsfunctor.cpp	2026-09-06 20:51:24.908089736 -0300
@@ -13,6 +13,7 @@
 #include "layer.h"
 #include "staff.h"
 #include "verticalaligner.h"
+#include "vrvprobe.h"
 
 //----------------------------------------------------------------------------
 
@@ -133,6 +134,12 @@
     StaffAlignment *below = NULL;
     current->GetOverflowStaffAlignments(above, below);
 
+    probe::Emit("{\"fn\":\"CalcBBoxOverflowsAlign\",\"path\":\"%s\",\"id\":\"%s\","
+                "\"class\":\"%s\",\"aboveN\":%d,\"belowN\":%d,\"crossStaff\":%s}\n",
+        probe::Path(current).c_str(), probe::Id(current).c_str(), current->GetClassName().c_str(),
+        (above && above->GetStaff()) ? above->GetStaff()->GetN() : -1,
+        (below && below->GetStaff()) ? below->GetStaff()->GetN() : -1, current->m_crossStaff ? "true" : "false");
+
     bool isScoreDefClef = false;
     // Exception for the scoreDef clef where we do not want to take into account the general overflow
     // We have instead distinct members in StaffAlignment to store them
@@ -151,6 +158,10 @@
                 above->SetOverflowAbove(overflowAbove);
             }
             above->AddBBoxAbove(current);
+            probe::Emit("{\"fn\":\"CalcBBoxOverflowsSet\",\"path\":\"%s\",\"id\":\"%s\","
+                        "\"class\":\"%s\",\"side\":\"above\",\"staffN\":%d,\"overflow\":%d}\n",
+                probe::Path(current).c_str(), probe::Id(current).c_str(), current->GetClassName().c_str(),
+                above->GetStaff() ? above->GetStaff()->GetN() : -1, overflowAbove);
         }
     }
 
