diff -ruN a/src/calcstemfunctor.cpp b/src/calcstemfunctor.cpp
--- a/src/calcstemfunctor.cpp	2026-05-20 11:59:15.000000000 -0300
+++ b/src/calcstemfunctor.cpp	2026-08-31 21:59:42.011402668 -0300
@@ -97,6 +97,12 @@
     firstSegment->InitCoordRefs(&coord);
     firstSegment->CalcBeam(layer, staff, m_doc, beamSpan, beamSpan->GetPlace());
 
+    fprintf(stderr,
+        "PROBE0541A beamSpan=%s beamedElements=%zu coords=%zu measure=%s drawingPlace=%d\n",
+        beamSpan->GetID().c_str(), beamSpan->GetBeamedElements().size(),
+        beamSpan->m_beamElementCoords.size(), measure ? measure->GetID().c_str() : "NULL",
+        (int)beamSpan->m_drawingPlace);
+
     return FUNCTOR_CONTINUE;
 }
 
diff -ruN a/src/view_beam.cpp b/src/view_beam.cpp
--- a/src/view_beam.cpp	2026-05-20 11:59:15.000000000 -0300
+++ b/src/view_beam.cpp	2026-08-31 21:59:47.235311469 -0300
@@ -441,6 +441,7 @@
     }
 
     BeamSpanSegment *segment = beamSpan->GetSegmentForSystem(system);
+    fprintf(stderr, "PROBE0541B beamSpan=%s segmentFound=%d\n", beamSpan->GetID().c_str(), segment ? 1 : 0);
     if (segment) {
         // Reset current segment and set coordinates based on stored begin/end iterators for the ElementCoords
         segment->Reset();
